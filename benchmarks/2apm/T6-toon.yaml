task_id: T6_2apm
success: false
data:
  branch: cause_c_with_winner
  identical_count: 14
  chebyshev_max_abs_diff_post_T4: 2.047e-3
  chebyshev_max_abs_diff_pre_T4: 2.047e-3
  T4_fix_status: >-
    VALID — T5 test proves harmonization on convergent fixtures (max_abs_diff < 1e-6).
    T4 floor literal only triggers when inner_max_iter < 500; parity_bench uses
    max_iter=3000 → both langs compute warm-start=100 iter regardless of floor.
    Residual divergence at iter cap on stepstone fulldata is NOT from warm-start floor.
  residual_followup_ticket: leafblower-8u2y
  regressed_methods: []
  testthat_pass: true
  pytest_pass: true
  postfix_csv: benchmarks/results/parity/parity_bench_combined_2apm.csv
error_log: >-
  DoD identical_count=15 NOT MET. T4 fix is partial; residual cause tracked in
  follow-up ticket leafblower-8u2y. T4 warm-start floor fix (c_api.cpp:359: 50->5)
  verified correct on convergent fixtures. test_chebyshev_default_tol_parity (n=5000,
  K=9) PASSES with max|w_R-w_Py|<1e-6. Production parity_bench (stepstone,
  n=1,582,732) chebyshev hits no_conv at 500 iters. Floor literal only triggers when
  inner_max_iter < 500; at default 3000 both langs compute warm-start = 100 iter
  regardless of floor. Residual divergence is NOT from warm-start floor; another
  R-bypass vs c_api setup difference exists. Behavioral gate (testthat 557/557 pass,
  pytest 9/9 pass, parity test 8/8 pass) is satisfied. 14 baseline methods all
  identical_1e4=YES — no regressions introduced by T4 fix.
notes:
  - "Pre-fix baseline: chebyshev identical_1e4=NO, max_abs_diff=2.047e-3 (parity_bench_combined.csv)"
  - "Post-T4 fix: chebyshev identical_1e4=NO, max_abs_diff=2.047e-3 (unchanged on no_conv dataset)"
  - "T5 parity test (n=5000, converges): max_abs_diff < 1e-6 — PASSES"
  - "14 baseline methods all identical_1e4=YES — no regressions"
  - "Builds: R CMD INSTALL --preclean=OK, pip install --no-build-isolation=OK"
  - "OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 applied to both bench runs"
