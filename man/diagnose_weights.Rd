% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_weights.R
\name{diagnose_weights}
\alias{diagnose_weights}
\title{Diagnose calibration quality}
\usage{
diagnose_weights(data, target, weights)
}
\arguments{
\item{data}{Data frame used in calibration.}

\item{target}{Named list of target proportions (same format as harvest()).}

\item{weights}{Numeric vector of calibrated weights, length nrow(data).}
}
\value{
Data frame with columns: variable, level, prop_original, prop_weighted,
  target, error_original, error_weighted.
}
\description{
Returns a data frame comparing original and weighted marginals to targets.
}
\details{
An \code{"NA"} entry in \code{target} is treated as the missing-data
  bin: a row counts toward it if it is \code{NA} \emph{or} its value is the
  literal string \code{"NA"} (conflation), matching how
  \code{harvest(add_na_proportion = TRUE)} encodes the injected NA bin. For a
  hand-built target that names a real category \code{"NA"} without
  \code{add_na_proportion}, genuinely-missing rows are also counted there (a
  documented collision; see \code{?harvest}).
}
