---
phase: 01-verification-coverage-closed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_parity_weights.py
  - python/leafblower/test_parity_weights.py
autonomous: true
requirements: [leafblower-x7n8, SC1, SC3]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The blocking Definition-of-Done gate collects and executes the weight-vector parity tests; its Python half reports 149 collected tests, not 141 (leafblower-x7n8, D-08, D-09)."
    - "All nine non-AUTO rk_algorithm_t solvers — oris, raking, sinkhorn, chebyshev, greg, oris_soft, greenkhorn, logit, newton_kl — are compared R-vs-Python on the weight vector inside the parametrized matrix; a divergence in any one fails the suite (ROADMAP SC1)."
    - "No parity tolerance in the file is unexplained: the per-method relaxation for logit is either removed in favour of the uniform bound or carries a comment naming the measured conditioning mechanism (ROADMAP SC3, D-06, D-07)."
    - "A wrong repo-root path arithmetic after relocation fails collection loudly instead of degrading to a silent pytest.skip (D-08 reversibility hazard)."
  artifacts:
    - "python/leafblower/test_parity_weights.py — relocated from tests/test_parity_weights.py, git-detectable as a rename"
    - "tests/test_parity_weights.py — no longer present"
  key_links:
    - "python/leafblower/test_parity_weights.py REPO_ROOT -> <repo_root>/tests/parity/run_parity_r.R, run_oris_soft_r.R, run_chebyshev_r.R (all three derive from REPO_ROOT)"
    - "pytest rootdir python/ -> python/leafblower/ collection -> .coverage-thresholds.json enforcement.command"
---

<objective>
Bring the weight-vector parity file inside the blocking Definition-of-Done gate, then extend
it to the full nine-solver matrix and remove its one unexplained tolerance.

Purpose: `tests/test_parity_weights.py` holds the only R-vs-Python weight comparison for
`oris_soft` and `chebyshev`, and the gate never runs it. The tests pass, so nothing is red —
the assertions simply never execute. Extending a matrix the gate does not collect is a no-op,
so the relocation lands first and alone (D-09).

Output: `python/leafblower/test_parity_weights.py`, collected by the gate, parametrized over
all nine non-AUTO solvers, with a single uniform parity tolerance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-coverage-closed/01-CONTEXT.md
@.planning/phases/01-verification-coverage-closed/01-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Note |
|----------|------|------|
| `python/leafblower/test_parity_weights.py` | relocated file | `git mv` target from `tests/test_parity_weights.py`; must show as a rename under `git diff -M` |
| `tests/test_parity_weights.py` | deleted | source of the move |
| `REPO_ROOT` (module constant) | modified | recomputed to `Path(__file__).resolve().parents[2]` |
| module-import-time helper-existence assertion | new | fails collection loudly on wrong path arithmetic |
| `test_weight_parity` parametrize entries `chebyshev`, `greg`, `oris_soft` | new | 6 -> 9 entries |
| per-method logit tolerance branch | removed | replaced by the uniform `1e-10` bound plus a measured comment |

<tasks>

<task type="tracer">
  <name>Task 1: Relocate the parity file into the gate's collection root (leafblower-x7n8, D-08, D-09)</name>
  <files>tests/test_parity_weights.py, python/leafblower/test_parity_weights.py</files>
  <precondition>The installed R package and the editable Python wheel are current for this working tree — `R CMD INSTALL --preclean .` and `cd python && uv pip install -e . --reinstall-package leafblower` have been run since the last `src/` change. Both collection counts below assume a working `import leafblower` and a resolvable `Rscript`.</precondition>
  <read_first>
    - `tests/test_parity_weights.py` (all 352 lines — the file being moved; note line 26's ImportError fallback, line 29's `REPO_ROOT`, line 30 `R_HELPER`, line 103 `_ORIS_SOFT_R_HELPER`, line 254 `_CHEBYSHEV_R_HELPER`)
    - `python/leafblower/test_solver_parity.py:1-40` — the sibling file in the destination directory; its bare `from leafblower import harvest` (line 38, no fallback) is the import idiom to match
    - `python/conftest.py` — removes `python/` from `sys.path` so the installed wheel is imported, not the source tree
    - `conftest.py` (repo root) — same strip, for root-launched runs
    - `.coverage-thresholds.json` — `enforcement.command`, the blocking gate whose Python half must collect this file
    - `.planning/phases/01-verification-coverage-closed/01-PATTERNS.md` lines 19-39 — the relocation pattern and the path-arithmetic hazard
  </read_first>
  <action>
Move the file with `git mv tests/test_parity_weights.py python/leafblower/test_parity_weights.py` so the rename is detectable under `git diff -M`. This is a PURE relocation plus the path fix it forces — per D-09 add no assertions, no methods, and no tolerance changes in this task; those are Task 2 and Task 3.

Fix the repo-root arithmetic. The module currently derives its repo root from the file's grandparent, correct only while the file sits one level below the repository root. From `python/leafblower/` it must climb three levels instead: use `Path(__file__).resolve().parents[2]`. All three R-helper constants (`R_HELPER`, `_ORIS_SOFT_R_HELPER`, `_CHEBYSHEV_R_HELPER`) derive from `REPO_ROOT`, so this one change repairs all three; verify each still resolves under `<repo_root>/tests/parity/`.

Delete the `try/except ImportError` wrapper around the `leafblower` import and replace it with the bare `from leafblower import harvest` used by the sibling `test_solver_parity.py`. The fallback inserted a source-tree path that `python/conftest.py` deliberately strips, and after the move it would point at a non-existent nested directory — keeping it would fight the conftest and could import a source tree that has no compiled extension module.

Add a module-level assertion, evaluated at import time rather than inside any test body, that each of the three R-helper paths exists. D-08 records this relocation as costly-to-reverse precisely because the failure mode is silent: the helper-missing and missing-Rscript branches in this file route to `pytest.skip`, so wrong path arithmetic yields skipped tests rather than failures — the exact invisible-gap failure mode this phase exists to close. An import-time assertion converts that into a loud collection error.

Keep the single-thread BLAS `os.environ.setdefault` trio ahead of the `numpy` import verbatim; it must stay above any numpy import for OpenBLAS/MKL to pick it up at initialisation.
  </action>
  <verify>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest --collect-only -q 2>&amp;1 | tail -1</automated>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest leafblower/test_parity_weights.py -q 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd python && ... pytest --collect-only -q | tail -1` reads `149 tests collected` (141 pre-existing + 8 relocated). Reading `141` means the move did not land inside pytest's rootdir and the task is NOT done.
    - `cd python && ... pytest leafblower/test_parity_weights.py -q` reports 8 passed, 0 skipped, 0 failed. Any `skipped` count above 0 indicates the R-helper paths or `Rscript` did not resolve — investigate, do not accept.
    - `git diff -M --stat HEAD` shows `tests/test_parity_weights.py => python/leafblower/test_parity_weights.py` as a rename, not an add+delete pair.
    - `test -e tests/test_parity_weights.py` exits non-zero (the source path no longer exists).
    - `grep -c 'parents\[2\]' python/leafblower/test_parity_weights.py` is at least 1.
    - Deliberately breaking the path (temporarily changing `parents[2]` to `parents[1]`) makes collection ERROR rather than skip; restore afterwards. This proves the loud-failure property rather than assuming it.
  </acceptance_criteria>
  <done>The 8 weight-parity tests are collected and executed by the gate's own Python command; the gate's collected count reads 149; a wrong repo-root path fails collection loudly.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the parity matrix to all nine non-AUTO solvers (SC1)</name>
  <files>python/leafblower/test_parity_weights.py</files>
  <read_first>
    - `python/leafblower/test_parity_weights.py` lines 59-96 — `_r_weights` and `test_weight_parity`, the parametrize list and the assertion being extended
    - `tests/parity/run_parity_r.R` — the R-side helper; it forwards its third argument straight into `harvest(method=)`, so it already accepts any method string with no helper change needed
    - `src/leafblower.h:42-52` — the `rk_algorithm_t` enum, ground truth for the solver set; slots 2 and 7 are permanently reserved holes and must NOT appear
    - `.planning/phases/01-verification-coverage-closed/01-RESEARCH.md` lines 366-408 — the target parametrize list and the enum quotation
  </read_first>
  <action>
Add `chebyshev`, `greg`, and `oris_soft` to the `test_weight_parity` parametrize list, taking it from six entries to the full nine non-AUTO `rk_algorithm_t` solver values: greenkhorn, logit, raking, oris, sinkhorn, newton_kl, chebyshev, greg, oris_soft. Do not add entries for enum slots 2 or 7 — both are permanently reserved holes (removed `lbfgsb` and the withdrawn Epic-K `cp`), and `auto` is not a solver.

Note in a brief comment that `oris_soft` and `chebyshev` also have dedicated, more elaborate tests further down this same file (`test_oris_soft_default_tol_parity` asserts `alm_capacity_mu_final` and iteration counts; `test_chebyshev_default_tol_parity` asserts the warm-start floor mirror invariant). The parametrized entries are additive coverage — the plain weight-vector comparison on the shared four-margin synthetic fixture at the default bounds — not duplicates of those targeted regression locks.

The R helper needs no change: it passes its method argument through to `harvest`, and all three new methods have been confirmed to run through it and return a weight column.

Expected result, measured on this fixture before planning: every one of the nine methods agrees R-vs-Python to roughly 5e-15, five orders inside the uniform `1e-10` bound. If any method instead lands above `1e-10`, that is a genuine divergence — record the measured value and file a beads ticket per D-07 rather than widening the tolerance to absorb it.
  </action>
  <verify>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest leafblower/test_parity_weights.py -q 2>&amp;1 | tail -3</automated>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest --collect-only -q 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `pytest leafblower/test_parity_weights.py -q` reports 11 passed (9 parametrized + 2 dedicated), 0 failed, 0 skipped.
    - `pytest --collect-only -q | tail -1` reads `152 tests collected` (149 from Task 1 plus the 3 new parametrize entries).
    - `pytest leafblower/test_parity_weights.py --collect-only -q` lists ids containing `test_weight_parity[chebyshev]`, `test_weight_parity[greg]`, and `test_weight_parity[oris_soft]`.
    - The parametrize list contains exactly 9 method strings; `grep -c 'oris_soft' python/leafblower/test_parity_weights.py` is at least 2 (parametrize entry plus the pre-existing dedicated test).
    - No entry named `lbfgsb`, `grake`, or `cp` appears in the parametrize list.
  </acceptance_criteria>
  <done>All nine shipped non-AUTO solvers are compared R-vs-Python on the weight vector in the parametrized matrix, and a divergence in any one of them fails the suite.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the unexplained logit parity tolerance (SC3, D-06, D-07)</name>
  <files>python/leafblower/test_parity_weights.py</files>
  <read_first>
    - `python/leafblower/test_parity_weights.py` lines 87-96 — the diff computation and the tolerance expression being replaced
    - `.planning/phases/01-verification-coverage-closed/01-RESEARCH.md` lines 311-336 — Pitfall 3, the two candidate conditioning mechanisms and the requirement to measure before touching the number
    - `src/logit_calib.cpp:152-155` and `:241-250` — the Armijo constants (`kMaxHalvings`, `kArmijoC`) and the exponent clamp, the algorithmic half of the hypothesis
    - `python/CMakeLists.txt:99` — the Python build's `-O3`, the build-asymmetry half of the hypothesis
  </read_first>
  <action>
Measure before changing anything, per D-06. Run the parametrized parity test for logit and record the actual maximum absolute weight difference. The test already prints this value per method. Do not adjust the threshold until the measurement is in hand.

The measurement taken during planning on this exact fixture was 5.329e-15 for logit — five orders of magnitude inside the uniform bound, and in the same range as every other solver (all nine sit near 5e-15). Reproduce it; if your run agrees, the conclusion is that no conditioning mechanism justifies a three-orders-of-magnitude relaxation on this fixture, and the correct fix is to delete the special case entirely rather than to document it.

Replace the method-conditional tolerance expression with the single uniform bound of `1e-10` applied to every method. Add a comment recording: the measured value for logit, the date measured, that all nine solvers were measured in the same range, and that the relaxation was removed because no measured divergence supported it. Name the two mechanisms that were considered and found not to be load-bearing here — the R-versus-Python build asymmetry (Python compiles at `-O3`, R takes the user's level with no package-supplied `-O`), and the Newton-with-Armijo-line-search conditioning in the logit solver — so a future reader sees which hypotheses were tested rather than an unexplained constant.

If, contrary to the planning measurement, your run shows logit above `1e-10`: do NOT widen the tolerance silently and do NOT pull a solver or binding fix into this phase. Per D-07, file a beads ticket recording the measured delta and the suspected mechanism, set the tolerance to a documented value that cites that ticket ID in the comment, and note it in the summary. The suspected cause may itself be the dual dispatch path that Phase 2 unifies.
  </action>
  <verify>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest leafblower/test_parity_weights.py -q -s 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest leafblower/test_parity_weights.py -q` reports 11 passed, 0 failed, 0 skipped, with the uniform tolerance in force.
    - `grep -c 'method == "logit"' python/leafblower/test_parity_weights.py` reads `0` — the method-conditional tolerance expression is gone. (If instead the D-07 ticket branch fired, this criterion is replaced by: the surviving conditional carries a comment containing a `leafblower-` ticket ID.)
    - The `-s` run prints a logit line whose reported difference is below `1e-10`.
    - A comment within 8 lines of the tolerance constant contains a measured numeric value and the word `measured`; `grep -c 'measured' python/leafblower/test_parity_weights.py` is at least 1.
    - No tolerance constant in this file lacks an adjacent explanatory comment.
  </acceptance_criteria>
  <done>Every parity tolerance in the file is uniform or explained: the logit special case is removed on measured evidence, or it survives citing a filed ticket that records the divergence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller -> C API | Untrusted `double*` + `int n` cross here. Unchanged by this plan — no `src/`, `R/`, or binding code is modified. |
| Test process -> `Rscript` subprocess | Test-only; arguments are locally generated fixture paths under pytest's `tmp_path`, never user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `python/leafblower/test_parity_weights.py` subprocess invocation | low | accept | Arguments are pytest `tmp_path` paths and a literal method string from a fixed parametrize list; no external or user-controlled input reaches the command line. No new surface versus the pre-move file. |
| T-01-02 | Information Disclosure | relocated test file inside the installed package directory | low | mitigate | `python/pyproject.toml:34` `wheel.exclude = ["leafblower/test_*.py"]` already excludes `test_*.py` from the shipped wheel; the relocated file matches that pattern, so it stays out of the artifact. Verify the exclusion still matches after the move. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — this plan installs zero packages. No `Package Legitimacy Audit` is required; `testthat`, `pytest`, and R `stats` are all pre-existing declared dependencies. |

This phase adds test files only and changes no input parsing, no network, credential, or
file-write code, and does not touch the C API boundary. Per the phase's security research,
the applicable ASVS L1 categories (V2 Authentication, V3 Session, V4 Access Control,
V6 Cryptography) do not apply to a local single-machine statistical library, and V5 Input
Validation is unchanged — `src/calib_validate.cpp` is untouched.
</threat_model>

<verification>
- The gate's own Python command collects the parity file: `cd python && ... pytest --collect-only -q | tail -1` reads 152.
- The full Python suite is green: `cd python && ... pytest -q` reports 0 failed.
- `git diff -M --stat` shows the relocation as a rename.
- No solver, binding, or `src/` file appears in `git diff --name-only` for this plan — the phase is test-layer only.
</verification>

<success_criteria>
- ROADMAP SC1 satisfied: all nine non-AUTO solvers compared R-vs-Python on the weight vector at a uniform tolerance inside the parametrized matrix.
- ROADMAP SC3 satisfied: no unexplained tolerance survives in the file.
- `leafblower-x7n8` closable: the 8 previously-uncollected parity tests now run inside the blocking gate.
</success_criteria>

<output>
Create `.planning/phases/01-verification-coverage-closed/01-01-SUMMARY.md` when done.
Record the measured per-method parity differences for all nine solvers in the summary — they
are the baseline a future regression is judged against.
</output>
