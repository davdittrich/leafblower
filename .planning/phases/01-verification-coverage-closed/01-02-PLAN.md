---
phase: 01-verification-coverage-closed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python/leafblower/test_solver_parity.py
autonomous: true
requirements: [SC2]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "raking and sinkhorn are covered by the convergence-rule / max_error parity checks, so a per-method default-rule divergence between the R and Python bindings fails the suite (ROADMAP SC2)."
    - "Each new parity test runs the documented four-step protocol including the precheck-both-sides-converged step, so a non-converged run fails loudly instead of silently comparing two garbage iterates."
    - "The resolved default rule string is asserted on BOTH bindings for raking and sinkhorn, because a fixture that converges to machine precision cannot distinguish one stopping rule from another by weights alone."
  artifacts:
    - "python/leafblower/test_solver_parity.py::test_raking_parity"
    - "python/leafblower/test_solver_parity.py::test_sinkhorn_parity"
    - "python/leafblower/test_solver_parity.py::test_raking_default_rule_parity"
    - "python/leafblower/test_solver_parity.py::test_sinkhorn_default_rule_parity"
  key_links:
    - "_assert_parity() -> _run_py()/_run_r() -> shared C++17 core via both bindings"
    - "_CONV_TOL comment block -> measured per-method max_error values (must include raking and sinkhorn once added)"
---

<objective>
Close ROADMAP SC2: bring `raking` and `sinkhorn` into the convergence-rule and `max_error`
parity checks, which today cover only `logit`.

Purpose: `test_logit_default_rule_parity` exists because per-method default-rule resolution
can diverge between the R and Python bindings — each binding parses an empty convergence spec
independently. Only logit has that guard. A raking or sinkhorn default-rule drift ships
undetected today.

Output: four new test functions in `python/leafblower/test_solver_parity.py` and a
`_CONV_TOL` rationale block that names every method it now covers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-coverage-closed/01-CONTEXT.md
@.planning/phases/01-verification-coverage-closed/01-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Note |
|----------|------|------|
| `test_raking_parity` | new test function | explicit-spec weight parity, mirrors `test_chebyshev_parity` |
| `test_sinkhorn_parity` | new test function | explicit-spec weight parity |
| `test_raking_default_rule_parity` | new test function | default-rule lock, mirrors `test_logit_default_rule_parity` |
| `test_sinkhorn_default_rule_parity` | new test function | default-rule lock |
| `_CONV_TOL` rationale comment block | modified | extended with measured raking/sinkhorn `max_error` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit-spec weight parity for raking and sinkhorn (SC2)</name>
  <files>python/leafblower/test_solver_parity.py</files>
  <read_first>
    - `python/leafblower/test_solver_parity.py:1-29` — the module docstring documenting the four-step protocol; step 3 is the precheck-both-sides-converged requirement the ROADMAP's notes-for-planning calls out explicitly
    - `python/leafblower/test_solver_parity.py:78-107` — the shared `_A`/`_B`/`_TARGETS` fixture and the `_CONV_TOL` rationale block listing measured `max_error` per method
    - `python/leafblower/test_solver_parity.py:138-186` — `_run_r`, `_run_py`, `_assert_parity`, the triplet to reuse rather than re-implement
    - `python/leafblower/test_solver_parity.py:245-257` — `test_chebyshev_parity` and `test_greenkhorn_parity`, the one-line wrapper shape to copy
    - `.planning/phases/01-verification-coverage-closed/01-PATTERNS.md` lines 72-113 — the analog mapping and the warning that `_CONV_TOL` must be re-measured, not assumed
  </read_first>
  <behavior>
    - `test_raking_parity`: with the explicit improvement spec on both sides, R and Python weights agree to `rtol=1e-6`; both sides' `max_error` is below `_CONV_TOL` so the precheck passes rather than being skipped.
    - `test_sinkhorn_parity`: same, for sinkhorn.
    - Both fail loudly if either binding stops at a different iterate.
  </behavior>
  <action>
Add `test_raking_parity` and `test_sinkhorn_parity` as one-line wrappers delegating to the
existing `_assert_parity` helper, exactly the shape used by `test_chebyshev_parity` and
`test_greenkhorn_parity` in the same file. Do not write a new comparison harness — the
existing `_run_r`/`_run_py`/`_assert_parity` triplet already handles the `Rscript`
subprocess, the JSON marshalling, the length check, the convergence precheck, and the
`np.allclose` assertion.

Place both functions in the per-solver test section alongside the existing wrappers, keeping
the file's ordering readable. Give each a one-line docstring in the established style naming
the method and the tolerance.

Update the `_CONV_TOL` rationale block. That comment currently enumerates the measured
`max_error` for five methods and identifies greg's chi2-scaled one-shot residual as the
binding case with roughly two-fold headroom under the `0.01` threshold. Add the measured
values for raking and sinkhorn so the block stays a complete record of what the single global
threshold covers. Values measured on this fixture during planning: raking 5.551e-17 and
sinkhorn 1.110e-16 — the two tightest of every solver in the file, so greg remains the
binding case and the `0.01` threshold needs no change. Re-measure and use your own numbers;
if either lands above `0.01` the precheck assert fires loudly and that is a fixture or
convergence regression to investigate, not a threshold to raise.

Note that sinkhorn is an entropic solver, so the sum of its weights does not equal n by
construction — the same property the file already documents for greenkhorn. The parity
assertion remains valid because both bindings call the same shared core; the `max_error`
precheck uses the marginal-error metric, not a sum-of-weights check.
  </action>
  <verify>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest leafblower/test_solver_parity.py -q 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest leafblower/test_solver_parity.py --collect-only -q` lists ids `test_raking_parity` and `test_sinkhorn_parity`.
    - `pytest leafblower/test_solver_parity.py -q` reports 0 failed and 0 skipped, with 2 more tests than before this task.
    - Both new functions delegate to `_assert_parity`; `grep -c '_assert_parity' python/leafblower/test_solver_parity.py` increases by at least 2 relative to the pre-task file.
    - The `_CONV_TOL` comment block names both `raking` and `sinkhorn` with a numeric measured value each: `grep -A22 '_CONV_TOL is correct\|Measured on' python/leafblower/test_solver_parity.py | grep -c 'raking\|sinkhorn'` is at least 2.
    - No new subprocess helper, comparison function, or epsilon-diff helper is introduced; the file's helper count is unchanged.
  </acceptance_criteria>
  <done>raking and sinkhorn are covered by the `max_error` precheck plus weight-parity assertion, using the existing four-step protocol unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the default-rule resolution for raking and sinkhorn (SC2)</name>
  <files>python/leafblower/test_solver_parity.py</files>
  <read_first>
    - `python/leafblower/test_solver_parity.py:212-242` — `test_logit_default_rule_parity`, the exact template: it asserts the resolved rule string on BOTH bindings and only then asserts weight parity, because weights alone cannot distinguish stopping rules on a fixture that converges to machine precision
    - `python/leafblower/test_solver_parity.py:157-168` — `_run_py`, and how `convergence={}` exercises the per-method default path
    - `python/leafblower/test_solver_parity.py:119-154` — `_R_TEMPLATE` and `_run_r`; the template already extracts `ri$convergence_used$rule`, and `conv_r="list()"` is the R-side empty spec
    - `.planning/phases/01-verification-coverage-closed/01-PATTERNS.md` lines 104-111 — the default-rule lock pattern
  </read_first>
  <behavior>
    - `test_raking_default_rule_parity`: `harvest` with an empty convergence spec resolves to the same rule string in R and in Python, and the weights then match at `rtol=1e-6`.
    - `test_sinkhorn_default_rule_parity`: same, for sinkhorn.
    - A future change that makes one binding default raking to a different stopping rule fails the assertion even though the weights would still agree.
  </behavior>
  <action>
Add `test_raking_default_rule_parity` and `test_sinkhorn_default_rule_parity`, mirroring
`test_logit_default_rule_parity` structurally: first call the Python runner with an empty
convergence dict and assert the resolved rule string read back from `convergence_used`, then
call the R runner with the empty R list expression and assert the rule string it reports, and
only then delegate to `_assert_parity` with both empty specs for the weight comparison.

The rule-string assertion is the point of these tests and must not be dropped in favour of
weight parity alone. On this shared fixture both methods converge to roughly 1e-16, so a
threshold rule and an improvement rule stop at the same iterate and produce identical
weights — weight parity therefore cannot detect a rule divergence, which is precisely the
reasoning recorded in the logit test's own docstring.

Both methods resolved to the improvement rule on both bindings when measured during planning,
with weights agreeing to roughly 2e-16 for raking and 4e-16 for sinkhorn. Assert the rule
string you actually observe rather than hard-coding from this note, and if a binding resolves
to something different from its counterpart, that is the divergence SC2 exists to catch —
report it as a finding and file a beads ticket rather than relaxing the assertion to match.

Give each test a docstring explaining, as the logit one does, why the rule string is asserted
separately from the weights.
  </action>
  <verify>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest leafblower/test_solver_parity.py -q 2>&amp;1 | tail -3</automated>
    <automated>cd python &amp;&amp; env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 ./.venv/bin/python -m pytest -q 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest leafblower/test_solver_parity.py --collect-only -q` lists `test_raking_default_rule_parity` and `test_sinkhorn_default_rule_parity`.
    - `pytest leafblower/test_solver_parity.py -q` reports 0 failed, 0 skipped, and 4 more tests in total than before this plan started.
    - Each new test asserts on `convergence_used` for the Python side and on the R-side reported rule before calling `_assert_parity`; `grep -c 'convergence_used' python/leafblower/test_solver_parity.py` is at least 3.
    - Temporarily changing one expected rule string to a wrong value makes exactly that test fail; restore afterwards. This proves the assertion is load-bearing rather than tautological.
    - The full Python suite `cd python && ... pytest -q` reports 0 failed.
  </acceptance_criteria>
  <done>A per-method default-rule divergence between the R and Python bindings fails the suite for raking and sinkhorn, not only for logit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller -> C API | Untrusted `double*` + `int n` cross here. Unchanged by this plan — no `src/`, `R/`, or binding code is modified. |
| Test process -> `Rscript -e` subprocess | Test-only. The R expression is built from a module-level constant fixture and a method string from a fixed literal set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | `_R_TEMPLATE` string interpolation in `_run_r` | low | accept | The interpolated values are a module-level JSON constant and a method name from a closed literal set inside the test file; no external, network, or user input reaches the R expression. Pre-existing pattern, unchanged by this plan. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — this plan installs zero packages. `pytest` and `numpy`/`pandas` are pre-existing declared dependencies. |

This plan adds four test functions and one comment block. It changes no input parsing, no
network, credential, or file-write code, and does not touch the C API boundary. Applicable
ASVS L1 categories (V2, V3, V4, V6) do not apply to a local statistical library; V5 Input
Validation is unchanged.
</threat_model>

<verification>
- `cd python && ... pytest leafblower/test_solver_parity.py -q` is green with 4 more tests than at plan start.
- `cd python && ... pytest -q` (the gate's Python half) reports 0 failed.
- `git diff --name-only` for this plan lists exactly one file.
</verification>

<success_criteria>
- ROADMAP SC2 satisfied: raking and sinkhorn are covered by the convergence-rule and `max_error` parity checks, so a per-method default-rule divergence between bindings fails the suite.
- The `_CONV_TOL` rationale block remains a complete, measured record of every method the threshold covers.
</success_criteria>

<output>
Create `.planning/phases/01-verification-coverage-closed/01-02-SUMMARY.md` when done.
Record the measured `max_error` and resolved default rule for raking and sinkhorn.
</output>
