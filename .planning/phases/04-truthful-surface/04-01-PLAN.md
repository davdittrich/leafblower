---
phase: 04-truthful-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/harvest.R
  - tests/testthat/test-harvest-rval.R
  - tests/testthat/test-logit.R
  - NEWS.md
autonomous: false
requirements: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "harvest(..., weights = w) raises an informative stop() naming design_weights= instead of silently ignoring the argument via ..."
    - "The one existing in-repo caller of the old weights= fallback (eb79.18) is renamed to design_weights= in the SAME commit as the guard, so the DoD gate does not go red"
  artifacts:
    - R/harvest.R
    - tests/testthat/test-harvest-rval.R
    - tests/testthat/test-logit.R
    - NEWS.md
  key_links:
    - "R/harvest.R's RVAL.2 dots-check gates the new weights= stop() before the existing generic unknown-argument warning fires"
---

<objective>
Close SC3 of Phase 4 (Truthful Surface): `harvest(..., weights = w)` currently falls silently
into `...`, trips only the non-fatal generic RVAL.2 "unknown argument(s) ignored" warning, and
the call proceeds with a plausible-but-wrong unweighted result. Add a hard `stop()` naming
`design_weights=` explicitly (D-04), and rename the one existing test caller that currently
relies on the old silent-fallback behavior (`tests/testthat/test-logit.R:195`, `eb79.18`) in
the same plan so the suite does not go red.

This is the phase's tracer task (TRACER_MODE): it is the one fix in this defect-driven phase
with a live, breaking-change-adjacent decision and a real test dependency — proven first,
before the lower-risk doc/comment fixes in `04-02-PLAN.md`.

Purpose: an R-level typo (`weights=` instead of `design_weights=`) currently produces a
statistically wrong result with no error and an easy-to-miss warning — SC3 makes this a hard
failure at the API boundary (V5 input validation).
Output: `R/harvest.R`'s RVAL.2 block gains a named-typo guard; `tests/testthat/test-harvest-rval.R`
gains a dedicated `RVAL.4` regression test; `tests/testthat/test-logit.R`'s `eb79.18` caller is
renamed; `NEWS.md` records the breaking change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-truthful-surface/04-CONTEXT.md
@.planning/phases/04-truthful-surface/04-RESEARCH.md
@.planning/phases/04-truthful-surface/04-PATTERNS.md
@R/harvest.R
@tests/testthat/test-harvest-rval.R
@tests/testthat/test-logit.R
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the weights= breaking-change decision (D-04)</name>
  <decision>
    Should `harvest(..., weights = w)` raise a hard `stop()` (breaking change) instead of the
    current silent fallthrough into `...` (which today only trips the generic, non-fatal RVAL.2
    "unknown argument(s) ignored" warning)?
  </decision>
  <context>
    CONTEXT.md D-04 already specifies the hard-`stop()` behavior and flags it "costly" —
    "changing an established R package's argument-handling from warn to error is a breaking
    change for any caller currently relying on (or silently tripping) the old fallback." D-04
    requires this checkpoint to run BEFORE the implementing task, with confirmation that no
    in-repo test or benchmark currently passes `weights=` expecting the old silent-fallback
    behavior. RESEARCH.md's repo-wide grep this session confirms exactly ONE such caller exists:
    `tests/testthat/test-logit.R:195` (`eb79.18: consistent collinear with HETEROGENEOUS design
    weights still reaches feasibility`), which passes `weights = base_w` and wraps the call in
    `suppressWarnings()` — already masking today's RVAL.2 warning. That caller will be renamed to
    `design_weights = base_w` in Task 2 of this same plan, in the same commit as the guard, so
    the DoD gate never goes red between the two changes.
  </context>
  <options>
    <option id="option-a">
      <name>Hard `stop()` naming `design_weights=` (D-04's default; SC3's literal wording)</name>
      <pros>Matches SC3 verbatim ("raises an informative error"); textbook V5 input-validation
      fix — reject unrecognized input at the trust boundary rather than silently coercing it;
      cannot be silently suppressed the way a warning can (the one real in-repo caller currently
      wraps its call in `suppressWarnings()`, which would otherwise mask a warning-only fix
      forever).</pros>
      <cons>Breaking change for any caller (in this repo or downstream) currently passing
      `weights=` and relying on the old silent-unweighted-fallback result — mitigated here by
      the repo-wide grep confirming exactly one such caller, fixed in the same commit, and by
      logging the change in `NEWS.md`'s `## Breaking changes` section.</cons>
    </option>
    <option id="option-b">
      <name>Sharpen the existing warning to explicitly name `design_weights=`, no `stop()`</name>
      <pros>Non-breaking; every existing caller keeps running.</pros>
      <cons>Does not satisfy SC3's literal wording ("raises an informative error" — a warning is
      not an error); remains silently swallowable by `suppressWarnings()`, exactly the pattern
      the one real in-repo caller (`eb79.18`) already uses today — the defect SC3 exists to close
      would still be reachable by any caller that wraps `harvest()` in `suppressWarnings()`.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Implement the weights= guard, RVAL.4 test, and eb79.18 rename</name>
  <files>R/harvest.R, tests/testthat/test-harvest-rval.R, tests/testthat/test-logit.R, NEWS.md</files>
  <read_first>
    R/harvest.R:266-307 (the `harvest()` signature — confirm no `weights=` formal exists, so a
    bare `weights=` call falls into `...` — and the RVAL.2 block: `dots <- list(...)` /
    `if (length(dots) > 0L) warning("harvest: unknown argument(s) ignored: ...")`);
    R/harvest.R:508 and :510-515 (the file's `stop("leafblower: <msg>", call. = FALSE)`
    convention, and the sibling `design_weights` + `start_weights` warning idiom to model tone
    on);
    tests/testthat/test-harvest-rval.R:35-46 (the `RVAL.2` block's `test_that` style and fixture
    shape to copy) and the file's full existing `test_that` inventory — RVAL.1 (lines 6, 20),
    RVAL.2 (35, 47), RVAL.3 (67, 92), META.2 (113, 124), dtkn.6 (134, 142) — for insertion-order
    reference (append after line 142);
    tests/testthat/test-logit.R:182-202 (the full `eb79.18` `test_that` block to edit) and
    tests/testthat/test-logit.R:164-179 (the sibling INCONSISTENT-margins test, which ALSO wraps
    in `suppressWarnings()` with no `weights=` argument at all — do not assume the wrap can be
    dropped from `eb79.18` without empirically checking);
    NEWS.md:1-27 (the top `# leafblower (development)` section's existing `## Breaking changes`
    block, 3 prior bullets, to append a 4th bullet to in the same style).
  </read_first>
  <behavior>
    - Test 1 (new, `RVAL.4` in test-harvest-rval.R): `harvest(df, tgt, weights = runif(100),
      convergence = list(absolute = 1e-4))` raises an error whose message matches the regexp
      `"design_weights"`.
    - Test 2 (existing, unaffected): all `RVAL.1`/`RVAL.2`/`RVAL.3`/`META.2`/`dtkn.6` blocks in
      test-harvest-rval.R continue to pass unchanged — the new guard only fires on the literal
      name `"weights"` in `names(dots)`, not on any other unknown-arg case RVAL.2 already covers.
    - Test 3 (existing, edited): `eb79.18` in test-logit.R still converges (`status == 0L`,
      `max_error < 1e-4`) after `weights = base_w` is renamed to `design_weights = base_w`.
  </behavior>
  <action>
    In R/harvest.R's RVAL.2 block (lines 303-306), insert, immediately before the existing
    `if (length(dots) > 0L) warning(...)` line: `if ("weights" %in% names(dots)) stop("leafblower:
    unrecognized argument 'weights' — did you mean 'design_weights'? harvest() takes
    per-observation design weights via design_weights=, not weights=.", call. = FALSE)` — base R
    `stop()` only, matching the file's own convention (no `cli` dependency exists in
    `DESCRIPTION`). In tests/testthat/test-harvest-rval.R, append a new block after the file's
    last existing `test_that` (line 142+): `test_that("RVAL.4: bare weights= arg errors naming
    design_weights", { ... })`, calling `harvest()` with a `weights = runif(100)` argument inside
    `expect_error(..., regexp = "design_weights")`, reusing RVAL.2's factor-column fixture shape
    (`df <- data.frame(x = factor(sample(c("a","b"), 100, replace = TRUE)))`,
    `tgt <- list(x = c(a = 0.5, b = 0.5))`). In tests/testthat/test-logit.R:195, rename
    `weights = base_w` to `design_weights = base_w` inside the `eb79.18` `test_that` block; after
    the rename, run the test once with `suppressWarnings()` removed to check empirically whether
    any warning still fires — keep `suppressWarnings()` in place unless that run confirms zero
    warnings (do not strip it by inspection alone; the sibling test at lines 164-179 uses
    `suppressWarnings()` with no `weights=` argument at all, so an independent warning source may
    legitimately remain). Add one new bullet to NEWS.md's top `## Breaking changes` block (after
    the existing 3, matching their exact bullet-and-indent style): `* harvest() now errors on a
    bare weights= argument (previously silently ignored via the generic unknown-argument
    warning) — rename to design_weights=.`
  </action>
  <verify>
    <automated>Rscript -e "testthat::test_dir('tests/testthat', filter='harvest-rval')" &amp;&amp; Rscript -e "testthat::test_dir('tests/testthat', filter='logit')"</automated>
  </verify>
  <acceptance_criteria>
    `harvest(df, tgt, weights = runif(nrow(df)))` raises an error (not a warning) whose message
    contains the literal string `design_weights`; both filtered testthat runs above report 0
    FAIL; `git diff R/harvest.R` shows only the one new `if` guard inserted before the existing
    RVAL.2 warning, no other lines changed; NEWS.md's `## Breaking changes` block has 4 bullets
    (3 pre-existing + 1 new) in the file's existing style.
  </acceptance_criteria>
  <done>
    `stop()` guard live in R/harvest.R naming `design_weights=`; `RVAL.4` test exists and passes;
    `eb79.18` passes under `design_weights=`; NEWS.md records the breaking change.
  </done>
  <reversibility rating="costly">
    Changing established argument-handling from warn to error is a breaking change for any
    caller relying on the old silent-fallback result — gated by Task 1's `checkpoint:decision`
    per D-04, and trivially `git revert`-able as code (the "costly" dimension is downstream
    caller impact, not implementation risk).
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → `harvest()` public R API | Untrusted/unvalidated keyword arguments enter via `...`; an unrecognized-but-plausible argument name (`weights=` vs the real `design_weights=`) can silently alter the effective computation with no signal to the caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `R/harvest.R` `harvest()` `...` dots handling | high | mitigate | Explicit `if ("weights" %in% names(dots)) stop(...)` reject-on-known-typo check inserted into the existing RVAL.2 block, ahead of the generic unknown-arg warning — Task 2. This is a correctness/trust defect (a silently-wrong statistical result), not a network-facing vulnerability; classified `high` because it produces a plausible-but-wrong result with no signal, the exact failure mode V5 input validation exists to close. |
| T-04-02 | Repudiation (of the fix itself) | Test suite | low | accept | `RVAL.4` regression test (Task 2) makes the guard's presence durable, not just point-in-time correct; no further mitigation needed. |

</threat_model>

<verification>
Both filtered testthat runs (`harvest-rval`, `logit`) report 0 FAIL after Task 2. Before
`/gsd:verify-work`, run the full DoD gate: `R CMD INSTALL --preclean .` (only if a build-affecting
file changed — this plan does not touch any `.cpp`/`.h`), `Rscript -e "devtools::test()"`
(0 FAIL), and the Python parity suite with single-thread BLAS (0 FAIL; unaffected by this plan's
R-only changes, run as part of the phase-level DoD gate, not per-plan).
</verification>

<success_criteria>
SC3 (ROADMAP Phase 4): `harvest(..., weights = w)` raises an informative error naming
`design_weights=` instead of silently ignoring the argument and returning a plausible-but-wrong
unweighted result.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

- `R/harvest.R` — RVAL.2 block gains a `stop()` guard rejecting bare `weights=`, naming
  `design_weights=` (new `if` statement, ~5 lines).
- `tests/testthat/test-harvest-rval.R` — new `test_that("RVAL.4: bare weights= arg errors
  naming design_weights", ...)` block appended after the file's existing RVAL.1-3/META.2/dtkn.6
  tests.
- `tests/testthat/test-logit.R` — `eb79.18` test's `weights = base_w` renamed to
  `design_weights = base_w`.
- `NEWS.md` — one new bullet in the top `## Breaking changes` section documenting the
  `weights=` → hard-error behavior change.
</artifacts_produced>

<output>
Create `.planning/phases/04-truthful-surface/04-01-SUMMARY.md` when done.
</output>
