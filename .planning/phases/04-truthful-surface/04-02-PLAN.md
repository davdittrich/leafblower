---
phase: 04-truthful-surface
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/raking.md
  - src/leafblower.h
  - CLAUDE.md
autonomous: true
requirements: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No live document attributes the unimplemented outer entropic-proximal-point loop, or any other unshipped capability, to ORIS or to a removed solver"
    - "A developer reading rk_algorithm_t sees why slot 7 is a hole, matching slot 2's documented convention, and cannot reuse either value by accident"
    - "An audit of README, NEWS.md, man/ and docs/ finds no surviving reference to grake, lbfgsb or cp as available methods"
  artifacts:
    - docs/raking.md
    - src/leafblower.h
    - CLAUDE.md
  key_links:
    - "src/leafblower.h's enum comment is the single source both the R and Python build sites compile against — editing it forces a full rebuild of both"
---

<objective>
Close SC1, SC2, and SC4 of Phase 4 (Truthful Surface). Sequenced after `04-01-PLAN.md`'s
tracer (the `weights=` guard, SC3) per TRACER_MODE — these are the phase's lower-risk
doc/comment expansion fixes, with no shared files and no dependency on 04-01's outcome beyond
execution ordering.

SC1: delete `docs/raking.md`'s §8.2/§12 passage, which currently attributes the unimplemented
outer entropic-proximal-point loop (the paper's headline contribution) to ORIS and recommends
the removed L-BFGS-B solver as "the definitive state-of-the-art" — the exact claim the
`ieppa` → `oris` rename exists to repudiate.

SC2: annotate `rk_algorithm_t` slot 7 (the removed `RK_ALG_GRAKE`) in `src/leafblower.h` and
`CLAUDE.md`, matching slot 2's existing convention, so neither hole can be reused by accident.

SC4: re-run the grake/lbfgsb/cp audit across `README.md`, `NEWS.md`, `man/`, `docs/methods/`,
`docs/raking.md`, and `docs/performance.md` and confirm zero surviving true-positive references
to any of the three as an available method (per D-03, `docs/superpowers/specs/` is out of
scope — historical planning records).

Purpose: nothing documented should be untrue, and a developer should not be able to reuse a
reserved enum slot by accident.
Output: `docs/raking.md` with §8.2/§12 removed; `src/leafblower.h` and `CLAUDE.md` annotating
slot 7; confirmation that no other true-positive grake/lbfgsb/cp reference exists in scope.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-truthful-surface/04-CONTEXT.md
@.planning/phases/04-truthful-surface/04-RESEARCH.md
@.planning/phases/04-truthful-surface/04-PATTERNS.md
@docs/raking.md
@docs/methods/oris.md
@src/leafblower.h
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete docs/raking.md §8.2/§12 misattribution (SC1)</name>
  <files>docs/raking.md</files>
  <read_first>
    docs/raking.md in full (10 physical lines / 43,826 bytes; the file has ZERO markdown
    headings — section markers like "8.2" and "12." are inline prose, not `#`-headers, so
    line-number edits will silently corrupt the file; edit by exact quoted substring only).
    docs/methods/oris.md:127 — the ground-truth, ALREADY-CORRECT description of what ORIS does
    NOT implement ("That paper's headline contribution — an outer inexact entropic-proximal-point
    loop ... is NOT implemented here ... mathematically inert" at `C = 0`). Per D-01: this is the
    single authoritative description. Do NOT copy or paraphrase it into docs/raking.md — delete
    the false passage, do not replace it with a correct one.
  </read_first>
  <action>
    Delete two passages from docs/raking.md by exact-substring boundary match (confirmed this
    session at byte offsets 30793-32701 for §8.2, 41273-43826/EOF for §12; do not rely on these
    byte offsets directly — re-locate by the substrings below, since any earlier edit in the
    session shifts them):
    (1) Delete everything from the substring "8.2 The ORIS Solver (renamed from iEPPA)" up to
    (but not including) "9. Real-World Architectures for Capacity-Constrained Sinkhorn" — this
    removes the passage claiming ORIS "achieves this by wrapping a proximal point framework
    around an inner solver" and "routinely and conclusively outperforms ... Gurobi". After the
    edit, the file must read directly from "...strictly respecting the capacity constraints."
    (end of §8.1) into "9. Real-World Architectures..." with nothing in between.
    (2) Delete everything from the substring "12. Synthesis and Final Conclusions" through the
    end of the file (the file's final section — "...massively complex, bounded survey
    calibrations." is the last sentence). This removes the passage calling L-BFGS-B "the
    definitive state-of-the-art" and recommending "offloading the L-BFGS-B or ORIS numerical
    routines" — both false/misleading for this package. After the edit, the file must end at
    "...without requiring external hardware accelerators." (end of §11).
    Do NOT touch §6.2 ("Quasi-Newton Methods: L-BFGS and L-BFGS-B") or §11 ("Hardware
    Utilization, Software Implementations, and CPU Scaling") — these sections discuss L-BFGS-B
    as a general classical numerical technique (not an ORIS/leafblower capability claim) and are
    out of D-01's narrow §8.2/§12 scope; 9 non-scoped "L-BFGS-B" mentions remain in the file
    after this edit (in §6.2 and §11) — this is expected, per D-02's deferred broader-quality-
    review ticket, not a gap in this task.
  </action>
  <verify>
    <automated>grep -c "renamed from iEPPA" docs/raking.md; grep -ci Gurobi docs/raking.md; grep -c "definitive state-of-the-art" docs/raking.md; grep -c "8.2 The ORIS Solver" docs/raking.md; grep -c "12. Synthesis and Final Conclusions" docs/raking.md — all five must report 0</automated>
  </verify>
  <acceptance_criteria>
    All five greps above return 0. `grep -c "strictly respecting the capacity constraints" docs/raking.md`
    still returns 1 (§8.1 untouched). `grep -c "external hardware accelerators" docs/raking.md`
    still returns 1 (§11 untouched, now the file's last sentence). `git diff docs/raking.md`
    shows exactly two deletions and zero insertions/rewrites — no paraphrase of the deleted
    passage was added anywhere in the file.
  </acceptance_criteria>
  <done>
    No ORIS-labelled description of the unimplemented outer entropic-proximal-point loop, and
    no claim that L-BFGS-B is "the definitive state-of-the-art", survives anywhere in
    docs/raking.md; §8.1 and §9-§11 content is otherwise unchanged.
  </done>
  <reversibility rating="reversible">
    Doc-only deletion, trivially `git revert`-able; no downstream code or API depends on this
    file's prose.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Annotate rk_algorithm_t slot 7 (SC2) and re-audit grake/lbfgsb/cp (SC4)</name>
  <files>src/leafblower.h, CLAUDE.md</files>
  <read_first>
    src/leafblower.h:41-53 — the full `rk_algorithm_t` enum, read verbatim. Slot 2 already has
    an inline comment (`/* 2 = removed (was RK_ALG_LBFGSB) */` at line 44); slot 7 is simply
    absent — the enum jumps `RK_ALG_GREG = 6` (line 48) directly to `RK_ALG_ORIS_SOFT = 8`
    (line 49), with no comment marking the gap.
    CLAUDE.md:65 — the existing sentence to extend: "Algorithm slot 2 is reserved (LBFGSB
    removed). Do not reuse in `rk_algorithm_t` enum."
    04-RESEARCH.md's SC4 findings table (the full grake/lbfgsb/cp grep sweep already executed
    this session) — every existing hit outside docs/raking.md is a confirmed false positive:
    `grake_norm` (a live, unrelated convergence-metric field in `man/harvest.Rd:271`,
    `src/greg.cpp:153`, `src/logit_calib.cpp:558`), `survey::grake` (a comparative-methods
    reference to the R `survey` package's own internal function, `docs/methods/logit.md`),
    `L-BFGS-B`/`LBFGSB` in `docs/methods/00-overview.md:3,81` (correctly documents the removed
    status), `Gurobi`/`CPLEX` in `docs/methods/chebyshev.md:115,119` (comparative discussion of
    commercial LP solvers, not a leafblower capability claim), the `cp` file-path reference in
    `docs/performance.md:176` (a withdrawn research-spike report, correctly framed as rejected),
    and `method="grake"` in `tests/testthat/test-calibration-solvers.R` (a regression test
    confirming grake is REMOVED — not part of the SC4 audit surface per D-03, and not a doc
    claim). Do NOT touch any of these six.
  </read_first>
  <action>
    In src/leafblower.h, insert one comment-only line between line 48 (`RK_ALG_GREG = 6,`) and
    line 49 (`RK_ALG_ORIS_SOFT = 8,`): `/* 7 = removed (was RK_ALG_GRAKE) */` — matching the
    exact form of the existing slot-2 comment on line 44. No enum value changes; this is a
    comment-only insertion. In CLAUDE.md line 65, replace the sentence "Algorithm slot 2 is
    reserved (LBFGSB removed). Do not reuse in `rk_algorithm_t` enum." with "Algorithm slots 2
    and 7 are reserved (LBFGSB and GRAKE removed, respectively). Do not reuse in
    `rk_algorithm_t` enum." — one line, same convention, no new bullet added.
    Then re-run the SC4 audit: grep `grake`, `lbfgsb`/`LBFGSB` (case-insensitive), and
    word-boundary `\bcp\b` across `README.md`, `README.Rmd`, `NEWS.md`, `man/*.Rd`,
    `docs/methods/*.md`, `docs/raking.md`, and `docs/performance.md` (per D-03's exact file-set
    scope — `docs/superpowers/specs/` stays explicitly out of scope). Confirm every hit still
    matches one of the six documented false-positive classes above (`docs/raking.md`'s hits are
    already resolved by Task 1). If a NEW true-positive hit appears that RESEARCH.md's sweep did
    not find — a live claim that `grake`, `lbfgsb`, or `cp` is an available `method=` value — fix
    it in this same task using the same delete-don't-rewrite convention as Task 1; if none
    found, make no additional file edits beyond the two above.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; Rscript -e "devtools::test()"</automated>
  </verify>
  <acceptance_criteria>
    `git diff src/leafblower.h` shows exactly one added comment line and zero enum value
    changes. `grep -c "RK_ALG_GRAKE" src/leafblower.h` returns 1 (inside the new comment only —
    no enum value named `RK_ALG_GRAKE` exists). `grep -n "Algorithm slot" CLAUDE.md` shows both
    slot 2 and slot 7 named in one sentence. The re-run SC4 grep sweep across the seven in-scope
    file globs produces zero hits outside the six documented false-positive classes (and
    docs/raking.md, already clean per Task 1). Full R suite (`Rscript -e "devtools::test()"`)
    reports 0 FAIL (editing `leafblower.h` forces a rebuild of both R and Python extensions per
    CLAUDE.md's "Two build sites" note — the Python side is exercised by the phase-level DoD
    gate, not required per-task here since no `.py`/`.cpp` file changed).
  </acceptance_criteria>
  <done>
    Slot 7 documented in both `src/leafblower.h` and `CLAUDE.md`, matching slot 2's convention;
    enum values unchanged; SC4 re-audit finds zero surviving true-positive grake/lbfgsb/cp
    references outside the six pre-classified cases.
  </done>
  <reversibility rating="reversible">
    Comment-only header change and one project-doc sentence; trivially `git revert`-able. Forces
    a full rebuild of both build sites (no `-O` asymmetry risk — comment-only, no code-generation
    change).
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation reader → package capability model | A reader (human or an automated doc-consumer, e.g. an LLM agent deciding how to call this package) trusts written claims about what the software does; a false claim is a spoofed-capability risk, not a network-facing vulnerability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Spoofing (of capability) | `docs/raking.md` §8.2/§12 | medium | mitigate | Delete the false ORIS/L-BFGS-B capability claims (Task 1, SC1) rather than leave them to mislead a reader or an automated doc-consuming caller. |
| T-04-04 | Information Disclosure (N/A — no secrets/PII in scope) | — | low | accept | No security-relevant surface in this task's file set; comment-only header change and one project-doc sentence carry no trust-boundary crossing. |

</threat_model>

<verification>
`Rscript -e "devtools::test()"` reports 0 FAIL after Task 2 (Task 1 is doc-only, verified by
grep, not the R suite). Before `/gsd:verify-work`, run the full DoD gate including the Python
parity suite with single-thread BLAS (0 FAIL) — required because `src/leafblower.h` forces a
rebuild of both build sites.
</verification>

<success_criteria>
SC1 (ROADMAP Phase 4): No live document attributes the unimplemented outer entropic-proximal-
point loop, or any other unshipped capability, to ORIS or to a removed solver.
SC2 (ROADMAP Phase 4): A developer reading `rk_algorithm_t` sees why slot 7 is a hole, as they
already do for slot 2, and cannot reuse either value by accident.
SC4 (ROADMAP Phase 4): An audit of README, NEWS.md, man/ and docs/ finds no surviving reference
to grake, lbfgsb or cp as available methods.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

- `docs/raking.md` — §8.2 ("The ORIS Solver (renamed from iEPPA)") and §12 ("Synthesis and
  Final Conclusions") passages deleted; §8.1, §9-§11 unchanged.
- `src/leafblower.h` — new comment `/* 7 = removed (was RK_ALG_GRAKE) */` inserted between the
  `RK_ALG_GREG` and `RK_ALG_ORIS_SOFT` enum values; no enum value changes.
- `CLAUDE.md` — the existing slot-2 sentence extended to also name slot 7/GRAKE.
</artifacts_produced>

<output>
Create `.planning/phases/04-truthful-surface/04-02-SUMMARY.md` when done.
</output>
