---
phase: 02-one-engine-not-two
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - src/calib_dispatch.hpp
  - src/c_api.cpp
  - src/r_bridge.cpp
autonomous: true
requirements: [US-004]

estimate:
  tokens: 105000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R and Python both reach lbw::oris_solve for method oris and method oris_soft through lbw::dispatch_solver."
    - "R's oris and oris_soft results still report sraa_demoted, aa_accepted_count, the SOR diagnostics and the four ALM diagnostics with unchanged values."
    - "oris_soft still sets use_admm_capacity before the solve and oris still sets oris_auto_selected exactly as before, on both bridges."
    - statement: "The M_cell estimate is computed at most once per solve; the estimate cache is not evaluated twice by the migration."
      verification: backstop
    - statement: "Rf_error unwinding still releases every heap-backed local on both the error and the success path."
      verification: backstop
  artifacts:
    - "src/calib_dispatch.hpp :: dispatch_solver case arms for RK_ALG_ORIS and RK_ALG_ORIS_SOFT"
    - "src/calib_dispatch.hpp :: DispatchResult ALM / SRAA / SOR diagnostic members populated for the oris family"
  key_links:
    - "r_bridge.cpp / c_api.cpp oris + oris_soft arms -> lbw::dispatch_solver -> lbw::oris_solve"
    - "oris_soft capacity_penalty auto-resolution -> lbw::estimate_M_cell (single evaluation per solve)"
  prohibitions:
    - statement: "Do not create a new src/*.cpp translation unit."
      verification: flagged-unverified
    - statement: "Do not edit rk_params_t, rk_result_t or rk_algorithm_t in leafblower.h."
      verification: flagged-unverified
    - statement: "Do not move hot per-iteration solver code across a translation-unit boundary."
      verification: flagged-unverified
    - statement: "Do not use the errRp fast proxy for SRAA best-iterate selection — the metric selector is correct and has been re-broken twice."
      verification: flagged-unverified
    - statement: "Do not alter the ORIS ALM Newton step formula; it is verified correct for the un-normalized-KL generator."
      verification: flagged-unverified
---

<objective>
Expansion wave 3: migrate the oris family — `oris` and `oris_soft` — the two solvers with
the largest superset-only diagnostic surface (SRAA, SOR and ALM fields).

Purpose: with these two migrated, only `newton_kl` and the AUTO branch still bypass the
shared table.
Output: two more case arms in `lbw::dispatch_solver` plus the oris-family diagnostic
members of `DispatchResult`.

Follow the migration recipe stated in `02-03-PLAN.md`'s objective (steps 1-6): add the arm,
rewire `c_api.cpp`, rewire `r_bridge.cpp`, re-read both `Rf_error` swap-release blocks,
update the stale "mirrors" comment, one commit per solver.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-PATTERNS.md
@.planning/phases/02-one-engine-not-two/02-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan: `RK_ALG_ORIS` and `RK_ALG_ORIS_SOFT` case arms inside
`lbw::dispatch_solver`, and the oris-family diagnostic members of `lbw::DispatchResult`
(`alm_capacity_mu_final`, `alm_n_growth_events`, `alm_max_dual_norm`, `alm_sum_drift`,
`aa_accepted_count`, the `sor_*` set) — `src/calib_dispatch.hpp`. No new files, no
`leafblower.h` change. (Phase-wide list: plan 01's `lbw::DispatchResult` /
`lbw::dispatch_solver`; plan 02's two sync tests; plan 08's `test_single_dispatch_site.py`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate oris (the default method)</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 895-908 (the default/oris branch, which sets `st.oris_auto_selected` from whether the requested method was the explicit one, then calls `pack_oris_result`)
    - src/r_bridge.cpp lines 600-640 (the `pack_oris_result` lambda: the SOR diagnostics, `aa_accepted_count`, `sraa_demoted`, and the `best_weights` move)
    - src/c_api.cpp lines 95-135 (`pack_oris_result_c` — the C-side twin of that lambda; diff the two field-by-field and record any divergence)
    - src/c_api.cpp (the `RK_ALG_ORIS` arm)
    - src/oris.cpp (the `lbw::oris_solve` signature and `ORISResult`'s members) and src/oris_finalize.cpp lines 145-165
    - src/r_bridge.cpp lines 914-938 and 983-994
  </read_first>
  <behavior>
    - Test 1: R oris results are unchanged from the pre-migration baseline on the existing oris fixtures.
    - Test 2: All SOR diagnostics, `aa_accepted_count` and `sraa_demoted` still surface in R's result with the same values.
    - Test 3: R-vs-Python oris weight parity holds at rtol=1e-6, both sides converged.
    - Test 4: `oris_auto_selected` is still false for an explicit oris request and true for the routed-to-oris case.
  </behavior>
  <action>
    Apply the migration recipe to `oris` / `RK_ALG_ORIS`. Add the oris-family diagnostic
    members to `DispatchResult` (`alm_capacity_mu_final`, `alm_n_growth_events`,
    `alm_max_dual_norm`, `alm_sum_drift`, `aa_accepted_count`, and the `sor_*` group:
    omega_mean, any_latched, n_pinned_fb, n_warmup_fb, n_conv_fb, n_resid_grew,
    n_monotone_cd), each defaulting to the same value its `res_*` counterpart in
    `r_bridge.cpp` currently defaults to. The new arm absorbs what both
    `pack_oris_result` (R lambda) and `pack_oris_result_c` (C twin) do today — read both
    first and, if they differ in any field, record the divergence in the summary and adopt
    the R behaviour, since R's is what the existing fixtures pin. Leave the `st.accelerate`
    and `st.oris_auto_selected` assignments at the CALLER side for this task: the arm takes
    `st` as already-configured state and must not decide routing policy, which is what
    keeps the AUTO consolidation in plan 07 a pure move rather than a redesign. Do not
    touch `oris.cpp`, `oris_finalize.cpp` or `oris_trajectory.cpp` — in particular do not
    move any per-iteration loop out of `oris.cpp`, which is deliberately split so its hot
    solve stays co-located with its callees under a build with no LTO.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="oris", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RK_ALG_ORIS\b' src/calib_dispatch.hpp` is at least 1.
    - R oris testthat files: 0 FAIL, 0 ERROR.
    - `git diff --stat src/oris.cpp src/oris_finalize.cpp src/oris_trajectory.cpp` is empty.
    - Any `pack_oris_result` vs `pack_oris_result_c` field divergence is written into `02-05-SUMMARY.md`, even if the answer is "none".
    - `git diff --stat src/leafblower.h` empty; `ls src/*.cpp | wc -l` unchanged.
    - Exactly one commit for this solver.
  </acceptance_criteria>
  <done>oris routes through the shared table on both bridges with every diagnostic intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate oris_soft (ALM diagnostics + capacity auto-resolution), then run the full DoD gate</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 884-895 (the oris_soft branch: `oris_auto_selected=false`, `use_admm_capacity=true`, `pack_oris_result`, then the four ALM fields)
    - src/r_bridge.cpp lines 475-505 (the `capacity_penalty` auto-resolution and the `m_cell_est_cache` local that memoizes `lbw::estimate_M_cell` across the two sites that need it)
    - src/c_api.cpp lines 520-545 (the C-side capacity resolution — verify whether it memoizes the estimate or recomputes it, and record which)
    - src/cell_table.hpp or wherever `lbw::estimate_M_cell` is declared (its cost profile: it is once-per-solve, but the R side deliberately caches it because two sites need it)
    - .planning/phases/02-one-engine-not-two/02-RESEARCH.md (Pitfall 3)
    - src/r_bridge.cpp lines 914-938 and 983-994
    - .coverage-thresholds.json
  </read_first>
  <behavior>
    - Test 1: R oris_soft results, including the four ALM diagnostics, are unchanged from the pre-migration baseline.
    - Test 2: R-vs-Python oris_soft parity holds at rtol=1e-6.
    - Test 3: The auto-resolved `capacity_penalty` value for a given fixture is identical to the pre-migration value on both bridges.
    - Test 4: `lbw::estimate_M_cell` is evaluated at most once for a solve that needs it at two sites.
  </behavior>
  <action>
    Apply the migration recipe to `oris_soft` / `RK_ALG_ORIS_SOFT`, populating
    `DispatchResult`'s four ALM members from the solver result. Leave the
    `use_admm_capacity` and `oris_auto_selected` assignments and the `capacity_penalty`
    auto-resolution on the caller side for now — but while reading, determine and record
    whether the R and C sides resolve `capacity_penalty` to the same value and whether each
    memoizes `lbw::estimate_M_cell` or recomputes it. That comparison is the input plan 07
    needs to consolidate the estimate into one evaluation without introducing either a
    double-compute (a measurable cost on large K) or an eager compute on paths that never
    needed it. If the two sides resolve `capacity_penalty` differently, that is a live
    R-vs-Python divergence: report it as a finding and file a `bd` ticket rather than
    silently normalizing it inside this migration. Then run the full DoD gate for the wave,
    and additionally run the stepstone benchmark gate, since this is the wave that touches
    the oris hot path's caller.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)' &amp;&amp; cd python &amp;&amp; uv pip install -e . --reinstall-package leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RK_ALG_ORIS_SOFT' src/calib_dispatch.hpp` is at least 1.
    - `grep -c 'alm_capacity_mu_final' src/calib_dispatch.hpp` is at least 1.
    - Full DoD gate: R 0 FAIL / 0 ERROR, Python 0 failed.
    - `LBW_BENCH_GATE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="bench-gate", stop_on_failure=TRUE)'` reports no regression.
    - The `capacity_penalty` / `estimate_M_cell` comparison between the two bridges is written into `02-05-SUMMARY.md`.
    - Two commits exist for this plan, one per solver.
  </acceptance_criteria>
  <done>The oris family routes through the shared table, the full gate is green, and the stepstone benchmark shows no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays supplied by the host language; the only boundary this package has. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | (none applicable) | `lbw::dispatch_solver` oris / oris_soft arms | low | accept | No network, auth, persistence or injection surface exists in this package. The applicable ASVS-L1 control, V5 Input Validation via `lbw::validate_calibrate_inputs`, runs before dispatch on both bridges and is untouched by this plan. |

Package legitimacy gate: not applicable — no npm/pip/cargo package is installed by this plan.
</threat_model>

<verification>
- Full DoD gate green; stepstone benchmark shows no regression.
- All oris-family diagnostics unchanged in R's result.
- `leafblower.h` unchanged; no new `src/*.cpp`.
</verification>

<success_criteria>
- oris and oris_soft are each reached through `lbw::dispatch_solver` from both bridges.
- The `capacity_penalty` / `estimate_M_cell` behaviour of both bridges is documented for plan 07.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-05-SUMMARY.md` when done
</output>
