---
phase: 02-one-engine-not-two
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python/leafblower/test_core_sources_sync.py
  - python/leafblower/test_finalize_weights_sync.py
  - python/CMakeLists.txt
  - CLAUDE.md
  - python/leafblower/test_solver_parity.py
autonomous: true
requirements: [US-004]

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Adding a src/*.cpp without adding it to python/CMakeLists.txt CORE_SOURCES fails the DoD gate as a test failure, not as a link error after the R tests are already green (SC4)."
    - "Removing a solver's call to the shared finalize_weights helper fails the DoD gate (SC3)."
    - "A reader at the R-vs-Python parity assertion learns that Python builds at -O3 and R builds at the user's own -O level, and why the asymmetry is deliberate (SC2)."
    - statement: "newton_kl's absence from the finalize_weights set is explicit and reasoned in the test source, not a silent omission."
      verification: backstop
    - statement: "Both new tests are pure text scans — no compiled artifact, no BLAS threading dependence, so they cannot flake under parallel or interrupted runs."
      verification: backstop
  artifacts:
    - "python/leafblower/test_core_sources_sync.py :: test_core_sources_matches_src_glob"
    - "python/leafblower/test_finalize_weights_sync.py :: test_every_bounded_solver_calls_shared_finalize"
  key_links:
    - "test_core_sources_sync.py -> src/*.cpp glob and python/CMakeLists.txt CORE_SOURCES"
    - "test_finalize_weights_sync.py -> src/calib_dispatch.hpp finalize_weights[_buf]"
    - ".coverage-thresholds.json enforcement.command -> pytest -> both new tests"
  prohibitions:
    - statement: "Do not add an -O flag to R's PKG_CXXFLAGS (CRAN tools:::.check_make_vars rejects it) — SC2 is documentation, not equalization."
      verification: flagged-unverified
    - statement: "Do not add a call to the shared finalize helper inside newton_calib.cpp as part of this plan (scope: verification only, per D-03)."
      verification: flagged-unverified
    - statement: "Do not introduce a separate manual script or Makefile target for either check — both must ride the existing DoD gate (D-05)."
      verification: flagged-unverified
---

<objective>
Close SC3, SC4 and SC2 — the three non-dispatch success criteria — as regression-prevention
guards that ride the existing DoD gate.

Purpose: SC3 and SC4 are verification tasks, not code changes (D-03, D-05). SC2 is a
documentation decision the CRAN build constraint already forecloses.
Output: two new pytest modules and three documentation edits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / file | Kind | Location |
|---|---|---|
| `test_core_sources_matches_src_glob` | pytest fn | `python/leafblower/test_core_sources_sync.py` (new file) |
| `test_every_bounded_solver_calls_shared_finalize` | pytest fn | `python/leafblower/test_finalize_weights_sync.py` (new file) |
| `_SOLVERS_EXPECTED_TO_CALL_FINALIZE` | module constant | `python/leafblower/test_finalize_weights_sync.py` |

Created by other plans in this phase: `lbw::DispatchResult`, `lbw::dispatch_solver`
(plan 01); `python/leafblower/test_single_dispatch_site.py` (plan 07).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC4 — build-list divergence fails the gate</name>
  <files>python/leafblower/test_core_sources_sync.py</files>
  <read_first>
    - python/CMakeLists.txt lines 55-85 (the `CORE_SOURCES` list, and the comment explaining why `r_bridge.cpp` is excluded)
    - python/leafblower/test_returned_weights_invariant.py (the shape of a small single-purpose pytest module in this directory)
    - .planning/phases/02-one-engine-not-two/02-RESEARCH.md (Code Examples → SC4 sync-check target shape, which gives the exact intended body)
    - CLAUDE.md ("Two build sites for src/*.cpp" footgun)
  </read_first>
  <behavior>
    - Test 1: with the tree as-is, the two file sets are equal and the test passes.
    - Test 2: if a name is added to `src/*.cpp` and not to `CORE_SOURCES`, the assertion message names it under the missing-side set.
    - Test 3: `r_bridge.cpp` is excluded from the src side and its absence from `CORE_SOURCES` does not fail the test.
  </behavior>
  <action>
    Create `python/leafblower/test_core_sources_sync.py` implementing
    `test_core_sources_matches_src_glob`: resolve `REPO_ROOT` as
    `Path(__file__).resolve().parents[2]`, build `src_files` from
    `(REPO_ROOT / "src").glob("*.cpp")` names minus `{"r_bridge.cpp"}`, build `listed` by
    regex-extracting `../src/<name>.cpp` occurrences from
    `(REPO_ROOT / "python" / "CMakeLists.txt").read_text()`, and assert the two sets are
    equal with a message reporting both set differences by name (missing-from-CORE_SOURCES
    and extra-in-CORE_SOURCES). Add a file-header comment recording the verified 2026-08-15
    baseline: 18 files in `src/*.cpp`, 17 after excluding the R-only bridge, exactly
    matching the 17 entries of `CORE_SOURCES` — zero drift today, so this is pure
    regression prevention. Restrict the glob to `REPO_ROOT / "src"` only: the repository
    contains stale `*.Rcheck/` build-artifact trees, and a recursive glob would pick them
    up. Do not modify `python/CMakeLists.txt` in this task.
  </action>
  <verify>
    <automated>cd python &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest leafblower/test_core_sources_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The new test passes on the unmodified tree.
    - Temporarily creating `src/zzz_probe.cpp` makes the test FAIL with `zzz_probe.cpp` named in the message; the probe file is deleted before commit and `git status --porcelain src/` is empty afterwards.
    - The test performs no import of `leafblower` and needs no compiled module.
  </acceptance_criteria>
  <done>A build-list divergence is caught by pytest instead of by an undefined-symbol link error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC3 — the unit-mode water-fill stays a single source</name>
  <files>python/leafblower/test_finalize_weights_sync.py</files>
  <read_first>
    - src/calib_dispatch.hpp lines 359-433 (`finalize_weights` / `finalize_weights_buf` — the single shared implementation)
    - src/oris_finalize.cpp lines 145-165 (the "Delegate to the single source" call site)
    - src/raking.cpp lines 160-175 (`water_fill_cat` — the algorithm-internal IPF box projection that is NOT the finalize step; the test must not confuse the two)
    - src/newton_calib.cpp lines 855-885 (the violation COUNTING path — newton_kl never redistributes)
    - .planning/phases/02-one-engine-not-two/02-RESEARCH.md (Pitfall 2, Assumption A1)
    - .planning/phases/02-one-engine-not-two/02-CONTEXT.md (D-03, D-04)
  </read_first>
  <behavior>
    - Test 1: each of `oris_finalize.cpp`, `raking.cpp`, `chebyshev.cpp`, `greenkhorn.cpp`, `greg.cpp`, `logit_calib.cpp`, `sinkhorn.cpp` contains a call to the shared finalize helper.
    - Test 2: the helper itself is defined exactly once, in `src/calib_dispatch.hpp`.
    - Test 3: `newton_calib.cpp` is excluded by name, with the reason recorded in the source of the test.
  </behavior>
  <action>
    Create `python/leafblower/test_finalize_weights_sync.py` implementing
    `test_every_bounded_solver_calls_shared_finalize`. Define module constant
    `_SOLVERS_EXPECTED_TO_CALL_FINALIZE = {"oris_finalize.cpp", "raking.cpp",
    "chebyshev.cpp", "greenkhorn.cpp", "greg.cpp", "logit_calib.cpp", "sinkhorn.cpp"}` and
    assert, for each, that the file's text contains `finalize_weights` — failing with a
    message naming the file that stopped delegating. Add a second assertion that the
    definition site is unique: across `src/*.cpp` plus `src/*.hpp`, exactly one file
    contains an `inline void finalize_weights_buf(` definition, and that file is
    `calib_dispatch.hpp` — this is what actually enforces "exists once" rather than merely
    "is called". Record two facts in the test source as comments so a future reader cannot
    misread the exclusions: (a) `newton_calib.cpp` is deliberately absent because
    `newton_kl` is a smooth-dual Newton method with no box-constrained finalize step — it
    only counts bound violations and falls back to a no-convergence status above a 5%
    violation fraction, so it never redistributes per observation, and this is an
    accepted pre-existing gap (RESEARCH.md Pitfall 2 / Assumption A1), NOT evidence that
    `bounds_mode="unit"` is enforced for that method; (b) `raking.cpp`'s own
    `water_fill_cat` is different math — an inline per-margin IPF box projection internal
    to the algorithm — and is not what this test is asserting about. Additionally, file a
    `bd` ticket for the `newton_kl` unit-mode bounds gap (referencing `leafblower-og7d.5`,
    which already records the same observation) so the gap is tracked rather than absorbed
    into this phase's scope; put the new ticket ID in the test's comment. Per D-04, if
    reading the seven files reveals a genuine second copy of the finalize logic, stop and
    report it — that supersedes D-03 and changes this task from verification to
    consolidation.
  </action>
  <verify>
    <automated>cd python &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest leafblower/test_finalize_weights_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The new test passes on the unmodified tree.
    - `grep -c 'newton_calib' python/leafblower/test_finalize_weights_sync.py` is at least 1 (the exclusion is stated in the file, not implied by omission).
    - The uniqueness assertion resolves to exactly one definition file and that file is `src/calib_dispatch.hpp`.
    - A `bd` ticket exists for the `newton_kl` unit-mode bounds gap and its ID appears in the test source.
  </acceptance_criteria>
  <done>A future regression that re-copies the water-fill into a solver, or that silently drops a solver's delegation, fails the gate.</done>
</task>

<task type="auto">
  <name>Task 3: SC2 — document the optimization-level asymmetry at the parity assertion</name>
  <files>python/CMakeLists.txt, CLAUDE.md, python/leafblower/test_solver_parity.py</files>
  <read_first>
    - python/CMakeLists.txt lines 90-105 (the `target_compile_options(_leafblower PRIVATE -O3)` line and the surrounding `-mavx2` rationale comments)
    - src/Makevars.in lines 1-20 and configure lines 1-25 (the deliberate absence of any `-O` level on the R side)
    - CLAUDE.md (the "no -O optimization level" bullet under Architecture)
    - python/leafblower/test_solver_parity.py lines 1-30 (the file-header protocol comment where the note belongs)
    - .beads ticket leafblower-qzto
  </read_first>
  <action>
    Make the asymmetry a stated, bounded decision in the three places a reader can hit it.
    (1) In `CLAUDE.md`, amend the "no -O optimization level" bullet so it is scoped to R
    and states the Python side explicitly: the R build supplies no `-O` of its own because
    CRAN's `tools:::.check_make_vars` rejects `-O` flags in `PKG_CXXFLAGS` and R passes the
    user's or site's level through `$(CXXFLAGS)`, while `python/CMakeLists.txt` hard-sets
    `-O3` for the pybind11 module, which has no such constraint. (2) At the `-O3` line in
    `python/CMakeLists.txt`, add a comment naming the R-side counterpart and the CRAN rule
    that forecloses equalizing the two. (3) In the header comment of
    `python/leafblower/test_solver_parity.py` — the parity assertion SC2 names — record
    that the two binaries under comparison are compiled at different optimization levels by
    design, that the tolerances in this file are therefore the bound on how much that
    asymmetry is allowed to move a result, and that a parity failure here is evidence of a
    real divergence, not of the flag difference. Close or comment `leafblower-qzto` with
    the resolution. Do not change any compiler flag on either side.
  </action>
  <verify>
    <automated>grep -c 'check_make_vars' CLAUDE.md python/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - `git diff python/CMakeLists.txt` contains no change to any `target_compile_options` argument list — comment lines only.
    - `git diff src/Makevars.in configure` is empty.
    - `CLAUDE.md`'s optimization bullet names both build sites and both levels.
    - `python/leafblower/test_solver_parity.py`'s header states the asymmetry and what the tolerances bound.
    - `leafblower-qzto` records the resolution.
  </acceptance_criteria>
  <done>SC2 is satisfied in the "documented as a deliberate, bounded decision" branch its own wording allows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays supplied by the host language; the only boundary this package has. This plan adds no code on that path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | (none applicable) | new pytest modules, doc comments | low | accept | The two new tests read text files and assert on set equality; they execute no package code and cross no trust boundary. The pre-existing ASVS-L1 V5 control (`lbw::validate_calibrate_inputs`) is untouched. No STRIDE category has a live surface. |

Package legitimacy gate: not applicable — no npm/pip/cargo package is installed by this plan.
</threat_model>

<verification>
- Both new pytest modules pass under the DoD gate's pytest invocation.
- No compiler flag changed on either build side.
- `newton_kl`'s exclusion from SC3 is written down and separately ticketed.
</verification>

<success_criteria>
- SC3, SC4 and SC2 each have a durable artifact in the repository.
- Both new checks run automatically as part of `.coverage-thresholds.json`'s `enforcement.command`.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-02-SUMMARY.md` when done
</output>
