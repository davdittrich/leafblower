---
phase: 02-one-engine-not-two
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - src/calib_dispatch.hpp
  - src/c_api.cpp
  - src/r_bridge.cpp
autonomous: true
requirements: [US-004]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R and Python both reach lbw::newton_calibrate through lbw::dispatch_solver."
    - "R's newton_kl result still contains n_projected_dims and a finite lm_mu_final on the converging fixture, and still surfaces n_bounds_violated."
    - "When newton_kl's violation guard leaves the best-weight vector empty, R still receives the zero-filled sentinel vector of length n."
    - "Every explicitly-named method in the R bridge now reaches its solver through lbw::dispatch_solver; only the AUTO branch still has its own routing copy."
    - statement: "Rf_error unwinding still releases every heap-backed local on both the error and the success path."
      verification: backstop
  artifacts:
    - "src/calib_dispatch.hpp :: dispatch_solver case arm for RK_ALG_NEWTON_KL"
    - "src/calib_dispatch.hpp :: DispatchResult::n_projected_dims and ::lm_mu_final populated"
  key_links:
    - "r_bridge.cpp / c_api.cpp newton_kl arms -> lbw::dispatch_solver -> lbw::newton_calibrate"
  prohibitions:
    - statement: "Do not create a new src/*.cpp translation unit."
      verification: flagged-unverified
    - statement: "Do not edit rk_params_t, rk_result_t or rk_algorithm_t in leafblower.h."
      verification: flagged-unverified
    - statement: "Do not move hot per-iteration solver code across a translation-unit boundary."
      verification: flagged-unverified
    - statement: "Do not add a shared finalize_weights call to newton_calib.cpp in this plan — the unit-mode bounds gap is separately ticketed."
      verification: flagged-unverified
    - statement: "Do not drop n_projected_dims or lm_mu_final from R's newton_kl result."
      verification: flagged-unverified
---

<objective>
Expansion wave 4: migrate `newton_kl`, the last explicitly-named method still on the
method-string chain, and the one carrying the two remaining superset-only fields
(`n_projected_dims`, `lm_mu_final`).

Purpose: after this plan, the only routing logic left outside the shared table is the AUTO
branch, which plan 07 consolidates.
Output: the final solver arm in `lbw::dispatch_solver`.

Follow the migration recipe stated in `02-03-PLAN.md`'s objective (steps 1-6).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-PATTERNS.md
@.planning/phases/02-one-engine-not-two/02-05-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan: the `RK_ALG_NEWTON_KL` case arm inside `lbw::dispatch_solver` and the
`n_projected_dims` / `lm_mu_final` members of `lbw::DispatchResult`
(`src/calib_dispatch.hpp`). No new files, no `leafblower.h` change. (Phase-wide list: plan
01's `lbw::DispatchResult` / `lbw::dispatch_solver`; plan 02's two sync tests; plan 08's
`test_single_dispatch_site.py`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate newton_kl</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 835-848 (the newton_kl branch: `n_projected_dims`, `lm_mu_final`, `n_bounds_violated`, and the empty-best-weights zero-fill sentinel)
    - src/c_api.cpp lines 146-173 (`pack_newton_result_c`) and the `RK_ALG_NEWTON_KL` arm
    - src/newton_calib.cpp (the `lbw::newton_calibrate` signature, its result type's `n_projected_dims` / `lm_mu_final` members, and the violation-counting path around line 869 that returns a no-convergence status above a 5% violation fraction)
    - src/leafblower.h (confirm `rk_result_t` has no `n_projected_dims` or `lm_mu_final` — this is why the narrowing copy drops them)
    - tests/testthat/test-newton-kl.R lines 20-35, tests/testthat/test-newton-tsvd-projection.R lines 15-50, tests/testthat/test-newton-kl-tsvd-ratio.R (the tripwires that pin these two fields)
    - src/r_bridge.cpp lines 914-938 and 983-994
  </read_first>
  <behavior>
    - Test 1: `lm_mu_final` is present in R's newton_kl result and finite on the converging fixture.
    - Test 2: `n_projected_dims` is present and matches the pre-migration value on the TSVD projection fixture.
    - Test 3: `n_bounds_violated` still surfaces for newton_kl.
    - Test 4: An empty best-weight vector still yields the zero-filled sentinel of length n.
    - Test 5: R-vs-Python newton_kl weight parity holds at rtol=1e-6, both sides converged.
  </behavior>
  <action>
    Apply the migration recipe to `newton_kl` / `RK_ALG_NEWTON_KL`. Populate
    `DispatchResult::n_projected_dims` and `::lm_mu_final` from the solver result, keep the
    explicit `n_bounds_violated` surfacing, and apply the empty-best-weights zero-fill
    fallback of length `st.n` inside the arm so both bridges inherit it from one place.
    `c_api.cpp`'s narrowing copy simply does not read the two superset-only members;
    `rk_result_t` stays 536 bytes and Python's result dict is unchanged. `r_bridge.cpp`
    copies them into the existing `res_n_projected_dims` and `res_lm_mu_final` locals, so
    the SEXP packing block is untouched. Run the three newton tripwire test files
    explicitly, not just the whole suite: they are the exact regression this phase's largest
    risk would produce, and a green whole-suite run that skipped them proves nothing. Do
    not add a shared finalize call to `newton_calib.cpp`: the absent unit-mode
    redistribution is a pre-existing, separately ticketed gap (see plan 02's ticket and
    `leafblower-og7d.5`), and closing it here would mix a behavioural change into a
    dispatch refactor.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="newton", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RK_ALG_NEWTON_KL' src/calib_dispatch.hpp` is at least 1.
    - `grep -c 'n_projected_dims' src/calib_dispatch.hpp` is at least 1; `grep -c 'n_projected_dims' src/leafblower.h` returns 0.
    - `tests/testthat/test-newton-kl.R`, `test-newton-tsvd-projection.R` and `test-newton-kl-tsvd-ratio.R` each report 0 FAIL, 0 ERROR.
    - `git diff --stat src/newton_calib.cpp` is empty.
    - `git diff --stat src/leafblower.h` empty; `ls src/*.cpp | wc -l` unchanged.
    - Exactly one commit for this solver.
  </acceptance_criteria>
  <done>newton_kl routes through the shared table with both of its R-only diagnostics intact.</done>
</task>

<task type="auto">
  <name>Task 2: Full DoD gate + stall-kind production-path check</name>
  <files>(no source edits — gate run)</files>
  <read_first>
    - R/harvest.R lines 690-710 (the PRODUCTION consumer of `convergence_stall_kind`, which drives the accelerate heuristic — a silent regression here degrades a real user-facing diagnostic, not just a test)
    - tests/testthat/test-cr-d5-auto-fallback-fields.R line 30-40
    - .coverage-thresholds.json
  </read_first>
  <action>
    Run the full Definition-of-Done gate. Then verify the `convergence_stall_kind` path
    specifically: every solver that can emit a stall status must still deliver a non-zero
    stall kind to R, because `harvest()` reads it in production to choose its acceleration
    heuristic and a silent fallback to a missing value would change user-visible behaviour
    without failing an obvious assertion. Confirm by reading that
    `DispatchResult::stall_kind` is populated in every arm from `res.base.stall_kind` and
    that the R side copies it into the local the SEXP packer reads. Report the eight
    migrated solvers' status in the summary as a table so plan 07 starts from a known
    state.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)' &amp;&amp; cd python &amp;&amp; uv pip install -e . --reinstall-package leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full DoD gate: R 0 FAIL / 0 ERROR, Python 0 failed.
    - Every arm of `lbw::dispatch_solver` assigns `stall_kind` (verified by reading each arm; state the count in the summary).
    - `02-06-SUMMARY.md` contains a per-solver migration status table covering all nine non-AUTO enum values.
  </acceptance_criteria>
  <done>All named methods are migrated and the full gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays supplied by the host language; the only boundary this package has. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | (none applicable) | `lbw::dispatch_solver` newton_kl arm | low | accept | No network, auth, persistence or injection surface exists in this package. The applicable ASVS-L1 control, V5 Input Validation via `lbw::validate_calibrate_inputs`, runs before dispatch on both bridges and is untouched by this plan. |

Package legitimacy gate: not applicable — no npm/pip/cargo package is installed by this plan.
</threat_model>

<verification>
- Full DoD gate green.
- The three newton tripwire test files pass individually.
- `leafblower.h` unchanged; no new `src/*.cpp`.
</verification>

<success_criteria>
- newton_kl is reached through `lbw::dispatch_solver` from both bridges.
- No named method remains on the method-string chain.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-06-SUMMARY.md` when done
</output>
