---
phase: 02-one-engine-not-two
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/calib_dispatch.hpp
  - src/c_api.cpp
  - src/r_bridge.cpp
autonomous: true
requirements: [US-004]

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "R's harvest(method=\"sinkhorn\") and Python's harvest(method=\"sinkhorn\") reach lbw::sinkhorn_solve through the SAME function in calib_dispatch.hpp."
    - "Every field R's harvest() returned for sinkhorn before this plan is still returned, with identical values (rtol=1e-6 R-vs-Python)."
    - "sizeof(rk_params_t)==264 and sizeof(rk_result_t)==536 still hold (ABI static_asserts untouched)."
    - statement: "Empty / single-element / null inputs are rejected before dispatch by lbw::validate_calibrate_inputs, which this plan does not touch."
      verification: backstop
    - statement: "Rf_error longjmp still unwinds every heap-backed local in C_rk_calibrate; the new neutral struct's heap members are released in BOTH swap blocks."
      verification: backstop
  artifacts:
    - "src/calib_dispatch.hpp :: lbw::DispatchResult (neutral internal result struct)"
    - "src/calib_dispatch.hpp :: lbw::dispatch_solver() (shared enum -> solver -> DispatchResult table)"
    - ".beads comment on leafblower-rywn carrying the full result-field inventory"
  key_links:
    - "r_bridge.cpp sinkhorn branch -> lbw::dispatch_solver"
    - "c_api.cpp RK_ALG_SINKHORN branch -> lbw::dispatch_solver"
    - "lbw::dispatch_solver -> lbw::sinkhorn_solve"
  prohibitions:
    - statement: "Do not create a new src/*.cpp translation unit for the dispatch table (no-LTO + CORE_SOURCES sync cost)."
      verification: flagged-unverified
    - statement: "Do not edit rk_params_t, rk_result_t or rk_algorithm_t in leafblower.h."
      verification: flagged-unverified
    - statement: "Do not move hot per-iteration solver code across a translation-unit boundary."
      verification: flagged-unverified
    - statement: "Do not drop n_projected_dims, lm_mu_final, sraa_demoted or convergence_stall_kind from R's result list."
      verification: flagged-unverified
    - statement: "Do not renormalize weights after the unit-mode water-fill."
      verification: flagged-unverified
    - statement: "Do not reuse rk_algorithm_t slots 2 or 7."
      verification: flagged-unverified
---

<objective>
Tracer slice: build the ONE shared dispatch site (neutral result struct + enum-dispatch
table in `src/calib_dispatch.hpp`) and route BOTH FFI callers through it for exactly one
low-risk solver — `sinkhorn` — end to end.

Purpose: prove the shared-table pattern against a real solver on both bridges before
migrating the other eight (D-01: solver-by-solver, never an atomic cutover).
Output: `lbw::DispatchResult` + `lbw::dispatch_solver()` in `calib_dispatch.hpp`; the
sinkhorn branch of `r_bridge.cpp` and of `c_api.cpp` both reduced to a call into it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / file | Kind | Location |
|---|---|---|
| `lbw::DispatchResult` | struct | `src/calib_dispatch.hpp` |
| `lbw::dispatch_solver(rk_algorithm_t, CalibState&, const CellTable&, DispatchResult&)` | inline fn | `src/calib_dispatch.hpp` |
| result-field inventory | `bd comment` on `leafblower-rywn` | beads |

Created by later plans in this phase (for reference): `python/leafblower/test_core_sources_sync.py`,
`python/leafblower/test_finalize_weights_sync.py` (plan 02); `python/leafblower/test_single_dispatch_site.py`
(plan 07); additional `case` arms inside `lbw::dispatch_solver` and additional fields on
`lbw::DispatchResult` (plans 03-06). No new `src/*.cpp`, no new struct in `leafblower.h`.

<tasks>

<task type="auto">
  <name>Task 1: Field inventory — what the neutral struct must carry</name>
  <read_first>
    - src/leafblower.h (the `rk_result_t` definition, ~lines 112-161, and both ABI `static_assert`s)
    - src/r_bridge.cpp lines 480-640 (the `res_*` local declarations and the `pack_solver_result` lambda)
    - src/c_api.cpp lines 57-173 (`pack_solver_result`, `pack_oris_result_c`, `pack_newton_result_c`)
    - .planning/phases/02-one-engine-not-two/02-RESEARCH.md (Pitfall 1)
  </read_first>
  <files>(none — bd comment only, no source file modified)</files>
  <action>
    Produce the authoritative superset inventory that `lbw::DispatchResult` will be built
    from. Tabulate three columns: (a) every field of `rk_result_t`; (b) every `res_*` local
    in `C_rk_calibrate` that ends up in the returned SEXP list; (c) the `res.base.*` /
    solver-specific source each is read from. Mark the four R-only fields that
    `rk_result_t` has no home for — `n_projected_dims`, `lm_mu_final`, `sraa_demoted`,
    `stall_kind` (surfaced to R as `convergence_stall_kind`) — plus the obs-level
    `res_best_weights` vector, as SUPERSET-ONLY rows. Post the table as a `bd comment` on
    `leafblower-rywn` (this satisfies that ticket's own Step 1). Do not edit any source
    file in this task. This is a read-and-record task per D-01's requirement that the
    migration be evidence-driven, not guessed.
  </action>
  <verify>
    <automated>bd show leafblower-rywn | grep -c 'n_projected_dims'</automated>
  </verify>
  <acceptance_criteria>
    - `bd show leafblower-rywn` output contains the inventory table.
    - The table names all five superset-only rows: `n_projected_dims`, `lm_mu_final`, `sraa_demoted`, `stall_kind`, `best_weights`.
    - Every field name in the table is quoted from `leafblower.h` or `r_bridge.cpp` — none invented.
    - No file under `src/` is modified by this task (`git status --porcelain src/` is empty).
  </acceptance_criteria>
  <done>The neutral-struct field set is written down and traceable to source, on the ticket.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Shared dispatch table + neutral result struct, wired for sinkhorn on BOTH bridges</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/calib_dispatch.hpp lines 359-433 (`finalize_weights` / `finalize_weights_buf` — the exact `inline`-helper shape to imitate)
    - src/c_api.cpp lines 414-505 (the enum-dispatch branches, including the `RK_ALG_SINKHORN` arm and the "mirrors r_bridge.cpp" comments at :458, :473, :505)
    - src/r_bridge.cpp lines 780-800 (the sinkhorn dispatch branch) and lines 570-640 (`pack_solver_result` lambda)
    - src/r_bridge.cpp lines 262-275, 640-650, 914-938, 983-994 (the RAII / `Rf_error` unwinding convention and both swap-release blocks)
    - src/sinkhorn.cpp (the `lbw::sinkhorn_solve` signature and its result type)
    - python/leafblower/_bindings.cpp lines 190-250 (the already-correct "call the shared entry point, do not reimplement dispatch" consumer shape)
  </read_first>
  <behavior>
    - Test 1: `harvest(method="sinkhorn")` in R returns the same named elements, in the same order, as before the change.
    - Test 2: R and Python sinkhorn weight vectors agree at rtol=1e-6 on the existing parity fixtures.
    - Test 3: R's sinkhorn result still carries `convergence_stall_kind`, `n_bounds_violated`, `n_bounds_clamped`.
    - Test 4: `sizeof(rk_result_t)` and `sizeof(rk_params_t)` static_asserts still compile.
  </behavior>
  <action>
    In `src/calib_dispatch.hpp`, inside `namespace lbw`, add (a) `struct DispatchResult` —
    plain aggregate whose members are the union computed in Task 1, named after the
    `r_bridge.cpp` locals minus their `res_` prefix (so `status`, `iterations`,
    `max_error`, `alg_used`, `mean_error`, `kl`, `chi2`, `l1_weight_change`, `grake_norm`,
    `convergence_metric`, `convergence_rule`, `convergence_tol`, `convergence_iter`,
    `convergence_solver_objective`, `convergence_minimized_metric`, `best_error`,
    `best_iter`, `metric_first_check`, `metric_prev_check`, `prev_check_iter`,
    `stall_kind`, `n_bounds_violated`, `n_bounds_clamped`, `solver_message`,
    `n_projected_dims`, `lm_mu_final`, `sraa_demoted`, plus `std::vector<double>
    best_weights`), every member default-initialized to the SAME default the corresponding
    `res_*` local currently uses; and (b)
    `inline void dispatch_solver(rk_algorithm_t alg, CalibState& st, const CellTable& ct, DispatchResult& out)`
    — an `if`/`switch` over `alg` that, for this plan, handles ONLY `RK_ALG_SINKHORN`
    (calls `lbw::sinkhorn_solve(st)`, copies `res.base.*` and the solver-specific fields
    into `out`, sets `out.alg_used = RK_ALG_SINKHORN`, and `std::move`s
    `res.base.best_weights` into `out.best_weights`) and, for every other enum value,
    leaves `out` untouched and returns without acting, so the existing caller-side branches
    for the not-yet-migrated solvers keep running unchanged. Keep it header-only and
    `inline` — do NOT add a `.cpp` (no-LTO / `CORE_SOURCES` rationale in RESEARCH.md's
    Alternatives Considered). Then rewire both callers to use it for sinkhorn only:
    in `src/c_api.cpp` replace the body of the `RK_ALG_SINKHORN` arm with a
    `lbw::dispatch_solver` call plus a narrowing copy of `DispatchResult` into
    `rk_result_t` (the four superset-only fields are simply not copied — `rk_result_t` is
    ABI-frozen at 536 bytes and must not change); in `src/r_bridge.cpp` replace the body of
    the method-string branch for sinkhorn (currently near line 788) with the same
    `lbw::dispatch_solver` call plus a copy of `DispatchResult` into the existing `res_*`
    locals, leaving the SEXP-packing block near lines 1017-1127 completely untouched.
    Because `DispatchResult` owns a `std::vector<double>`, add its release to BOTH
    `Rf_error` swap-release blocks alongside the existing locals — the error path near
    lines 914-938 and the success path near lines 983-994 — per the RAII convention
    documented at lines 262-275 and 640-650. Update or delete the now-stale
    "mirrors r_bridge.cpp" line-number comment in `c_api.cpp` that points at the sinkhorn
    branch. Leave `leafblower.h` untouched.
  </action>
  <reversibility rating="costly">
    The `DispatchResult` field set is consumed by eight later solver migrations; reshaping
    it after plans 03-06 land means re-touching every migrated branch. It is NOT one-way:
    the struct is internal (`leafblower.h` and both ABI `static_assert`s are unchanged), so
    no published contract or on-disk format is broken. Task 1's inventory is the mitigation.
  </reversibility>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="sinkhorn", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'struct DispatchResult' src/calib_dispatch.hpp` returns 1.
    - `grep -c 'dispatch_solver' src/c_api.cpp` and `grep -c 'dispatch_solver' src/r_bridge.cpp` each return at least 1.
    - `ls src/*.cpp | wc -l` is unchanged from before the task (no new translation unit).
    - `git diff --stat src/leafblower.h` is empty.
    - Both `Rf_error` swap-release blocks in `r_bridge.cpp` mention the `DispatchResult` instance (verified by reading, not grep — this is the manual-review item from RESEARCH.md Pitfall 4).
    - R sinkhorn testthat files pass with 0 FAIL.
  </acceptance_criteria>
  <done>Both bridges reach `lbw::sinkhorn_solve` through `lbw::dispatch_solver`; the R result list is byte-for-byte the same shape as before.</done>
</task>

<task type="auto">
  <name>Task 3: Full DoD gate + R-Python sinkhorn parity</name>
  <files>(no source edits — gate run)</files>
  <read_first>
    - .coverage-thresholds.json (the `enforcement.command` string, verbatim)
    - python/leafblower/test_solver_parity.py (the four-step parity protocol, incl. the precheck-both-sides-converged step)
    - python/leafblower/test_parity_weights.py
  </read_first>
  <action>
    Run the project Definition-of-Done gate exactly as `.coverage-thresholds.json`'s
    `enforcement.command` specifies, with single-thread BLAS exported. Any failure is a
    finding to fix in this plan, not noise to defer: the tracer's whole purpose is proving
    the pattern before eight more solvers ride on it. If the R suite and the Python parity
    suite are both green, commit the migration as one commit so a later `git revert` of
    this solver is clean (D-01/D-02: git history IS the rollback mechanism).
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)' &amp;&amp; cd python &amp;&amp; uv pip install -e . --reinstall-package leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - R testthat: 0 FAIL, 0 ERROR.
    - Python pytest: 0 failed.
    - `test_solver_parity.py` sinkhorn case passes at its declared tolerance with both sides reported converged.
    - The migration is a single commit touching only `src/calib_dispatch.hpp`, `src/c_api.cpp`, `src/r_bridge.cpp`.
  </acceptance_criteria>
  <done>The full DoD gate is green with sinkhorn routed through the shared table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays supplied by the host language; the only boundary this package has. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | (none applicable) | `lbw::dispatch_solver` | low | accept | Single-process, single-user numerical library: no network, no auth, no persistence, no string interpolation into a query/shell/HTML sink. The one pre-existing ASVS-L1 control that applies, V5 Input Validation via `lbw::validate_calibrate_inputs` (`src/validation.cpp`, called identically from `r_bridge.cpp:444-456` and `c_api.cpp:336`), runs BEFORE dispatch and is not touched by this phase — this plan changes what happens after validation already passed, not the validation boundary itself. No STRIDE category has a live attack surface here; fabricating threats to fill the template would be noise. |

Package legitimacy gate: not applicable — this plan installs no npm/pip/cargo package.
</threat_model>

<verification>
- Full DoD gate green (`.coverage-thresholds.json` `enforcement.command`).
- `sizeof(rk_params_t)==264` / `sizeof(rk_result_t)==536` static_asserts compile.
- R sinkhorn result element names identical to the pre-change baseline.
</verification>

<success_criteria>
- One `lbw::dispatch_solver` + `lbw::DispatchResult` exist in `src/calib_dispatch.hpp`.
- Sinkhorn is reached through it from both `c_api.cpp` and `r_bridge.cpp`.
- No new `src/*.cpp`; `leafblower.h` unchanged; DoD gate green.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-01-SUMMARY.md` when done
</output>
