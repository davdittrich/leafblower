---
phase: 02-one-engine-not-two
plan: 08
type: execute
wave: 7
depends_on: ["02-02", "02-07"]
files_modified:
  - python/leafblower/test_single_dispatch_site.py
  - .planning/phases/02-one-engine-not-two/02-08-SUMMARY.md
autonomous: false
requirements: [US-004]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Re-introducing a per-method dispatch chain into the R bridge fails the DoD gate (SC1 becomes enforced, not just achieved)."
    - "The full Definition-of-Done gate is green: R CMD INSTALL, R testthat 0 FAIL, Python pytest 0 FAIL, single-thread BLAS (SC5)."
    - "The stepstone benchmark shows no regression against the pre-phase baseline (SC5)."
    - "A developer can state, from the repository alone, that R and Python reach every solver through one dispatch site."
    - statement: "Empty, single-element and null inputs are still rejected by lbw::validate_calibrate_inputs before dispatch; the phase changed nothing on the validation boundary."
      verification: backstop
    - statement: "Text encoding is not applicable to this phase — the only string crossing the boundary is a method name matched against a closed key set; all other data is numeric arrays."
      verification: backstop
    - statement: "Under R's Rf_error longjmp (the phase's only interruption mode), every heap-backed local in C_rk_calibrate is released on both the error and the success path; the C++ core remains single-threaded per solve and the gate is run with single-thread BLAS for determinism."
      verification: backstop
  artifacts:
    - "python/leafblower/test_single_dispatch_site.py :: test_r_bridge_has_no_method_dispatch_chain"
  key_links:
    - "test_single_dispatch_site.py -> src/r_bridge.cpp"
    - ".coverage-thresholds.json enforcement.command -> pytest -> the new SC1 guard"
  prohibitions:
    - statement: "Do not weaken or skip the stepstone benchmark gate because the phase is otherwise green."
      verification: flagged-unverified
    - statement: "Do not close the phase with any Definition-of-Done step reported as skipped."
      verification: flagged-unverified
---

<objective>
Phase gate: make SC1 enforceable by a test rather than by discipline, then prove SC5 — the
full Definition-of-Done gate green and the stepstone benchmark unregressed — and have a
human confirm the phase's user-visible surface is unchanged.

Purpose: a refactor that is correct today and undefended tomorrow has not closed the gap
this phase exists to close.
Output: one new pytest guard, a green DoD run, a benchmark comparison, and a human sign-off.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-07-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan: `test_r_bridge_has_no_method_dispatch_chain` in
`python/leafblower/test_single_dispatch_site.py` (new file). Phase-wide artifact list, for
the record: `lbw::DispatchResult`, `lbw::dispatch_solver` and its nine solver arms,
`lbw::route_auto`, the single enum-to-name table (all in `src/calib_dispatch.hpp`);
`python/leafblower/test_core_sources_sync.py`;
`python/leafblower/test_finalize_weights_sync.py`;
`python/leafblower/test_single_dispatch_site.py`. No new `src/*.cpp`; `leafblower.h`
unchanged throughout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC1 guard — a second dispatch chain cannot come back unnoticed</name>
  <files>python/leafblower/test_single_dispatch_site.py</files>
  <read_first>
    - src/r_bridge.cpp (the slimmed `C_rk_calibrate` as plan 07 left it — confirm what the dispatch region now looks like before asserting on it)
    - python/leafblower/test_core_sources_sync.py (the sibling source-text-scan test written in plan 02; copy its `REPO_ROOT` resolution and message style)
    - .planning/phases/02-one-engine-not-two/02-07-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `src/r_bridge.cpp`, with comment lines excluded, contains zero occurrences of `strcmp(method_str` — the pattern that was the second dispatch chain.
    - Test 2: `src/r_bridge.cpp` contains at most two `dispatch_solver` call sites (the primary and the AUTO fallback).
    - Test 3: The test excludes comment lines, so documenting the retired pattern in a comment cannot make the guard self-invalidating.
  </behavior>
  <action>
    Create `python/leafblower/test_single_dispatch_site.py` with
    `test_r_bridge_has_no_method_dispatch_chain`. Read `src/r_bridge.cpp`, drop every line
    whose first non-whitespace characters begin a C++ line comment, and assert on the
    remainder: zero occurrences of the retired per-method comparison pattern, and at most
    two call sites of the shared dispatch entry point. Give the failure message the reason
    rather than the mechanics — that a per-method dispatch chain in the R bridge is the
    exact divergence this phase removed, and that a new solver belongs in the shared table
    in `calib_dispatch.hpp`. Keep it a pure text scan with no import of the compiled module,
    matching its two sibling guards from plan 02.
  </action>
  <verify>
    <automated>cd python &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest leafblower/test_single_dispatch_site.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The new test passes on the post-plan-07 tree.
    - Temporarily inserting a line containing `strcmp(method_str, "raking")` into `src/r_bridge.cpp` makes the test FAIL; the line is removed before commit and `git status --porcelain src/` is empty afterwards.
    - Adding that same text inside a `//` comment does NOT fail the test (the comment-exclusion works).
    - The test imports no compiled module.
  </acceptance_criteria>
  <done>SC1 is defended by the gate, not only satisfied by the current source.</done>
</task>

<task type="auto">
  <name>Task 2: SC5 — full DoD gate and stepstone no-regression</name>
  <files>(no source edits — gate run)</files>
  <read_first>
    - .coverage-thresholds.json (`enforcement.command`, verbatim)
    - tests/testthat/test-bench-gate.R (the `LBW_BENCH_GATE` opt-in guard and what the benchmark actually compares)
    - .planning/phases/02-one-engine-not-two/02-05-SUMMARY.md (the mid-phase benchmark run, for the comparison point)
  </read_first>
  <action>
    Run the full Definition-of-Done gate exactly as `.coverage-thresholds.json` specifies,
    with single-thread BLAS exported on both sides — the R and Python results only compare
    deterministically under single-threaded BLAS, so a multi-threaded run is not a weaker
    check, it is a meaningless one. Then run the stepstone benchmark gate with
    `LBW_BENCH_GATE=1`. This phase moved a caller across a translation-unit boundary in a
    build with no LTO, so the benchmark is the only evidence that the dispatch consolidation
    did not cost inlining somewhere hot; record the measured numbers in the summary rather
    than asserting "no regression" without them. Report every gate step's result explicitly,
    including any that was skipped and why — a phase cannot close on a skipped step.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)' &amp;&amp; cd python &amp;&amp; uv pip install -e . --reinstall-package leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - R testthat: 0 FAIL, 0 ERROR. Python pytest: 0 failed.
    - `LBW_BENCH_GATE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="bench-gate", stop_on_failure=TRUE)'` passes.
    - `02-08-SUMMARY.md` records the measured stepstone numbers and the baseline they are compared against.
    - No gate step is reported as skipped.
  </acceptance_criteria>
  <done>SC5 is proven with numbers, not asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirmation that the user-visible surface is unchanged</name>
  <what-built>
    R and Python now reach all nine solvers plus AUTO routing through one shared dispatch
    site in `src/calib_dispatch.hpp`. Three source-text guards (build-list sync, shared
    finalize-helper delegation, single dispatch site) now run inside the DoD gate. The
    optimization-level asymmetry between the R and Python builds is documented as a
    deliberate, bounded decision at the parity assertion.
  </what-built>
  <how-to-verify>
    1. In R: `library(leafblower)`, run a `harvest()` call on any fixture for each of
       `oris`, `oris_soft`, `raking`, `sinkhorn`, `greg`, `greenkhorn`, `logit`,
       `chebyshev`, `newton_kl` and `auto`. Confirm each returns without error and that
       `names()` of the result is what it was before the phase.
    2. Confirm the four fields that only R exposes are still present where they should be:
       `n_projected_dims` and `lm_mu_final` on `newton_kl`, `sraa_demoted` on `raking` and
       the oris family, `convergence_stall_kind` on any stalled run.
    3. In Python: run the same methods and confirm the result dict is unchanged — it should
       NOT have gained the four R-only fields, because the C ABI struct was deliberately
       left frozen.
    4. Read `02-08-SUMMARY.md`'s stepstone numbers and judge whether the benchmark delta is
       acceptable for your machine's noise level.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what changed that should not have.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays plus one method string supplied by the host language; the only boundary this package has. This plan adds no code on that path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | (none applicable) | new pytest guard, gate runs | low | accept | The new test reads source text and asserts on occurrence counts; it executes no package code and crosses no trust boundary. Phase-wide: this is a single-process, single-user numerical library with no network, auth, persistence or injection surface, and the one applicable ASVS-L1 control — V5 Input Validation via `lbw::validate_calibrate_inputs`, called identically from both bridges before any solver runs — was never touched by this phase, which changed only what happens after validation has already passed. |

Package legitimacy gate: not applicable — no npm/pip/cargo package is installed by this plan.
</threat_model>

<verification>
- All three source-text guards pass inside the DoD gate.
- Full DoD gate green with single-thread BLAS.
- Stepstone benchmark numbers recorded and unregressed.
- Human sign-off recorded.
</verification>

<success_criteria>
- SC1 through SC5 all have a durable artifact or a recorded measurement in the repository.
- No phase success criterion rests on an unverified assertion.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-08-SUMMARY.md` when done
</output>
