---
phase: 02-one-engine-not-two
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/calib_dispatch.hpp
  - src/c_api.cpp
  - src/r_bridge.cpp
autonomous: true
requirements: [US-004]

estimate:
  tokens: 100000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The chebyshev oris warm-start exists once, inside lbw::dispatch_solver, instead of once per bridge."
    - "R and Python both reach lbw::chebyshev_ipm and lbw::raking_solve through lbw::dispatch_solver."
    - "R's raking result still reports sraa_demoted; R's chebyshev result still falls back to a zero-filled best-weight vector when the solver's violation guard leaves it empty."
    - statement: "The warm-start CalibState copy never escapes the scope that owns its weights buffer (no dangling pointer after the move into DispatchResult)."
      verification: backstop
    - statement: "Rf_error unwinding still releases every heap-backed local, including the warm-start vector, on both the error and the success path."
      verification: backstop
  artifacts:
    - "src/calib_dispatch.hpp :: dispatch_solver case arms for RK_ALG_CHEBYSHEV and RK_ALG_RAKING, including the shared chebyshev warm-start"
    - "src/calib_dispatch.hpp :: DispatchResult::sraa_demoted populated for raking"
  key_links:
    - "lbw::dispatch_solver chebyshev arm -> lbw::oris_solve (warm start) -> lbw::chebyshev_ipm"
    - "r_bridge.cpp / c_api.cpp chebyshev + raking arms -> lbw::dispatch_solver"
  prohibitions:
    - statement: "Do not create a new src/*.cpp translation unit."
      verification: flagged-unverified
    - statement: "Do not edit rk_params_t, rk_result_t or rk_algorithm_t in leafblower.h."
      verification: flagged-unverified
    - statement: "Do not move hot per-iteration solver code across a translation-unit boundary."
      verification: flagged-unverified
    - statement: "Do not drop sraa_demoted from R's raking result."
      verification: flagged-unverified
    - statement: "Do not renormalize weights after the unit-mode water-fill."
      verification: flagged-unverified
---

<objective>
Expansion wave 2: migrate `chebyshev` (the solver with a bridge-duplicated warm-start) and
`raking` (the first solver carrying an R-only field, `sraa_demoted`).

Purpose: these two are where the shared table starts paying — the chebyshev warm-start and
raking's superset-only field are exactly the kinds of logic that drift between two
hand-synced dispatch chains.
Output: two more case arms in `lbw::dispatch_solver`, with the warm-start moved inside it.

Follow the migration recipe stated in `02-03-PLAN.md`'s objective (steps 1-6): add the arm,
rewire `c_api.cpp`, rewire `r_bridge.cpp`, re-read both `Rf_error` swap-release blocks,
update the stale "mirrors" comment, one commit per solver.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-PATTERNS.md
@.planning/phases/02-one-engine-not-two/02-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan: `RK_ALG_CHEBYSHEV` and `RK_ALG_RAKING` case arms inside
`lbw::dispatch_solver`, plus the shared chebyshev warm-start helper inside that arm and the
`sraa_demoted` population path (`src/calib_dispatch.hpp`). No new files, no `leafblower.h`
change. (Phase-wide list: plan 01's `lbw::DispatchResult` / `lbw::dispatch_solver`; plan
02's two sync tests; plan 08's `test_single_dispatch_site.py`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate chebyshev, moving the oris warm-start into the shared table</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 849-882 (the oris warm-start block and the `dispatch_cheb` lambda — the exact logic to move into the shared table, including the `weights_copy` safety comment, the `inner_max_iter` clamp `max(5, min(100, st.inner_max_iter / 10))`, the three-part acceptance guard on the warm iterate, and the empty-best-weights zero-fill fallback)
    - src/c_api.cpp lines 495-540 (the `RK_ALG_CHEBYSHEV` arm and the "mirrors r_bridge.cpp" comment at :505 — compare its warm-start against the R one line by line and record any behavioural difference found)
    - src/chebyshev.cpp (the `lbw::chebyshev_ipm(st, warm_ref)` signature)
    - src/calib_dispatch.hpp (`lbw::DispatchResult` / `lbw::dispatch_solver` as plans 01/03 left them)
    - src/r_bridge.cpp lines 914-938 and 983-994
  </read_first>
  <behavior>
    - Test 1: R chebyshev results are unchanged from the pre-migration baseline on the existing chebyshev fixtures.
    - Test 2: R-vs-Python chebyshev parity holds at the declared tolerance, both sides converged.
    - Test 3: When the solver's violation guard leaves the best-weight vector empty, the returned vector is the zero-filled sentinel of length n on BOTH bridges.
    - Test 4: The warm-start is invoked once per solve, not twice.
  </behavior>
  <action>
    Move the chebyshev oris warm-start into `lbw::dispatch_solver`'s `RK_ALG_CHEBYSHEV` arm
    so it exists once: copy `st.weights` into a locally owned buffer, build a `CalibState`
    copy pointing at that buffer, clamp its `inner_max_iter` to `max(5, min(100,
    st.inner_max_iter / 10))`, run `lbw::oris_solve` on the copy, and accept the resulting
    best iterate as the warm vector only when it is non-empty, of length `st.n`, and its
    `max_error` is finite. The `CalibState` copy must not outlive the buffer it points at —
    keep it in an inner scope, exactly as the current R-side block does. Then call
    `lbw::chebyshev_ipm(st, warm_ref)` and fill `DispatchResult`, applying the same
    empty-best-weights zero-fill fallback of length `st.n` the R branch has today. Before
    writing the shared version, diff the two existing warm-start implementations: if
    `c_api.cpp`'s differs from `r_bridge.cpp`'s in any observable way (clamp bounds,
    acceptance guard, fallback), that is a pre-existing R-vs-Python divergence — record it
    in the summary and adopt the R behaviour, since R's is the one the existing R test
    fixtures pin. Then rewire both bridges' chebyshev arms to the shared call and delete
    the now-dead warm-start code from each. This is the first task in the phase where the
    hot/cold split matters: the warm-start and the dispatch are once-per-solve (cold), so
    moving them into the header costs nothing, but do not pull any per-iteration loop body
    out of `chebyshev.cpp` or `oris.cpp` — there is no LTO in this build.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="cheb", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'chebyshev_ipm' src/r_bridge.cpp` returns 0 and `grep -c 'chebyshev_ipm' src/c_api.cpp` returns 0 (the solver is now reached only through the shared table).
    - `grep -c 'chebyshev_ipm' src/calib_dispatch.hpp` returns 1.
    - `grep -c 'oris_solve' src/calib_dispatch.hpp` is at least 1 (the warm-start lives there now).
    - R chebyshev testthat files: 0 FAIL, 0 ERROR.
    - Any warm-start difference found between the two former implementations is written into `02-04-SUMMARY.md`, even if the answer is "none".
    - `git diff --stat src/leafblower.h` empty; `ls src/*.cpp | wc -l` unchanged.
    - Exactly one commit for this solver.
  </acceptance_criteria>
  <done>Chebyshev's warm-start-plus-solve exists once and both bridges call it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate raking (first superset-only field), then run the full DoD gate</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 654-663 (the raking method-string branch, which sets `res_sraa_demoted` and moves `res_best_weights`)
    - src/c_api.cpp lines 414-445 (the `RK_ALG_RAKING` arm — note it has no home for `sraa_demoted`)
    - src/raking.cpp (the `lbw::raking_solve` signature, `RakingResult`'s `sraa_demoted` member, and `water_fill_cat` around lines 160-175, which is algorithm-internal and must not be touched)
    - src/leafblower.h (confirm `rk_result_t` has no `sraa_demoted` field — this is why the narrowing copy drops it)
    - src/r_bridge.cpp lines 914-938 and 983-994
    - .coverage-thresholds.json
  </read_first>
  <behavior>
    - Test 1: R's raking result still contains `sraa_demoted` with the same value as the pre-migration baseline (0/1 from the solver's boolean).
    - Test 2: Python's raking result dict is unchanged — it never had `sraa_demoted` and must not gain it, because `rk_result_t` is ABI-frozen.
    - Test 3: R-vs-Python raking weight parity holds at rtol=1e-6.
    - Test 4: raking's convergence-rule / `max_error` parity checks added in Phase 1 still pass.
  </behavior>
  <action>
    Apply the migration recipe to `raking` / `RK_ALG_RAKING`. This is the first arm that
    populates a superset-only member: set `DispatchResult::sraa_demoted` from
    `res.sraa_demoted` (as an int, 0 or 1, matching the existing R local's type), and move
    `res.base.best_weights` into `DispatchResult::best_weights`. The narrowing copy in
    `c_api.cpp` simply does not read `sraa_demoted` — `rk_result_t` stays 536 bytes and
    Python's result dict stays exactly as it is; the field asymmetry between the two
    bridges is pre-existing and out of scope. `r_bridge.cpp` copies it into the existing
    `res_sraa_demoted` local so the SEXP packing block is untouched. Leave `water_fill_cat`
    in `raking.cpp` alone: it is the algorithm's own per-margin box projection, not the
    post-hoc finalize step, and moving it would be both a behavioural change and a hot-code
    TU move. Then run the full DoD gate for the wave.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)' &amp;&amp; cd python &amp;&amp; uv pip install -e . --reinstall-package leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'raking_solve' src/r_bridge.cpp` returns 0 for the explicit-method path (any remaining occurrence is inside the AUTO branch, which plan 07 owns — state which case applies in the summary).
    - `grep -c 'sraa_demoted' src/calib_dispatch.hpp` is at least 1.
    - `grep -c 'sraa_demoted' src/leafblower.h` returns 0 (the ABI struct did not grow).
    - Full DoD gate: R 0 FAIL / 0 ERROR, Python 0 failed.
    - `src/raking.cpp` is not modified by this plan (`git diff --stat src/raking.cpp` empty).
    - Two commits exist for this plan, one per solver.
  </acceptance_criteria>
  <done>raking routes through the shared table with its R-only field intact, and the full gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays supplied by the host language; the only boundary this package has. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | (none applicable) | `lbw::dispatch_solver` chebyshev/raking arms | low | accept | No network, auth, persistence or injection surface exists in this package. The applicable ASVS-L1 control, V5 Input Validation via `lbw::validate_calibrate_inputs`, runs before dispatch on both bridges and is untouched. The one memory-safety hazard in this plan — the warm-start `CalibState` outliving its weights buffer — is a correctness bug, not an attacker-reachable threat, and is covered by the task's scoping requirement and the `Rf_error` unwind re-verification. |

Package legitimacy gate: not applicable — no npm/pip/cargo package is installed by this plan.
</threat_model>

<verification>
- Full DoD gate green.
- The chebyshev warm-start appears in exactly one file.
- `rk_result_t` still 536 bytes; `leafblower.h` unchanged.
</verification>

<success_criteria>
- chebyshev and raking are each reached through `lbw::dispatch_solver` from both bridges.
- `sraa_demoted` still reaches R users; Python's result dict is unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-04-SUMMARY.md` when done
</output>
