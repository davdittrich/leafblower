---
phase: 02-one-engine-not-two
plan: 07
type: execute
wave: 6
depends_on: ["02-06"]
files_modified:
  - src/calib_dispatch.hpp
  - src/c_api.cpp
  - src/r_bridge.cpp
autonomous: true
requirements: [US-004]

estimate:
  tokens: 115000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "AUTO routing (the compression/skew decision that picks raking, oris, oris+SRAA or newton_kl) exists in exactly one place; adding a routing rule requires editing one site."
    - "R's AUTO path selects the same algorithm as Python's AUTO path for every fixture, including at the exact M_cell/n = 0.9 boundary."
    - "The enum-to-name table exists once; slots 2 and 7 are still holes and a new enum value fails a static_assert until the table is updated."
    - "The R bridge no longer contains a method-string comparison chain; it resolves the method string to an enum once and calls the shared dispatch."
    - statement: "The AUTO fallback path still restores the user's original accelerate flag and the backed-up weights before the fallback solve."
      verification: backstop
    - statement: "lbw::estimate_M_cell is evaluated at most once per solve on both bridges."
      verification: backstop
    - statement: "Rf_error unwinding still releases every heap-backed local, including the AUTO path's weights backup, on both the error and the success path."
      verification: backstop
  artifacts:
    - "src/calib_dispatch.hpp :: lbw::route_auto() (the single AUTO routing decision)"
    - "src/calib_dispatch.hpp :: the single enum-to-name table with its reserved-slot holes and static_assert"
  key_links:
    - "r_bridge.cpp AUTO branch -> lbw::route_auto -> lbw::dispatch_solver"
    - "c_api.cpp AUTO branch -> lbw::route_auto -> lbw::dispatch_solver"
    - "r_bridge.cpp kAlgMap (string -> enum) -> lbw::dispatch_solver"
  prohibitions:
    - statement: "Do not create a new src/*.cpp translation unit."
      verification: flagged-unverified
    - statement: "Do not edit rk_params_t, rk_result_t or rk_algorithm_t in leafblower.h."
      verification: flagged-unverified
    - statement: "Do not reuse rk_algorithm_t slots 2 or 7; any positional name table must carry both holes."
      verification: flagged-unverified
    - statement: "Do not move hot per-iteration solver code across a translation-unit boundary."
      verification: flagged-unverified
    - statement: "Do not change the AUTO decision thresholds (the 0.9 compression comparison, K>=5, target_skew > 5) — this plan moves the logic, it does not retune it."
      verification: flagged-unverified
---

<objective>
Collapse what remains: consolidate the duplicated AUTO routing into one site, unify the two
enum-to-name tables, and reduce the R bridge's dispatch to a single string-to-enum lookup
plus one shared call.

Purpose: this is the plan that actually delivers SC1's wording — "adding a solver, a result
field, or a routing rule requires editing ONE dispatch site".
Output: `lbw::route_auto()` and one enum-name table in `src/calib_dispatch.hpp`; a
`C_rk_calibrate` with no per-method branching left.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-one-engine-not-two/02-CONTEXT.md
@.planning/phases/02-one-engine-not-two/02-RESEARCH.md
@.planning/phases/02-one-engine-not-two/02-PATTERNS.md
@.planning/phases/02-one-engine-not-two/02-05-SUMMARY.md
@.planning/phases/02-one-engine-not-two/02-06-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan: `lbw::route_auto()` and the single enum-to-name table in
`src/calib_dispatch.hpp`. Removed by this plan: the R bridge's inline AUTO routing copy,
its method-string comparison chain, and its duplicate name table. No new files, no
`leafblower.h` change. (Phase-wide list: plan 01's `lbw::DispatchResult` /
`lbw::dispatch_solver`; plans 03-06's nine solver arms; plan 02's two sync tests; plan 08's
`test_single_dispatch_site.py`.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One AUTO routing decision</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 663-788 (R's inline AUTO routing: the `m_cell_est_cache` reuse, the exact-integer 0.9 comparison, the weights backup, the accelerate backup, the target_skew computation, the severe-skew ORIS+SRAA branch, the moderate-skew newton_kl branch, and the auto-fallback that supersedes the primary result)
    - src/c_api.cpp lines 280-400 (the C-side AUTO copy — read it against the R one and tabulate every difference before writing anything)
    - src/r_bridge.cpp lines 475-505 (the `capacity_penalty` auto-resolution and the `m_cell_est_cache` local it shares with the AUTO path)
    - .planning/phases/02-one-engine-not-two/02-05-SUMMARY.md (the recorded `estimate_M_cell` / `capacity_penalty` comparison between the two bridges)
    - .planning/phases/02-one-engine-not-two/02-RESEARCH.md (Pitfall 3)
    - tests/testthat/test-cr-d5-auto-fallback-fields.R
    - src/r_bridge.cpp lines 914-938 and 983-994
  </read_first>
  <behavior>
    - Test 1: For every existing AUTO fixture, R selects the same algorithm as before the change.
    - Test 2: R and Python select the same algorithm for the same input, including at exactly M_cell/n = 0.9 (the comparison is `>=`, not `>`, on both sides).
    - Test 3: The auto-fallback path still restores the backed-up weights and the user's original accelerate flag before the fallback solve, and the fallback's result supersedes the primary's.
    - Test 4: `estimate_M_cell` is evaluated at most once per solve even when both the capacity resolution and the AUTO routing need it.
  </behavior>
  <action>
    Before writing code, tabulate every observable difference between the two AUTO
    implementations (thresholds, comparison operators, ordering of the skew computation,
    what is backed up and restored, which result wins on fallback) into the summary. Then
    add `lbw::route_auto()` to `src/calib_dispatch.hpp`: it takes the already-validated
    `CalibState` plus the inputs the estimate needs, returns the chosen `rk_algorithm_t`
    together with the flag saying the choice was automatic, and computes the M_cell
    estimate lazily-and-cached so a caller that already has the value does not pay for it
    twice and a caller that never needs it does not pay at all. Keep every threshold exactly
    as it is: the exact-integer form of the 0.9 compression comparison using `>=`, the K>=5
    condition, and the target_skew > 5 severe-skew split. Keep the routing DECISION separate
    from the solve: `route_auto` returns an enum, the caller then calls
    `lbw::dispatch_solver` with it, which is what makes the fallback path expressible as a
    second dispatch call rather than as nested routing. Rewire both bridges to use it and
    delete both inline copies. If the tabulated diff shows the two sides disagree anywhere,
    adopt the R behaviour (its fixtures pin it), and file a `bd` ticket recording the
    Python-side change so the divergence is a tracked fix, not a silent one.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); test_dir("tests/testthat", filter="auto", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'route_auto' src/calib_dispatch.hpp` is at least 1; `grep -c 'route_auto' src/c_api.cpp` and `grep -c 'route_auto' src/r_bridge.cpp` are each at least 1.
    - `grep -v '^ *//' src/r_bridge.cpp | grep -c 'estimate_M_cell'` returns 0 (the estimate is reached only through the shared router).
    - `tests/testthat/test-cr-d5-auto-fallback-fields.R` reports 0 FAIL, 0 ERROR.
    - The AUTO-implementation diff table is written into `02-07-SUMMARY.md`, even if the answer is "identical".
    - `git diff --stat src/leafblower.h` empty; `ls src/*.cpp | wc -l` unchanged.
  </acceptance_criteria>
  <done>AUTO routing exists once; both bridges call it and then dispatch on its answer.</done>
</task>

<task type="auto">
  <name>Task 2: One enum-to-name table</name>
  <files>src/calib_dispatch.hpp, src/c_api.cpp, src/r_bridge.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 940-960 (R's positional name table: canonical method names, empty strings at slots 2 and 7, `kAlgNamesLen = 12`, and the `static_assert` pinning the last enum value)
    - src/c_api.cpp lines 28-48 (the C-side table, which uses DIFFERENT display spellings for the same enum values, plus its two `static_assert`s)
    - src/c_api.cpp lines 570-590 (the only consumer of the C-side table: it formats the default `result->message`, which is user-visible in Python's result dict)
    - src/r_bridge.cpp lines 94-105 (`kAlgMap`, the string-to-enum map — already a single correct copy; reuse it, do not re-derive it)
    - src/leafblower.h (the `rk_algorithm_t` enum and the comment explaining why slot 2 is reserved)
  </read_first>
  <action>
    First determine whether the C-side display spellings are observable: grep the R and
    Python test suites and `R/harvest.R` for them, since they reach users through the
    default solver message. Record the answer in the summary. Then put ONE positional
    enum-to-name table in `src/calib_dispatch.hpp`, 12 entries long, carrying explicit holes
    at slot 2 (removed LBFGSB) and slot 7 (withdrawn GRAKE) with the reason written on each
    hole, guarded by a `static_assert` that fails when the last enum value moves and a
    second one pinning the table length to 12. Have both bridges read from it. If the grep
    showed something depends on the C-side spelling, keep exactly one presentational mapping
    alongside the canonical one and state in a comment which is which — do not change a
    user-visible message string inside a dispatch refactor. If nothing depends on it,
    converge on the canonical method names `kAlgMap` already uses and note the message-text
    change in the summary. Either way the outcome is one table of record, not two
    hand-synced ones.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'kAlgNames' src/r_bridge.cpp` returns 0 and `grep -c 'kAlgNames' src/c_api.cpp` returns 0, OR the summary states which single presentational table was deliberately retained and why.
    - The shared table in `src/calib_dispatch.hpp` has 12 entries with slots 2 and 7 empty and both `static_assert`s present.
    - Full R suite: 0 FAIL, 0 ERROR.
    - The observability finding for the C-side spellings is written into `02-07-SUMMARY.md`.
  </acceptance_criteria>
  <done>One enum-to-name table of record, with the reserved-slot holes and the tripwire intact.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse the R bridge's method-string chain and retire the stale mirror comments</name>
  <files>src/r_bridge.cpp, src/c_api.cpp</files>
  <read_first>
    - src/r_bridge.cpp lines 640-910 (the whole dispatch region as plans 01-06 left it — every branch body should now be a shared call)
    - src/r_bridge.cpp lines 94-105 (`kAlgMap`) and lines 430-460 (where the method string is read and validated)
    - src/c_api.cpp lines 450-510 (the remaining "mirrors r_bridge.cpp" comments pinned to specific line numbers in the other file, at :458, :473 and :505 in the pre-phase source)
    - src/r_bridge.cpp lines 914-938 and 983-994 (both `Rf_error` swap-release blocks — this task is the last chance to confirm the invariant holds after every branch is gone)
    - .coverage-thresholds.json
  </read_first>
  <action>
    Replace the per-method branching in `C_rk_calibrate` with the shape the C ABI already
    uses: look the method string up once in `kAlgMap`, error cleanly on an unknown method
    the same way the current code does, resolve AUTO through `lbw::route_auto`, then make
    exactly one `lbw::dispatch_solver` call (plus a second one only on the AUTO fallback
    path). Any per-method state assignment that the earlier plans deliberately left on the
    caller side — the `oris_auto_selected` and `use_admm_capacity` flags — moves into the
    shared table's corresponding arm now that both bridges set them identically; verify that
    claim by reading, and if the two bridges set them differently, leave them at the caller
    and record why. Then delete or rewrite every "mirrors r_bridge.cpp" comment in
    `c_api.cpp`: after this task there is nothing left to mirror, and a comment pointing at
    a line number in a file that no longer has that code is worse than no comment. Finally,
    re-read both swap-release blocks and confirm every heap-backed local still alive in the
    slimmed function is released on both paths.
  </action>
  <verify>
    <automated>R CMD INSTALL --preclean . &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(testthat); library(leafblower); out &lt;- test_dir("tests/testthat", stop_on_failure=TRUE)' &amp;&amp; cd python &amp;&amp; uv pip install -e . --reinstall-package leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' src/r_bridge.cpp | grep -c 'strcmp(method_str'` returns 0.
    - `grep -v '^ *//' src/c_api.cpp | grep -c 'mirrors r_bridge'` returns 0.
    - `grep -c 'dispatch_solver' src/r_bridge.cpp` is 2 or fewer (the primary call plus the AUTO fallback call).
    - An unknown method string still produces the same R error message as before the change (verified against the existing error-path test).
    - Full DoD gate: R 0 FAIL / 0 ERROR, Python 0 failed.
    - Both `Rf_error` swap-release blocks were re-read and the summary states which locals each releases.
  </acceptance_criteria>
  <done>`C_rk_calibrate` extracts arguments, resolves one enum, and calls the shared dispatch — nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R/Python caller → C++ core | In-memory numeric arrays plus one method string supplied by the host language; the only boundary this package has. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | (none applicable) | `lbw::route_auto`, `C_rk_calibrate` method-string lookup | low | accept | The method string is matched against a closed `kAlgMap` of known keys and never interpolated into a query, shell, path or format sink — an unknown key produces a clean error, which is the pre-existing behaviour this plan preserves rather than introduces. No network, auth or persistence surface exists. The applicable ASVS-L1 control, V5 Input Validation via `lbw::validate_calibrate_inputs`, still runs before dispatch on both bridges and is untouched. |

Package legitimacy gate: not applicable — no npm/pip/cargo package is installed by this plan.
</threat_model>

<verification>
- Full DoD gate green.
- AUTO routing and the enum-name table each exist exactly once.
- `leafblower.h` unchanged; no new `src/*.cpp`; slots 2 and 7 still holes.
</verification>

<success_criteria>
- Adding a solver, a result field, or a routing rule requires editing one dispatch site (SC1).
- The R bridge has no per-method branching left.
</success_criteria>

<output>
Create `.planning/phases/02-one-engine-not-two/02-07-SUMMARY.md` when done
</output>
