---
phase: 03-honest-performance-gate
plan: 06
type: execute
wave: 2
depends_on: ["03-05"]
files_modified:
  - benchmarks/oris_soft_vs_competitors.R
  - benchmarks/results/oris_soft_vs_competitors.csv
  - benchmarks/results/oris_soft_vs_competitors_env.txt
autonomous: true
requirements: [US-003, KPI-04]
gap_closure: true
gap_ids: [G-03-1]

estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "leafblower_chebyshev has a fresh, measured row on medium_100k_5margins, compared against optweight's norm='linf' minimax-flavored objective"
    - "the objective mismatch between optweight's weight-deviation minimax and chebyshev's margin-error minimax is stated explicitly in the CSV note, not silently treated as a like-for-like replication"
    - "optweight's absence of a max.w (upper-bound) argument is recorded as an honest gap, not silently assumed bound-compliant"
  artifacts:
    - benchmarks/oris_soft_vs_competitors.R
    - benchmarks/results/oris_soft_vs_competitors.csv
  key_links:
    - "leafblower_chebyshev arm -> optweight::optweight.svy(norm='linf') per docs/methods/chebyshev.md's own citation + the CRAN-verified argument set recorded in 03-RESEARCH.md's extended Package Legitimacy Audit"
---

<objective>
G-03-1 (UAT gap), continued: `chebyshev` is the one remaining method whose doc-named closest
competitor (per `docs/methods/chebyshev.md`'s own Practitioner-implementations section) is a
package NOT already installed — the R package `optweight`, which "is the primary open-source
tool supporting the minimax norm explicitly." This plan is sequenced after 03-05 (same file,
same-wave file-overlap rule) and kept separate because it is the one arm needing a NEW
dependency with its own legitimacy verification, distinct in concern from 03-05's
zero-new-dependency arms.

Research this session (CRAN PDF manual fetch, `03-RESEARCH.md`'s extended Package Legitimacy
Audit) confirmed `optweight` is real, actively maintained (v2.0.1, 2026-03-19, Noah Greifer —
also maintains `WeightIt`/`cobalt`) and genuinely supports `norm = "linf"`
(`f(w,b,s) = max_i s_i|w_i-b_i|`). It also surfaced two honest mismatches that MUST be stated
in the published note, not glossed over: (1) `optweight`'s minimax is over WEIGHT DEVIATION
FROM A BASE WEIGHT, with margin balance enforced via `tols` as a CONSTRAINT — `chebyshev`
instead minimizes max MARGIN ERROR directly as the LP objective, with weights unconstrained in
the objective (per `docs/methods/chebyshev.md`'s own "Drawbacks": "no closeness-to-design term
in the objective"). These are related but non-identical minimax formulations. (2) `optweight`
exposes only `min.w` (a lower floor); there is no `max.w` argument, so upper-bound compliance
cannot be checked on this arm — a real, reportable finding, not a defect to paper over. Package
verdict was OK (CRAN-audited this session, given the same rigor as `survey`/`icarus`/
`ReGenesees`) so no blocking-human checkpoint is required — same treatment those three got.

Purpose: close the last R-side method in G-03-1's per-method coverage gap.
Output: `benchmarks/oris_soft_vs_competitors.R` extended with `leafblower_chebyshev` and
`optweight_linf` rows on `medium_100k_5margins`; regenerated CSV/provenance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-honest-performance-gate/03-CONTEXT.md
@.planning/phases/03-honest-performance-gate/03-RESEARCH.md
@benchmarks/oris_soft_vs_competitors.R
@docs/methods/chebyshev.md
@R/harvest.R
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: leafblower_chebyshev + optweight_linf arms</name>
  <files>benchmarks/oris_soft_vs_competitors.R</files>
  <action>
  First, install `optweight` from CRAN into the project's ambient R library (`Rscript -e "install.packages('optweight', repos='https://cloud.r-project.org')"`), mirroring exactly how `icarus`/`ReGenesees` were made available in the 03-02 plan — an ambient-library install, not an auto-install inside the benchmark script itself.

  Add a `leafblower_chebyshev` arm: `harvest(df, tgt, method = "chebyshev", max_weight = 3, bounds_mode = "unit", attach_weights = FALSE, convergence = list())` — an empty `convergence` list, per-method default. `chebyshev` ignores the `rule`/`metric`/`stop_when` convergence keys entirely (per R/harvest.R's own documented parameter note: "method='chebyshev' ignores 'rule' ... its interior-point solver stops on its own complementarity-gap criterion"), so passing an explicit metric here would be a silent no-op at best — leave `convergence = list()` and let the solver's own `kTolMu = 1e-6` complementarity-gap stop rule govern, same empty-list convention as every other new arm in 03-05.

  Add an `optweight_linf` competitor arm inside a `requireNamespace("optweight", quietly = TRUE)`-guarded, `tryCatch()`-isolated block, matching the file's existing `icarus_calibration`/`ReGenesees_e_calibrate` structure exactly (skip-row-with-explanatory-note if the package is absent; a failed call must not abort the other arms). Use `optweight::optweight.svy(formula, data = df, targets = &lt;margin-proportion vector, one entry per (margin, non-reference category), in the SAME order `process_targets()` would produce&gt;, tols = 0, norm = "linf", min.w = 1e-8)` — `optweight.svy()` is the "Stable Balancing Weights for Generalization" variant (no `treat=` argument), the shape that matches a pure population-calibration problem with no binary/multi-category treatment variable, unlike the plain `optweight()` function whose primary examples are all treatment-balancing. `tols = 0` requests exact margin matching (the closest analogue to `harvest()`'s own equality-constraint margins). `min.w = 1e-8` mirrors the package's own documented default rather than inventing a new floor.

  In the `optweight_linf` row's `note` field, write out verbatim (do not summarize away): "optweight.svy(norm='linf') minimizes max_i s_i|w_i-b_i| (deviation from base weight, default 1), margin balance enforced via tols=0 as a CONSTRAINT, not the objective; leafblower's chebyshev instead minimizes max weighted margin error DIRECTLY as the LP objective (weights unconstrained in the objective) -- a related but non-identical minimax formulation, per docs/methods/chebyshev.md's own citation of optweight as the primary open-source tool supporting the minimax norm explicitly. optweight has no max.w argument (only min.w, confirmed against the CRAN manual fetched 2026-08-15) so max_w bound-compliance is UNVERIFIABLE on this arm." Because there is no upper bound to check on this arm, set `ok` to `NA` (not `TRUE`) and `max_w` to the raw achieved maximum weight (report what was measured, but do not claim it satisfies a bound the package cannot express) — do not silently coerce `ok` to `TRUE` just because the call succeeded.
  </action>
  <verify>
    <automated>cd /home/dd/Gemini/leafblower &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript benchmarks/oris_soft_vs_competitors.R &amp;&amp; grep -c "leafblower_chebyshev,\|optweight_linf," benchmarks/results/oris_soft_vs_competitors.csv</automated>
  </verify>
  <done>leafblower_chebyshev row present with real metrics; optweight_linf row present either with the exact caveat note above and ok=NA, or (if the package genuinely cannot be installed in the execution environment) a skip-row naming the missing package -- never silently omitted from the CSV.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate CSV/env, confirm optweight stays benchmark-only, DoD check</name>
  <files>benchmarks/results/oris_soft_vs_competitors.csv, benchmarks/results/oris_soft_vs_competitors_env.txt, DESCRIPTION</files>
  <action>
  Regenerate `benchmarks/results/oris_soft_vs_competitors.csv` and `_env.txt` fresh under the single-thread BLAS envelope. Add `"optweight"` to the script's existing `competitor_pkgs` provenance vector (alongside `survey`/`icarus`/`ReGenesees`) so its installed version is recorded in `_env.txt`, matching the existing provenance-capture convention exactly -- do not build a separate provenance mechanism. Confirm `DESCRIPTION`'s `Suggests:` list is unchanged: `optweight` is benchmark-scoped only (D-09, same treatment as `icarus`/`ReGenesees`), never added to the package's own dependency manifest. Run `R CMD INSTALL --preclean .` and `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e "devtools::test()"` to confirm 0 FAIL.
  </action>
  <verify>
    <automated>cd /home/dd/Gemini/leafblower &amp;&amp; git diff --stat DESCRIPTION | wc -l &amp;&amp; grep -c "optweight" DESCRIPTION</automated>
  </verify>
  <done>CSV has 13 rows for medium_100k_5margins total (11 from 03-05 + 2 from this plan); DESCRIPTION diff empty and contains zero mentions of optweight; devtools::test() reports 0 FAIL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `install.packages('optweight')` -> CRAN | new package-manager install this plan; sole new install surface |
| `optweight_linf` CSV row -> `docs/performance.md` (Plan 08) | the objective-mismatch and no-max.w caveats must survive into the published page verbatim, not just live in a CSV comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-SC | Tampering | `install.packages('optweight')` | high | mitigate | Package-legitimacy gate already run this session (planner research: CRAN PDF manual fetch confirming maintainer, version, publish date, real `norm="linf"` argument) and recorded as an OK verdict in `03-RESEARCH.md`'s extended Package Legitimacy Audit -- same standard of evidence `survey`/`icarus`/`ReGenesees` received, which did not require a blocking-human checkpoint. No blocking checkpoint is inserted here for the same reason; the audit table itself is the mitigation artifact. |
| T-03-06-01 | Repudiation | `optweight_linf` objective-mismatch claim | medium | mitigate | The exact caveat text is specified verbatim in Task 1's action and must appear in both the CSV `note` field and (Plan 08) the published page -- a reviewer can trace the claim back to this plan's own wording rather than a paraphrase drifting from what was actually verified. |

</threat_model>

<verification>
`OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript benchmarks/oris_soft_vs_competitors.R` exits 0 and produces 13 `medium_100k_5margins` rows. `DESCRIPTION` diff empty, zero `optweight` mentions. `devtools::test()` reports 0 FAIL.
</verification>

<success_criteria>
`chebyshev` has a doc-grounded, honestly-caveated competitor comparison on `medium_100k_5margins` -- completing G-03-1's R-reachable method coverage (all of `oris`, `oris_soft`, `raking`, `newton_kl`, `greg`, `logit`, `chebyshev` now measured against a doc-named competitor).
</success_criteria>

<output>
Create `.planning/phases/03-honest-performance-gate/03-06-SUMMARY.md` when done
</output>
