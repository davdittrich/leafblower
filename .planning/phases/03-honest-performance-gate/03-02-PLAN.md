---
phase: 03-honest-performance-gate
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - benchmarks/oris_soft_vs_competitors.R
autonomous: true
requirements: [US-003, KPI-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`benchmarks/results/oris_soft_vs_competitors.csv` gains an `input_class == \"large_stepstone_fulldata\"` row measured on the tracked 1,582,732-row / 9-margin / 836-category fixture — a genuine 1M-plus figure measured on this codebase with a live solver (SC1)."
    - "`benchmarks/results/oris_soft_vs_competitors.csv` gains `input_class == \"known_limit_k20_uniform\"` rows whose recorded `m_cell_over_n` is at or near 1.0, making the reason the composite `<30 s AND <1e-6` gate is unachievable a MEASURED number rather than a cited claim (SC3, D-01)."
    - "The known-limit class carries both an `oris_soft` arm and a raking-with-acceleration arm, so D-04's statement that raking is the fallback path for zero-compression-benefit inputs is backed by a number from this run."
    - "`icarus` and `ReGenesees` arms run when the packages resolve and are skipped with an informative note row when they do not — neither package is added to `DESCRIPTION` or `python/pyproject.toml` (D-09)."
  artifacts:
    - benchmarks/oris_soft_vs_competitors.R
    - benchmarks/results/oris_soft_vs_competitors.csv
  key_links:
    - "`benchmarks/stepstone_fulldata_bench_data.parquet` + `benchmarks/stepstone_fulldata_bench_targets.json` -> the `large_stepstone_fulldata` row — the large-scale claim reuses a tracked fixture rather than inventing one (D-08, ROADMAP 'reuse benchmarks/')."
    - "The `known_limit_k20_uniform` fixture parameters are byte-identical to `tests/testthat/test-bench-gate.R`'s kk1204 block (seed 1204, n=500000, K=20, 5 categories, max_weight=3) — the published known-limit number and the in-repo test describe the same fixture instead of becoming a fourth divergent variant."
    - "`requireNamespace()` guard -> `note` column — an unavailable competitor produces a recorded, publishable skip reason, not a silent absence from the comparison table."
---

<objective>
Expand the proven tracer to the two remaining input classes and the two remaining doc-named
competitors, without changing the CSV schema, the wrapper command, or the gate assertion.

Purpose: SC1 needs a large-scale figure (the tracer's 100K class is medium by construction),
SC3 needs the degenerate class measured rather than asserted, and D-07 names three
competitors rather than one. All three additions are rows in the table the tracer already
publishes and gates.

Output: `benchmarks/oris_soft_vs_competitors.R` covering three input classes and four
competitor arms; a regenerated `benchmarks/results/oris_soft_vs_competitors.csv`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-honest-performance-gate/03-CONTEXT.md
@.planning/phases/03-honest-performance-gate/03-RESEARCH.md
@.planning/phases/03-honest-performance-gate/03-PATTERNS.md
@.planning/phases/03-honest-performance-gate/03-01-SUMMARY.md
@benchmarks/oris_soft_vs_competitors.R
@tests/testthat/test-bench-gate.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Large-scale input class — the tracked 1.58M-row stepstone fulldata fixture</name>

  <read_first>
    - `benchmarks/oris_soft_vs_competitors.R` as produced by plan 01 — the `run_input_class()` / `arm_row()` structure and the frozen 17-column schema this task adds rows to.
    - `benchmarks/stepstone_fulldata_benchmark.R` lines 1-60 and its target-construction and `harvest()` call sites — how this exact fixture and its 9-margin target list are loaded and passed. Reuse the loading convention; do NOT reuse its comparison target.
    - `.planning/phases/03-honest-performance-gate/03-01-SUMMARY.md` — the measured medium-class numbers and the machine line, so the large-class run is interpreted against a known baseline on the same host.
  </read_first>

  <precondition>`benchmarks/stepstone_fulldata_bench_data.parquet` (about 64 MB, tracked) and `benchmarks/stepstone_fulldata_bench_targets.json` both exist in the working tree, and `requireNamespace("arrow")` and `requireNamespace("jsonlite")` return TRUE.</precondition>

  <files>benchmarks/oris_soft_vs_competitors.R</files>

  <behavior>
    - The script emits an `input_class == "large_stepstone_fulldata"` row for `leafblower_oris_soft` with `n == 1582732`, `n_margins == 9` and `n_categories == 836`.
    - The emitted `m_cell_over_n` for that class is strictly below 1.0, distinguishing it from the known-limit class added in task 2.
    - The `max_w` for that row satisfies the per-observation bound it was given.
    - The medium-class rows from plan 01 are unchanged in schema and still present in the same CSV.
  </behavior>

  <action>
Add a second input class, `large_stepstone_fulldata`, to
`benchmarks/oris_soft_vs_competitors.R`, reusing the `run_input_class()` structure plan 01
established so the addition is a call site plus a fixture loader, not a second harness.

Load the fixture with `arrow::read_parquet("benchmarks/stepstone_fulldata_bench_data.parquet")`
and the targets with `jsonlite::fromJSON("benchmarks/stepstone_fulldata_bench_targets.json")`,
following `benchmarks/stepstone_fulldata_benchmark.R`'s loading convention. The data frame
carries a `uuid` identifier column plus the nine `rk_*` margin columns; select the margin
columns by intersecting the data frame's names with the target list's names rather than by
position, so a fixture regeneration that reorders columns cannot silently mis-map a margin to
the wrong target. Guard the fixture's presence with `file.exists()` and, if absent, emit a
row with `ok = FALSE` and a `note` naming the missing file rather than aborting the whole run
— the medium class must still publish.

Use the same `max_weight = 3` and `bounds_mode = "unit"` the medium class uses, so the two
leafblower figures are on one bound convention and can be quoted in the same sentence.

Do not run `survey`, `icarus` or `ReGenesees` on this class. Record why, as a computed number
rather than an opinion: these three implementations calibrate through a dense
observation-by-category model matrix, so at n = 1,582,732 and 836 categories the matrix alone
projects to `n * n_categories * 8` bytes, on the order of 10 GB. Compute that projection in
the script from the actual `n` and `n_categories` and write it into the `note` column of an
`ok = FALSE` row per competitor for this class, with `wall_s` as `NA_real_`. This is an
honest, checkable statement of a real feasibility boundary and it is itself a publishable
comparative finding — it is not a silent omission of the competitors from the large-scale
claim. The competitor comparison proper stays on the medium class, which is also where SC2's
contradiction lives.

State the input class in the emitted row rather than only in prose: `n`, `n_margins`,
`n_categories`, `m_cell` and `m_cell_over_n` are already schema columns, and SC1's "on a
stated input class" is satisfied by those columns being populated from the fixture itself,
not typed by hand into the docs page later.

Expect this class to be the longest-running arm in the script. Keep `bench::mark`'s
`iterations = 2` as in plan 01 rather than reducing it — two samples is already the analog's
minimum and dropping to one would remove the only defence against a single anomalous run
becoming the published number. Print the per-arm `cat()` progress line before starting the
solve as well as after, so a maintainer can tell a long solve from a hang.

D-05 prohibition continues to apply to this file: the unreleased package that the old
"Nx faster than" framing used as its baseline must not appear as an arm, a variable name, a
comment or a commented-out block. `benchmarks/stepstone_fulldata_benchmark.R` does contain
such calls — reuse only its fixture-loading and target-construction scaffolding, never its
comparison target or its speedup-ratio framing.
  </action>

  <verify>
    <automated>OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript benchmarks/oris_soft_vs_competitors.R && Rscript -e "d <- read.csv('benchmarks/results/oris_soft_vs_competitors.csv'); L <- d[d\$input_class=='large_stepstone_fulldata' & d\$arm=='leafblower_oris_soft',]; stopifnot(nrow(L)==1, L\$n==1582732L, L\$n_margins==9L, L\$n_categories==836L, is.finite(L\$wall_s), is.finite(L\$max_error), L\$max_w <= L\$max_weight + 1e-10, L\$m_cell_over_n < 1.0); stopifnot(sum(d\$input_class=='medium_100k_5margins')>=2); cat('OK large tier\n')"</automated>
  </verify>

  <acceptance_criteria>
    - The CSV contains exactly one `leafblower_oris_soft` row for `input_class == "large_stepstone_fulldata"` with `n == 1582732`, `n_margins == 9`, `n_categories == 836`, finite `wall_s`, finite `max_error`, `max_w <= max_weight + 1e-10` and `m_cell_over_n < 1.0`.
    - The plan-01 medium-class rows are still present and still conform to the frozen 17-column schema.
    - Competitor infeasibility at this scale is recorded as data, not omitted: `Rscript -e "d <- read.csv('benchmarks/results/oris_soft_vs_competitors.csv'); s <- d[d\$input_class=='large_stepstone_fulldata' & d\$arm=='survey_calibrate',]; stopifnot(nrow(s)==1, isFALSE(as.logical(s\$ok)), nchar(as.character(s\$note))>20)"` exits 0.
    - Margin columns are selected by name intersection, not position: `grep -c 'intersect' benchmarks/oris_soft_vs_competitors.R` returns at least 1.
    - <!-- planner-discipline-allow: autumn --> `grep -v '^\s*#' benchmarks/oris_soft_vs_competitors.R | grep -ci 'autumn'` returns 0.
  </acceptance_criteria>

  <done>SC1's large-scale figure exists as a measured row on a tracked 1.58M-row real-survey fixture, with its input class carried in the data rather than asserted in prose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Known-limit input class — K=20 uniform-random, with M_cell/n measured</name>

  <read_first>
    - `tests/testthat/test-bench-gate.R` lines 28-54 — the exact kk1204 fixture construction (`set.seed(1204)`, `n <- 500000`, `K <- 20`, `cats <- 5`, uniform `1/cats` targets, `max_weight = 3`) that this class must reproduce parameter-for-parameter.
    - `docs/investigations/2026-04-23-kk1204-convergence.md` — the original finding that `M_cell/n = 1.0` at this shape means zero cell-compression benefit and that the composite gate is structurally unachievable; the measured number this task produces must be consistent with it or the discrepancy must be reported.
    - `benchmarks/oris_soft_vs_competitors.R` as extended by task 1.
  </read_first>

  <files>benchmarks/oris_soft_vs_competitors.R</files>

  <behavior>
    - The script emits `input_class == "known_limit_k20_uniform"` rows whose `m_cell_over_n` is at or very near 1.0, in contrast to the other two classes.
    - Two arms are emitted for this class: `leafblower_oris_soft` and `leafblower_raking_accelerated`.
    - Both arms report a finite `max_error` well above the `1e-6` the retired composite gate demanded — the run demonstrates the ceiling rather than failing to reach it silently.
    - The run completes within a bounded iteration budget; it does not iterate toward `1e-6`.
  </behavior>

  <action>
Add a third input class, `known_limit_k20_uniform`, whose fixture parameters are identical to
the kk1204 block already living in `tests/testthat/test-bench-gate.R` at lines 28-54:
`set.seed(1204)`, `n = 500000`, `K = 20` factor columns named `m1`..`m20` each sampled
uniformly from 5 levels, uniform `1/5` targets per margin, `max_weight = 3`. Reproducing that
block's parameters exactly is the point: RESEARCH.md Pitfall 2 records that the repo already
carries three divergent descriptions of "the kk1204 gate" (the PRD's `n=1M`/`<1e-6`, the
investigation's `n=1M`, and the test's `n=500k`/`1e-3`). This class must reproduce the test's
parameters so the published known-limit figure and the in-repo assertion describe one fixture,
not a fourth variant.

This class exists to make SC3's known limit a measured fact. The single most important output
is `m_cell_over_n`: the script already computes `m_cell` as the count of distinct margin-column
combinations, so at K=20 with 5 categories and n=500,000 this class should report a ratio at
or extremely close to 1.0, meaning every observation is effectively its own cell and the
cell-compression that ORIS's whole per-iteration advantage rests on yields nothing. That
number is the *reason* the composite gate is unachievable, computed here rather than quoted
from a document.

Two arms for this class:
  - `leafblower_oris_soft` with `bounds_mode = "unit"`, `max_weight = 3`.
  - `leafblower_raking_accelerated`: `harvest(..., method = "raking", accelerate = TRUE, bounds_mode = "unit", max_weight = 3)`. D-04 keeps raking-with-acceleration as the documented fallback path for zero-compression-benefit inputs; this arm is the number that backs that statement, measured on the exact class where the statement applies.

Label the raking arm by what the code does, not by an algorithm name the code does not
verify. The repo uses both "SQUAREM" and "SRAA-m Anderson acceleration" in different places
for what `accelerate = TRUE` selects; naming the arm after the argument (`raking_accelerated`)
and recording `accelerate = TRUE` in the `note` column keeps the published table honest about
what was actually switched on. Resolving which acceleration scheme the flag selects is out of
this phase's scope; do not assert one in the arm name or the note.

Bound the cost. Pass an explicit `max_iterations` (the investigation's extrapolation is that
reaching `1e-6` on this class needs on the order of 1.76 million iterations, roughly two
weeks) — use a budget in the same order as the existing kk1204 block's 500 and record the
budget in the `note` column. The achieved `max_error` at that budget IS the finding. Do NOT
add a wall-time or accuracy assertion for this class to the gate test: per D-01 this class is
not the basis of any headline claim and per D-10 the hard gate covers the headline number
only. This class is measured and published as a documented ceiling, not gated.

Record both arms' `deff` and `n_eff` as for every other row. The `leafblower-ylsy` closure
concluded this fixture is near-infeasible with design effects in the thousands and effective
observations in the low hundreds across every solver; the `n_eff` this run reports is the
check on whether that conclusion still holds at current HEAD, and any material divergence
from it must be reported in the summary rather than quietly published.
  </action>

  <verify>
    <automated>OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript benchmarks/oris_soft_vs_competitors.R && Rscript -e "d <- read.csv('benchmarks/results/oris_soft_vs_competitors.csv'); K <- d[d\$input_class=='known_limit_k20_uniform',]; stopifnot(nrow(K)==2, all(sort(K\$arm)==sort(c('leafblower_oris_soft','leafblower_raking_accelerated')))); stopifnot(all(K\$n==500000L), all(K\$n_margins==20L), all(K\$m_cell_over_n > 0.95), all(is.finite(K\$wall_s)), all(is.finite(K\$max_error)), all(K\$max_error > 1e-6), all(is.finite(K\$n_eff))); cat('OK known-limit tier\n')"</automated>
  </verify>

  <acceptance_criteria>
    - The CSV contains exactly two `known_limit_k20_uniform` rows, arms `leafblower_oris_soft` and `leafblower_raking_accelerated`, each with `n == 500000` and `n_margins == 20`.
    - Both rows report `m_cell_over_n > 0.95`, and every other input class in the same CSV reports `m_cell_over_n < 0.5` — the degeneracy is demonstrated by contrast within one table.
    - Both rows report a finite `max_error` strictly greater than `1e-6`, and both report finite `deff` and `n_eff`.
    - The iteration budget is recorded in the data, not only in code: `Rscript -e "d <- read.csv('benchmarks/results/oris_soft_vs_competitors.csv'); K <- d[d\$input_class=='known_limit_k20_uniform',]; stopifnot(all(grepl('max_iterations', as.character(K\$note))))"` exits 0.
    - The fixture parameters match the in-repo test's: `grep -c '1204' benchmarks/oris_soft_vs_competitors.R` returns at least 1, and the summary records a side-by-side of the two parameter sets.
    - No gate assertion was added for this class: `grep -c 'known_limit_k20_uniform' tests/testthat/test-bench-gate.R` returns 0.
  </acceptance_criteria>

  <done>The input class on which the retired composite gate is structurally unachievable is measured, its zero-compression reason is a number in the same table as the headline figure, and the raking fallback path has a measurement backing it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remaining doc-named competitors — icarus and ReGenesees, benchmark-scoped</name>

  <read_first>
    - `docs/methods/oris.md`, the practitioner-implementations table and the "Bounds handling" row of "How leafblower deviates" — the sole sanctioned competitor set and each package's calibration entry point (`icarus::calibration()`, `ReGenesees::e.calibrate()`) and bounds mechanism (D-07).
    - `DESCRIPTION` — confirm what is currently under `Suggests:` before deciding what NOT to add there.
    - The installed help for both packages before writing either call: `Rscript -e "?icarus::calibration"` and `Rscript -e "?ReGenesees::e.calibrate"`, plus `?ReGenesees::pop.template` and `?ReGenesees::fill.template` for the population-totals construction. Read the argument contracts; do not write these calls from recollection.
    - `benchmarks/oris_soft_vs_competitors.R` as extended by tasks 1 and 2 — in particular the plan-01 `survey_calibrate` arm, which is the shape the two new arms mirror.
  </read_first>

  <precondition>`requireNamespace("icarus")` and `requireNamespace("ReGenesees")` both return TRUE in the active R library (verified available at planning time: `ReGenesees` 2.4).</precondition>

  <files>benchmarks/oris_soft_vs_competitors.R</files>

  <behavior>
    - With both packages resolvable, the medium class emits four arms: `leafblower_oris_soft`, `survey_calibrate`, `icarus_calibration`, `ReGenesees_e_calibrate`.
    - With a competitor package unavailable, its arm still emits a row with `ok = FALSE`, `wall_s` as `NA_real_` and a `note` naming the missing package — the comparison table records the absence rather than omitting the arm.
    - Every competitor arm's `max_error` and `max_w` come from the script's own helpers applied to that package's returned weights, on weights renormalised to the same `sum(w) == n` convention.
    - Neither `icarus` nor `ReGenesees` appears in `DESCRIPTION` or `python/pyproject.toml` after this task.
  </behavior>

  <action>
Add `icarus_calibration` and `ReGenesees_e_calibrate` arms to the medium input class,
completing the doc-named competitor set from `docs/methods/oris.md`'s bounds-handling row
(D-07). Do not add arms for any package outside that row — the wider roster in
`benchmarks/study/` belongs to the separate `leafblower-2ouc` epic and D-07/D-08 explicitly
forbid drawing competitors from it.

Dependency scoping (D-09): guard each arm with `requireNamespace(pkg, quietly = TRUE)` and,
on failure, emit the arm's row with `ok = FALSE`, `wall_s = NA_real_` and a `note` naming the
package and the install line a maintainer would run. Do NOT add either package to the
package's own `DESCRIPTION` `Suggests:` field and do NOT add either to
`python/pyproject.toml`. `survey` is already under `Suggests:` for unrelated pre-existing
reasons; that is not a precedent to follow here. RESEARCH.md's assumption A1 records this as
a judgment call — it is resolved in favour of the guard, because a benchmark-only competitor
in `Suggests:` would widen what `R CMD check --as-cran` expects of a CRAN submission in
Phase 5 for no shipped-package benefit.

Call construction. Both packages calibrate through a population-totals contract that differs
from `survey`'s, so each needs its own translation from the same `tgt` list this script
already builds — read each package's help before writing the call:
  - `icarus::calibration()` takes the sample data, a margin matrix in icarus's own layout, a design-weight column, a `method` selecting the distance function, and bounds as a low/high pair on the weight ratio.
  - `ReGenesees::e.calibrate()` takes a design object from `ReGenesees::e.svydesign()`, a population data frame built via `ReGenesees::pop.template()` and `ReGenesees::fill.template()`, a `calmodel` formula, a `calfun`, and `bounds` on the ratio.
Both arms must receive the identical control totals and the identical `[0, 3]` ratio bound
the `leafblower_oris_soft` and `survey_calibrate` arms received, and a comparable convergence
tolerance recorded in `note`. Arms differing in anything except the implementation under test
is the confound this whole benchmark exists to avoid.

Metric computation stays arm-independent: pass each package's returned weight vector through
the script's existing `margin_max_error()` and bound/`deff` helpers after renormalising to
`sum(w) == n`, exactly as the `survey_calibrate` arm does. Never record a package's
self-reported convergence value as `max_error` — a package that stops early would otherwise
look both fast and accurate.

Wrap each competitor call in `tryCatch()`: a package that errors on this fixture emits
`ok = FALSE` with the condition message truncated into `note`, and the script continues. A
single competitor failure must not cost the maintainer the whole run, and a recorded failure
is a legitimate, publishable comparative result.

Record `packageVersion()` for both new packages into
`benchmarks/results/oris_soft_vs_competitors_env.txt` alongside the ones plan 01 already
writes — this is the T-03-01 mitigation, and with these two packages undeclared in
`DESCRIPTION` the provenance file is the only record of which build produced the number.
  </action>

  <verify>
    <automated>OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript benchmarks/oris_soft_vs_competitors.R && Rscript -e "d <- read.csv('benchmarks/results/oris_soft_vs_competitors.csv'); M <- d[d\$input_class=='medium_100k_5margins',]; stopifnot(setequal(M\$arm, c('leafblower_oris_soft','survey_calibrate','icarus_calibration','ReGenesees_e_calibrate'))); ok <- M[as.logical(M\$ok),]; stopifnot(nrow(ok)>=2, all(is.finite(ok\$wall_s)), all(is.finite(ok\$max_error)), all(ok\$max_w <= ok\$max_weight + 1e-10)); cat('OK competitor set\n')" && Rscript -e "stopifnot(!any(grepl('icarus|ReGenesees', readLines('DESCRIPTION'))))"</automated>
  </verify>

  <acceptance_criteria>
    - The medium class emits exactly the four arms `leafblower_oris_soft`, `survey_calibrate`, `icarus_calibration`, `ReGenesees_e_calibrate` (arms whose package is unavailable or which errored are present with `ok = FALSE`, not absent).
    - Every arm with `ok == TRUE` has finite `wall_s` and `max_error` and satisfies `max_w <= max_weight + 1e-10`.
    - Neither new package is declared in the shipped manifests: `Rscript -e "stopifnot(!any(grepl('icarus|ReGenesees', readLines('DESCRIPTION'))))"` exits 0, and `grep -ci 'icarus\|regenesees' python/pyproject.toml` returns 0.
    - Both packages are guarded, not assumed: `grep -c 'requireNamespace' benchmarks/oris_soft_vs_competitors.R` returns at least 2.
    - Every competitor call is failure-isolated: `grep -c 'tryCatch' benchmarks/oris_soft_vs_competitors.R` returns at least 3.
    - Provenance names all four implementations: `grep -c 'leafblower\|survey\|icarus\|ReGenesees' benchmarks/results/oris_soft_vs_competitors_env.txt` returns at least 4.
    - Competitor arms are graded by the script, not self-reported: the summary quotes, for each competitor arm, the package's own reported convergence value alongside the script-computed `max_error`, and states whether they agree.
  </acceptance_criteria>

  <done>The full doc-named competitor set runs on the medium class, all graded by one accuracy function on one bound convention, with the two undeclared packages guarded and version-recorded rather than added to the shipped manifests.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Extended by this plan (03-02) — no new files:
- `benchmarks/oris_soft_vs_competitors.R` gains `input_class` handlers `large_stepstone_fulldata` and `known_limit_k20_uniform`, and arm builders `icarus_calibration`, `ReGenesees_e_calibrate`, `leafblower_raking_accelerated`.
- `benchmarks/results/oris_soft_vs_competitors.csv` gains rows for those classes and arms; the 17-column schema frozen by 03-01 is unchanged.
- `benchmarks/results/oris_soft_vs_competitors_env.txt` gains `packageVersion()` lines for `icarus` and `ReGenesees`.

Not created by this plan (avoid duplicating): `docs/performance.md` and the wall-time gate
threshold are 03-03; `README.md`, the REQUIREMENTS/PRD edits and the kk1204 re-gate are
03-04.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R session -> `icarus` / `ReGenesees` resolved by name | Both packages are deliberately absent from `DESCRIPTION` per D-09, so they resolve from the ambient R library with no manifest pinning them. |
| tracked parquet fixture -> benchmark process | `benchmarks/stepstone_fulldata_bench_data.parquet` is respondent-level survey microdata (with a `uuid` column) already tracked in this repo; this plan reads it. |

No new runtime attack surface: this plan adds rows to a maintainer-run developer script. ASVS
L1 categories V2, V3, V4 and V6 remain not applicable; V5 applies only in the sense recorded
as T-03-02.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `icarus` / `ReGenesees` resolution in `benchmarks/oris_soft_vs_competitors.R` | low | mitigate | Task 3 writes `packageVersion()` for both into `benchmarks/results/oris_soft_vs_competitors_env.txt`. With no `DESCRIPTION` entry (D-09) that provenance line is the sole record of which build produced a published competitor figure, so a substituted local package is visible as a provenance diff rather than a silent number change. |
| T-03-02 | Information disclosure | `benchmarks/stepstone_fulldata_bench_data.parquet` read by task 1 | low | mitigate | Task 1 reads the fixture only to derive aggregate shape (`n`, `n_margins`, `n_categories`, `m_cell`) and timing. No row-level value, no `uuid`, and no out-of-repo source path reaches the CSV or the provenance file. Exposure is unchanged from what the already-tracked file is. |
| T-03-04 | Denial of service | the `known_limit_k20_uniform` and `large_stepstone_fulldata` arms | low | mitigate | Both classes are genuinely expensive (the known-limit class would need on the order of 1.76 million iterations to reach the retired `1e-6` target). Task 2 caps it with an explicit `max_iterations` recorded in the `note` column, and task 3 wraps every competitor call in `tryCatch()` so one hostile arm cannot cost the maintainer the entire run. The script is maintainer-invoked, so this is a cost-control measure, not an availability control. |
| T-03-SC | Tampering | package-manager installs | low | accept | No install occurs; `03-RESEARCH.md` § Package Legitimacy Audit rates `icarus` and `ReGenesees` both `OK` (CRAN-canonical, cited in `docs/methods/oris.md` with journal references, resolvable locally). No `[ASSUMED]`/`[SUS]`/`[SLOP]` package appears, so no blocking legitimacy checkpoint is required. |

No threat is rated high; nothing blocks at the configured ASVS L1 / block-on-high threshold.
</threat_model>

<verification>
- `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript benchmarks/oris_soft_vs_competitors.R` exits 0 and the resulting CSV carries three distinct `input_class` values.
- The three classes are distinguishable by compression: `m_cell_over_n` is below 0.5 for the medium and large classes and above 0.95 for the known-limit class.
- `CI=1 bash benchmarks/run_honest_gate.sh` still exits 0 — plan 01's single command still regenerates every figure without modification.
- `CI=1 LBW_BENCH_GATE=1 Rscript -e "testthat::test_dir('tests/testthat', filter='bench-gate', stop_on_failure=TRUE)"` still exits 0 — the expanded CSV does not break the plan-01 gate assertion.
- Project Definition of Done unbroken: `R CMD INSTALL --preclean .` then `Rscript -e "devtools::test()"` reports 0 FAIL.
- `git diff --stat DESCRIPTION python/pyproject.toml` is empty.
</verification>

<success_criteria>
- Three input classes are measured in one run and one table: a medium class carrying SC2's number, a 1.58M-row large class carrying SC1's figure, and the degenerate class carrying SC3's documented ceiling.
- The reason the retired composite gate is unachievable is a measured `m_cell_over_n` in that table, not a citation.
- All four doc-named implementations are graded by one accuracy function, one bound convention and one machine.
- The two undeclared competitor packages are guarded and version-recorded, and appear in neither shipped manifest.
</success_criteria>

<output>
Create `.planning/phases/03-honest-performance-gate/03-02-SUMMARY.md` when done. Record: the
full result table as measured (every class, every arm, every column); the measured
`m_cell_over_n` for all three classes side by side; the `deff` and `n_eff` for the
known-limit class compared against the `leafblower-ylsy` closure's stated ranges, flagging
any material divergence; the projected dense model-matrix size that justified not running
competitors at large scale; each competitor's own reported convergence value next to the
script-computed `max_error`; and the side-by-side of this class's fixture parameters against
`tests/testthat/test-bench-gate.R`'s kk1204 block.
</output>