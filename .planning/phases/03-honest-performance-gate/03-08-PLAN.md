---
phase: 03-honest-performance-gate
plan: 08
type: execute
wave: 3
depends_on: ["03-05", "03-06", "03-07"]
files_modified:
  - docs/performance.md
autonomous: true
requirements: [US-003, KPI-04]
gap_closure: true
gap_ids: [G-03-1, G-03-4]

estimate:
  tokens: 36000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/performance.md's Competitors section covers all 9 leafblower methods (oris, oris_soft, raking, sinkhorn, greenkhorn, chebyshev, greg, logit, newton_kl), each against its own doc-named closest competitor, with the source docs/methods/*.md citation named per row"
    - "every new figure is transcribed from the regenerated CSVs (oris_soft_vs_competitors.csv, greenkhorn_sinkhorn_vs_pot.csv), never estimated or asserted"
    - "the two honest scope caveats (optweight's objective mismatch + no max.w; POT's K=2-only scope + no bounds) are stated on the published page, not left only in CSV note fields"
  artifacts:
    - docs/performance.md
  key_links:
    - "docs/performance.md Results/Competitors sections -> benchmarks/results/oris_soft_vs_competitors.csv + benchmarks/results/greenkhorn_sinkhorn_vs_pot.csv (both regenerated by plans 05-07)"
---

<objective>
G-03-4 (UAT gap): "docs/performance.md's competitors section covers every leafblower method
against its own closest competitor" -- explicitly recorded as G-03-1's "documentation-side
consequence": the underlying per-method benchmark data did not exist until plans 03-05/06/07
produced it. This plan is the one that actually closes G-03-4, and by publishing the data,
finally closes G-03-1 end to end (measured AND documented, not just measured).

Purpose: rewrite `docs/performance.md`'s narrow, 3-package "Competitors" section into a full
9-method mapping table, and extend its "Results" section with every new row the prior three
plans produced -- transcribed verbatim, per this page's own standing rule ("every number on
this page is transcribed from one measured artefact").
Output: `docs/performance.md` extended; full project Definition-of-Done re-verified across all
four gap-closure plans (05-08) together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-honest-performance-gate/03-CONTEXT.md
@.planning/phases/03-honest-performance-gate/03-UAT.md
@docs/performance.md
@docs/methods/00-overview.md
@docs/methods/oris.md
@docs/methods/raking.md
@docs/methods/greenkhorn.md
@docs/methods/sinkhorn.md
@docs/methods/chebyshev.md
@docs/methods/greg.md
@docs/methods/logit.md
@docs/methods/newton_kl.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the Competitors section into a full 9-method table; extend Results</name>
  <files>docs/performance.md</files>
  <action>
  Read the regenerated `benchmarks/results/oris_soft_vs_competitors.csv` (now 13 rows for
  `medium_100k_5margins`: `leafblower_oris_soft`/`oris`/`raking`/`newton_kl`/`greg`/`logit`/
  `chebyshev` + `survey_calibrate`/`survey_calibrate_linear`/`survey_calibrate_logit`/
  `icarus_calibration`/`ReGenesees_e_calibrate`/`optweight_linf`) and
  `benchmarks/results/greenkhorn_sinkhorn_vs_pot.csv` (4 rows:
  `leafblower_greenkhorn`/`leafblower_sinkhorn` + `pot_greenkhorn`/`pot_sinkhorn`). Do NOT read
  `benchmarks/study/report/tables/*.csv` -- per D-08, these two freshly-regenerated files are
  the only source for this page's numbers.

  Under `## Results`, add a new subsection `### medium_100k_5margins -- per-method competitor
  coverage`, transcribing every new row's `wall_s`/`max_error`/`max_w`/`min_w`/`n_eff` verbatim
  in a table, same "paired" framing as the existing headline claim -- never a bare speed number
  without its accuracy/bound/n_eff siblings on the same row. Add a second new subsection
  `### k2_margin_pot_equiv -- greenkhorn/sinkhorn vs POT (K=2-margin special case)`, transcribing
  the 4 rows from `greenkhorn_sinkhorn_vs_pot.csv`, and stating explicitly, in prose, ABOVE the
  table: this class exists only because leafblower's K-margin cyclic sweep coincides with POT's
  2-marginal formulation at K=2 (cite `docs/methods/oris.md`'s "Relation to Sinkhorn and
  Greenkhorn" §Axis 1), both arms ran effectively unbounded because POT has no box-constraint
  mechanism, and this class is NOT representative of leafblower's normal K>2, bounded workload.

  Rewrite the existing `## Competitors` section. Keep its first paragraph (the `survey`/`icarus`/
  `ReGenesees` scoping rationale) as the description of the ALREADY-published `oris_soft`
  comparison, then add a new table with columns `Method | Closest competitor | Source | Objective
  match | Caveats`, one row per each of the 9 methods (`oris`, `oris_soft`, `raking`, `sinkhorn`,
  `greenkhorn`, `chebyshev`, `greg`, `logit`, `newton_kl`), citing the exact
  `docs/methods/&lt;name&gt;.md` §Practitioner-implementations passage each choice came from (D-07
  grounding -- match the citation style the page's existing Competitors section already uses:
  "see the practitioner implementations table ... in docs/methods/oris.md"). Populate the table
  as follows, each row's Caveats column transcribing the exact note text from the corresponding
  CSV row (do not paraphrase away the specifics):
  - `oris`/`oris_soft`/`raking`/`newton_kl`: share the `survey`/`icarus`/`ReGenesees` set already
    described (cross-reference the Results table, do not re-derive); `newton_kl`'s row notes that
    `survey::calibrate`'s Newton-Raphson-on-the-dual IS the same algorithm class newton_kl attacks
    via a trust-region variant, per `docs/methods/newton_kl.md`.
  - `greg`: `survey::calibrate(calfun="linear")` -- distance-matched (chi-square), not the
    raking-calfun arm.
  - `logit`: `survey::calibrate(calfun="logit")` -- distance-matched; note `icarus_calibration`
    is also logit-based (already in the Results table) as a bonus same-distance match.
  - `chebyshev`: `optweight` (`optweight.svy(norm="linf")`) -- Caveats column carries BOTH the
    objective-mismatch statement (max weight-deviation vs max margin-error) AND the "no max.w
    argument, bound-compliance unverifiable" statement, verbatim from the `optweight_linf` CSV
    row's note.
  - `sinkhorn`/`greenkhorn`: POT (`ot.sinkhorn`/`ot.bregman.greenkhorn`) -- Caveats column
    carries the K=2-only scope and no-bounds-support statement verbatim from the
    `k2_margin_pot_equiv` rows' notes.
  </action>
  <verify>
    <automated>cd /home/dd/Gemini/leafblower &amp;&amp; for m in oris oris_soft raking sinkhorn greenkhorn chebyshev greg logit newton_kl; do grep -qi "$m" docs/performance.md || echo "MISSING: $m"; done; grep -ci "optweight" docs/performance.md; grep -ci "POT\|Python Optimal Transport" docs/performance.md</automated>
  </verify>
  <done>Competitors section has 9 method rows with source citations; Results section carries every new figure verbatim; both scope caveats (optweight objective mismatch + no max.w, POT K=2-only + no bounds) appear on the published page, not only in CSV notes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final Definition-of-Done verification across all four gap-closure plans</name>
  <files>none (verification only)</files>
  <action>
  Run the full project Definition of Done, confirming plans 05-08's combined edits leave it
  green: `R CMD INSTALL --preclean .`; `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1
  MKL_NUM_THREADS=1 Rscript -e "devtools::test()"` (expect 0 FAIL, same baseline as
  03-04-SUMMARY.md's `[ FAIL 0 | WARN 141 | SKIP 13 | PASS 1836 ]`, allowing only benign
  run-to-run drift, no new FAILs); `cd python &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1
  MKL_NUM_THREADS=1 uv pip install -e . --reinstall-package leafblower &amp;&amp; .venv/bin/python -m
  pytest` (expect 160 passed / 0 failed); `bash benchmarks/run_honest_gate.sh` (expect exit 0,
  now covering the R competitor script AND the new Python POT script). Confirm `git diff --stat
  DESCRIPTION python/pyproject.toml` is empty across the whole gap-closure batch (optweight and
  POT stayed benchmark-only across plans 05-07, per D-09). Spot-check three numbers transcribed
  into `docs/performance.md` in Task 1 against the actual CSV cells they claim to come from, to
  catch a transcription slip before this plan is marked done.
  </action>
  <verify>
    <automated>cd /home/dd/Gemini/leafblower &amp;&amp; git diff --stat DESCRIPTION python/pyproject.toml | wc -l &amp;&amp; OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'testthat::test_dir("tests/testthat")' 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>Full DoD green (R 0 FAIL, Python 160 passed/0 failed, run_honest_gate.sh exit 0); DESCRIPTION/pyproject.toml diff empty; spot-checked figures match their CSV source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV cell -> published `docs/performance.md` prose/table | a transcription error here becomes a published falsehood the page's own header claims never happens |
| `docs/performance.md` -> downstream README/REQUIREMENTS references | this page is already the single linked methodology source for US-003/KPI-04 (03-04); a broken link or contradicted number here propagates outward |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08-01 | Tampering | manual transcription of CSV figures into markdown prose | medium | mitigate | Task 2's spot-check step re-reads three transcribed numbers against their CSV source cell before the plan is marked done, catching a copy/rounding slip rather than shipping it silently. |
| T-03-08-02 | Repudiation | caveat text (optweight/POT mismatches) surviving into the published page | medium | mitigate | Task 1 requires the Caveats column to transcribe the CSV note field VERBATIM, not paraphrase it -- a reviewer can diff the published sentence against the CSV note to confirm nothing was softened in transit. |

</threat_model>

<verification>
Every one of the 9 method names appears in `docs/performance.md`'s Competitors table with a `docs/methods/*.md` citation. Both new Results subsections transcribe real CSV rows. Full DoD green: R 0 FAIL, Python 160 passed/0 failed, `run_honest_gate.sh` exit 0, `DESCRIPTION`/`pyproject.toml` diff empty.
</verification>

<success_criteria>
G-03-4 closed: `docs/performance.md`'s Competitors section covers all 9 leafblower methods against their own doc-named closest competitor, with every figure traceable to a regenerated CSV and both honest scope caveats stated on the page itself. G-03-1 closed end to end (measured in 05-07, documented here).
</success_criteria>

<output>
Create `.planning/phases/03-honest-performance-gate/03-08-SUMMARY.md` when done
</output>
