---
phase: 03-honest-performance-gate
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - tasks/prd-leafblower-core.md
  - tests/testthat/test-bench-gate.R
autonomous: true
requirements: [US-003, KPI-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`README.md` exists and carries the headline performance claim in one line with a link to `docs/performance.md`, and nothing else that belongs to Phase 5's CRAN-readiness scope (D-13)."
    - "Exactly one medium-scale number is readable across the repository: the `< 1 s` and `< 2 s` statements in `tasks/prd-leafblower-core.md` are marked superseded in place and point at the live artefact, so a reader cannot land on the contradiction (SC2)."
    - "`.planning/REQUIREMENTS.md`'s US-003 and KPI-04 entries each name a live measuring artefact that exists in the tree — `benchmarks/oris_soft_vs_competitors.R` and the `honest gate:` assertion in `tests/testthat/test-bench-gate.R` — in place of the void `test-lbfgsb.R` (SC4)."
    - "The pre-existing kk1204 assertion is opt-in under `LBW_BENCH_GATE` like every other heavy gate in its file, and its name says what it actually measures (D-11, D-01)."
  artifacts:
    - README.md
    - .planning/REQUIREMENTS.md
    - tasks/prd-leafblower-core.md
    - tests/testthat/test-bench-gate.R
  key_links:
    - "`README.md` -> `docs/performance.md` — D-13's one-line-plus-linked-page shape; the link must resolve to the file 03-03 created."
    - "`.planning/REQUIREMENTS.md` KPI-04 -> `benchmarks/oris_soft_vs_competitors.R` and the `honest gate:` assertion — the KPI table's performance rows name artefacts that exist and run, closing SC4's second clause."
    - "`tasks/prd-leafblower-core.md` §1 and §11 -> `docs/performance.md` — the two contradictory medium-scale statements are retired in place and both point at the one number."
---

<objective>
Close the loop: publish the one-line claim, retire every stale number that contradicts it,
point the KPI bookkeeping at artefacts that actually exist, and bring the last ungoverned
performance assertion under the project's one gate convention.

Purpose: SC2 and SC4 are not satisfied by writing a new number — they are satisfied by
leaving exactly one number readable and by every performance row naming something a
maintainer can run. That means editing the places the old numbers still live, not only the
places the new one goes.

Output: `README.md`; rewritten US-003 and KPI-04 entries in `.planning/REQUIREMENTS.md`;
supersede markers in `tasks/prd-leafblower-core.md`; the re-gated kk1204 assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-honest-performance-gate/03-CONTEXT.md
@.planning/phases/03-honest-performance-gate/03-RESEARCH.md
@.planning/phases/03-honest-performance-gate/03-03-SUMMARY.md
@docs/performance.md
@tests/testthat/test-bench-gate.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md — one headline line, one link, nothing else (D-13)</name>

  <read_first>
    - `docs/performance.md` as created by 03-03 — the exact claim wording and the anchor the README line links to.
    - `.planning/phases/03-honest-performance-gate/03-03-SUMMARY.md` — the developer's verbatim metric-and-ceiling selection, which the one line must state without embellishment.
    - `DESCRIPTION` — the `Title:` and `Description:` fields, so the README's single orienting sentence agrees with the package's own declared description rather than inventing a second one.
    - `.planning/ROADMAP.md` Phase 5 — confirm what CRAN/distribution work belongs there, so this task does not pre-empt it.
  </read_first>

  <files>README.md</files>

  <action>
Create `README.md`. It does not currently exist and has never existed in this repository, so
there is no baseline to preserve and no house style to match — RESEARCH.md verified this by
git history, not by absence of a file on disk.

Scope is deliberately narrow and this is the whole risk of the task. Include exactly: the
package name; one sentence stating what the package does, agreeing with `DESCRIPTION`'s
`Title:` and `Description:`; the one-line headline performance claim exactly as task 1 of
03-03 decided it, naming `oris_soft` (D-04); and a link to `docs/performance.md` for the
methodology, machine, input class and competitor detail.

Do NOT include installation instructions, CRAN or PyPI badges, a quick-start example, a
feature list, a solver roster, a licence section, or a citation block. A complete package
README is Phase 5's distribution work; this phase needs only the headline surface. The
absence of a README today makes it very easy for this task to become "write the package
README", and that expansion would put Phase 5 scope into a Phase 3 commit where it will not
be reviewed as CRAN-readiness work. The line-count bound in the acceptance criteria exists to
make that expansion visible rather than to be clever.

The headline line must be self-limiting in the same way the docs page is: it must not state a
speed without stating what it was achieved at. If the developer selected the paired framing
in 03-03, state all its clauses. If the developer selected a single metric, the line still
names the input class in short form and links out for the rest — a speed number with no input
class attached is the shape of claim this phase exists to retire.

Prohibited per D-05: the unreleased package that the old "Nx faster than" framing used as its
baseline must not appear. Prohibited per D-08: no number in this file may be typed from
anywhere but `docs/performance.md`, which is itself transcribed from the measured CSV.
  </action>

  <verify>
    <automated>test -f README.md && Rscript -e "p <- readLines('README.md'); stopifnot(length(p) <= 25); stopifnot(any(grepl('docs/performance.md', p, fixed=TRUE))); stopifnot(any(grepl('oris_soft', p, fixed=TRUE))); stopifnot(!any(grepl('install.packages|pip install|badge|shields.io', p))); cat('OK readme\n')"</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` exists, is at most 25 lines, and contains a link to `docs/performance.md` and the string `oris_soft`.
    - Phase 5 scope has not leaked in: `grep -ci 'install.packages\|pip install\|badge\|shields.io\|CRAN' README.md` returns 0.
    - <!-- planner-discipline-allow: autumn --> `grep -ci 'autumn' README.md` returns 0.
    - The link resolves: `test -f docs/performance.md` exits 0, and the path as written in `README.md` is repo-root-relative.
    - Every numeric value in `README.md` also appears in `docs/performance.md` — verify by listing them side by side in the summary.
  </acceptance_criteria>

  <done>The headline claim is on the package's front surface in one line, pointing at the page that backs it, with no Phase 5 scope attached.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the contradictory numbers and name live artefacts (SC2, SC4)</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 100-152 — the US-003 entry (lines 102-112) and the Project-level acceptance KPI list (lines 132-151), including KPI-04 at lines 145-146. Note the entry style the file already uses: bold status marker, cited investigation document with commit hash, explicitly named void artefact.
    - `.planning/REQUIREMENTS.md` Superseded table and Traceability table — the existing conventions for marking a PRD statement superseded and for the per-requirement phase/status rows (`US-003 | Phase 3 | Partial`, `KPI-04 | Phase 3 | Open`).
    - `tasks/prd-leafblower-core.md` lines 28-32, 178-184 and 670-676 — the three sites stating a medium-scale target: §1's `100K rows, 5 margins (3-5 cats each) < 1 s`, the L-BFGS-B acceptance checkbox at line 181, and §11's KPI row `100K rows, 5 margins < 2 s, max_error < 1e-6` naming `test-lbfgsb.R`.
    - `docs/performance.md` — the one number that replaces all three.
  </read_first>

  <precondition>`docs/performance.md` exists and the `honest gate:` assertion in `tests/testthat/test-bench-gate.R` carries the ceiling selected in 03-03.</precondition>

  <files>.planning/REQUIREMENTS.md, tasks/prd-leafblower-core.md</files>

  <action>
Two files, one job: leave exactly one medium-scale number readable, and make every
performance row name an artefact that exists.

In `tasks/prd-leafblower-core.md`, mark all three medium-scale statements superseded in place.
Edit, do not delete — the PRD is a frozen historical document (Draft v3, 2026-04-18) that
`.planning/REQUIREMENTS.md` already treats as superseded in specific places, and deleting the
lines would erase the record of what was promised. Append a short superseded marker to each of
the three sites naming `docs/performance.md` as the live statement and the current date. Match
the marker style `.planning/REQUIREMENTS.md`'s Superseded table already uses for other PRD
sections. All three sites must be marked: §1's line, the L-BFGS-B acceptance checkbox, and
§11's KPI row. RESEARCH.md Pitfall 1 records that a plan touching only the new documents
leaves the PRD's own internal contradiction readable and SC2 unmet — the check is that no
reader can land on two different numbers for the same shape.

Note that the §11 row names `test-lbfgsb.R` as its measuring artefact. That file does not
exist and the solver it measured was withdrawn (algorithm slot 2 is a permanently reserved
hole). The marker on that row must say so explicitly, because a named-but-absent artefact is
worse than an unnamed one.

In `.planning/REQUIREMENTS.md`:

Rewrite the US-003 entry. Its current text says the performance targets have never been
verified against a live solver and are internally contradictory. That is now false in both
clauses, and leaving it would be a new inaccuracy. State what was measured, on which input
classes, by which solver, and name the live artefacts. Keep the structural-unachievability
finding about the K=20 uniform-random class, but restate it as a documented known limit with
its measured `m_cell_over_n` and a pointer to `docs/performance.md`'s known-limit section,
rather than as an open blocker. Update the status marker per what the measurements actually
support — do not mark it complete if a clause of the original requirement (for example, the
`verbose = 1` routing output) was not exercised by this phase's work; say which clauses are
now evidenced and which are not.

Rewrite the KPI-04 entry. It currently reads as blocked on the US-003 reframe with its
measuring artefact being the Phase-2 gate against the removed solver. Replace the input class
with the one actually measured, transcribed from `docs/performance.md`, and name both live
artefacts: `benchmarks/oris_soft_vs_competitors.R` as the measurement and the `honest gate:`
assertion in `tests/testthat/test-bench-gate.R` as the regression gate, with the
`LBW_BENCH_GATE=1` invocation that runs it. SC4's second clause is that the KPI table's
performance rows each name a live measuring artefact — check the other performance-adjacent
rows in the same list while here and report, in the summary, any that still name something
that does not exist. Do not silently fix an unrelated KPI row; report it.

Update the Traceability table rows for US-003 and KPI-04 to the status the evidence supports,
and add rows to the Superseded table for the three PRD sites marked in this task, following
the table's existing column conventions.

Per D-03, do not reopen, comment on, or otherwise modify `leafblower-ylsy` — cite its
conclusion only. Cite `leafblower-kk1.20.4` as the REFRAME decision this phase resolved, and
record in the summary that its resolution is D-01 (kk1204 dropped as the headline basis)
rather than any of the three options as originally worded.
  </action>

  <verify>
    <automated>Rscript -e "r <- readLines('.planning/REQUIREMENTS.md'); p <- readLines('tasks/prd-leafblower-core.md'); stopifnot(any(grepl('oris_soft_vs_competitors.R', r, fixed=TRUE))); stopifnot(any(grepl('LBW_BENCH_GATE', r, fixed=TRUE))); stopifnot(any(grepl('docs/performance.md', r, fixed=TRUE))); s <- grep('100K rows, 5 margins', p); stopifnot(length(s) >= 2); stopifnot(all(sapply(s, function(i) any(grepl('[Ss]uperseded', p[i:min(i+2,length(p))]))))); stopifnot(any(grepl('docs/performance.md', p, fixed=TRUE))); cat('OK bookkeeping\n')"</automated>
  </verify>

  <acceptance_criteria>
    - Every line in `tasks/prd-leafblower-core.md` matching `100K rows, 5 margins` carries a superseded marker within two lines of itself, and the marker names `docs/performance.md`.
    - The §11 row's marker states that `test-lbfgsb.R` does not exist and that the solver it measured was withdrawn: `grep -c 'test-lbfgsb' tasks/prd-leafblower-core.md` is unchanged from before the edit (the line is marked, not deleted) and a superseded marker on that row mentions it.
    - `.planning/REQUIREMENTS.md` names both live artefacts and the invocation: `grep -c 'oris_soft_vs_competitors.R' .planning/REQUIREMENTS.md` returns at least 1, `grep -c 'test-bench-gate.R' .planning/REQUIREMENTS.md` returns at least 1, `grep -c 'LBW_BENCH_GATE' .planning/REQUIREMENTS.md` returns at least 1.
    - The US-003 entry no longer asserts the two now-false claims. Verify by reading the rewritten entry and quoting it in the summary alongside the text it replaced.
    - Both named artefacts exist and run: `test -f benchmarks/oris_soft_vs_competitors.R` and `CI=1 LBW_BENCH_GATE=1 Rscript -e "testthat::test_dir('tests/testthat', filter='bench-gate', stop_on_failure=TRUE)"` both exit 0.
    - The Traceability and Superseded tables are updated: the summary quotes the US-003 and KPI-04 traceability rows before and after, and the three new Superseded rows.
    - No beads ticket was modified: `git status --porcelain .beads/` shows no change attributable to this task beyond pre-existing working-tree noise, and the summary states that `leafblower-ylsy` was cited only.
  </acceptance_criteria>

  <done>One medium-scale number is readable across the repository, and every performance row in the KPI bookkeeping names an artefact that exists and runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bring the kk1204 assertion under the one gate convention (D-11, D-01)</name>

  <read_first>
    - `tests/testthat/test-bench-gate.R` lines 28-54 in full — the kk1204 block as it stands: `skip_if(Sys.getenv("CI") != "")`, `n = 500000`, `K = 20`, `method = "oris"`, `max_iterations = 500`, `expect_equal(status, 0L)`, `expect_lte(best_error, 1e-3)`, `expect_lte(elapsed, 30)`.
    - The same file's lines 1-26 and the `honest gate:` block — the `skip_if(Sys.getenv("LBW_BENCH_GATE") == "")` idiom every other heavy assertion in the file uses.
    - `.planning/phases/03-honest-performance-gate/03-02-SUMMARY.md` — the measured `known_limit_k20_uniform` numbers on the identical fixture parameters, which tell you what this assertion can honestly assert at current HEAD.
    - `docs/investigations/2026-04-23-kk1204-convergence.md` and `docs/performance.md`'s known-limit section — what this class is now documented as.
  </read_first>

  <precondition>`docs/performance.md` exists with a known-limit section, and `03-02-SUMMARY.md` records the measured `status`, `max_error`, `wall_s`, `deff` and `n_eff` for the `known_limit_k20_uniform` class at the same fixture parameters this block uses.</precondition>

  <files>tests/testthat/test-bench-gate.R</files>

  <behavior>
    - Before any edit, running the block in isolation with `CI` unset produces a recorded PASS or FAIL — its current state is established by observation, not assumed.
    - After the edit, with `LBW_BENCH_GATE` unset the block skips and a default `devtools::test()` run does not spend a 500,000-row 500-iteration solve on it.
    - After the edit, with `LBW_BENCH_GATE=1` the block runs and its assertions match what 03-02 measured on the same fixture parameters.
    - The block's `test_that()` description states what it measures now, not the retired composite target.
  </behavior>

  <action>
First, establish the block's current state by running it, before changing anything. RESEARCH.md
Open Question 1 flags that nobody knows whether this assertion currently passes: it runs
whenever `CI` is unset, that is in ordinary local development, and its thresholds
(`best_error <= 1e-3` at `n = 500000`) are a third variant distinct from both the PRD's
`n = 1M` / `< 1e-6` and the investigation's `n = 1M`. Run it in isolation with `CI` unset and
record the outcome, the wall time, the status code, the iteration count and the achieved
`best_error` verbatim in the summary. Decide from that observation, not from the plan's
expectation.

Then make two changes.

Change the skip guard from `skip_if(Sys.getenv("CI") != "")` to the file's own
`skip_if(Sys.getenv("LBW_BENCH_GATE") == "")` idiom, keeping `skip_on_cran()`. This is D-11's
one-gate-convention applied to the last assertion in the file that has its own. The current
guard has it backwards relative to every other heavy gate here: it runs by default locally and
skips only under CI, so an ordinary `devtools::test()` pays for a 500,000-row, 500-iteration
solve nobody asked for, while the project's stated Definition of Done says the heavy
regression checks are opt-in. After this change `benchmarks/run_honest_gate.sh` — which
already exports `LBW_BENCH_GATE=1` — becomes the place this assertion runs, which is where it
belongs.

Rewrite the `test_that()` description and the assertion labels to say what the block actually
is. It is not a headline gate: D-01 dropped this class as the basis of any headline claim, and
`docs/performance.md` now documents it as a known limit with a measured zero-compression
reason. It is a regression floor on the current best-effort result for the documented
degenerate class. Say that. Add a short comment above the block cross-referencing
`docs/performance.md`'s known-limit section and
`docs/investigations/2026-04-23-kk1204-convergence.md`, so a maintainer reading a failure
lands on the documentation rather than on the retired target.

Reconcile the thresholds against what 03-02 actually measured on identical fixture
parameters. If the measurements support the existing `1e-3` and 30-second values, keep them
and add a comment recording that they were confirmed against the 03-02 run and on what
machine. If they do not, adjust to what the measurement supports, with the same recorded
derivation — and report the adjustment prominently in the summary, because a threshold moving
is exactly the kind of change that should be seen rather than absorbed. If the block was
already failing before this plan, say so plainly in the summary; do not relax a threshold to
make a pre-existing failure disappear without stating that is what happened.

Do not delete the block. It is the only live artefact that exercises the documented known
limit, and SC3 is better served by a running assertion pointing at the documentation than by
a documentation section with nothing behind it.

Per D-03, no beads work on `leafblower-ylsy`: cite its conclusion in the comment if useful,
but do not reopen it, comment on it, or file a follow-up against it.
  </action>

  <verify>
    <automated>CI=1 LBW_BENCH_GATE=1 Rscript -e "testthat::test_dir('tests/testthat', filter='bench-gate', stop_on_failure=TRUE)" 2>&1 | tee /tmp/lbw-gate4.log; grep -q 'kk1204' /tmp/lbw-gate4.log && Rscript -e "l <- readLines('tests/testthat/test-bench-gate.R'); stopifnot(length(grep('LBW_BENCH_GATE', l)) == 4); stopifnot(length(grep('Sys.getenv\\\\(\"CI\"\\\\)', l)) == 0); cat('OK regate\n')"</automated>
  </verify>

  <acceptance_criteria>
    - The block's current pre-edit state is recorded in the summary with its observed status code, iteration count, `best_error`, wall time and PASS/FAIL verdict.
    - The `CI`-based guard is gone and the file uses one convention throughout: `grep -c 'LBW_BENCH_GATE' tests/testthat/test-bench-gate.R` returns 4 and `grep -c 'Sys.getenv("CI")' tests/testthat/test-bench-gate.R` returns 0.
    - A default run no longer pays for the solve: `Rscript -e "devtools::test(filter='bench-gate')"` completes in under 30 seconds and reports the block as SKIP.
    - With the gate on, the block runs and passes: `CI=1 LBW_BENCH_GATE=1 Rscript -e "testthat::test_dir('tests/testthat', filter='bench-gate', stop_on_failure=TRUE)"` exits 0.
    - The block points at the documentation: `grep -c 'docs/performance.md\|2026-04-23-kk1204-convergence' tests/testthat/test-bench-gate.R` returns at least 1.
    - Any threshold change is stated with its derivation and its before/after values in the summary; if no threshold changed, the summary states that the 03-02 measurement confirmed them and names the machine.
    - The block still exists: `grep -c 'kk1204' tests/testthat/test-bench-gate.R` returns at least 1.
  </acceptance_criteria>

  <done>Every heavy assertion in the file runs under one opt-in convention, and the kk1204 block says what it measures and points at the documentation that explains why.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by this plan (03-04):
- `README.md` — new file, at most 25 lines: package name, one orienting sentence, the one-line headline claim, the link to `docs/performance.md`.
- `.planning/REQUIREMENTS.md` — rewritten US-003 and KPI-04 entries; updated Traceability rows for both; three new Superseded rows.
- `tasks/prd-leafblower-core.md` — superseded markers at §1's medium-scale line, the L-BFGS-B acceptance checkbox, and §11's KPI row.
- `tests/testthat/test-bench-gate.R` — kk1204 block re-gated onto `LBW_BENCH_GATE`, relabelled as a known-limit regression floor, cross-linked to the documentation.

Phase 3 artefact inventory, complete across all four plans:
`benchmarks/oris_soft_vs_competitors.R`, `benchmarks/run_honest_gate.sh`,
`benchmarks/results/oris_soft_vs_competitors.csv`,
`benchmarks/results/oris_soft_vs_competitors_env.txt`, `docs/performance.md`, `README.md`,
the `honest gate:` assertion and the re-gated kk1204 block in
`tests/testthat/test-bench-gate.R`, and the rewritten US-003 / KPI-04 bookkeeping.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/performance.md` -> `README.md` | The claim crosses onto the package's most-read surface by transcription. |
| frozen historical PRD -> maintained requirements | Editing a frozen document risks erasing the record of what was originally promised. |
| default test run -> heavy solve | Changing a skip guard changes what an ordinary contributor's `devtools::test()` executes. |

No new runtime attack surface: this plan writes markdown and one test guard. ASVS L1
categories V2, V3, V4, V5 and V6 are not applicable.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Repudiation | `README.md` headline claim | medium | mitigate | The README line links to `docs/performance.md`, which carries the machine, versions, input class, achieved accuracy and the single regeneration command. The acceptance criteria require every numeric value in `README.md` to also appear in `docs/performance.md`, so the front-surface claim cannot drift from its evidence. |
| T-03-05 | Tampering | supersede edits to `tasks/prd-leafblower-core.md` | low | mitigate | Task 2 marks the three sites in place and explicitly forbids deleting them, preserving the record of what was originally promised. The verify step asserts each matching line still exists and now carries a marker, so an edit that removed rather than annotated a promise fails the check. |
| T-03-06 | Denial of service | kk1204 skip-guard inversion | low | mitigate | The current guard makes an ordinary local `devtools::test()` pay for a 500,000-row, 500-iteration solve. Task 3 moves it onto the opt-in `LBW_BENCH_GATE` convention and the acceptance criteria assert a default filtered run completes in under 30 seconds, removing a self-inflicted cost from every contributor's default test run. |
| T-03-02 | Information disclosure | `README.md` | low | mitigate | The README describes no fixture in detail; the large-fixture description lives on `docs/performance.md`, which 03-03 already constrained to aggregate shape with no `uuid` and no out-of-repo path. |
| T-03-SC | Tampering | package-manager installs | low | accept | No install occurs in this plan; no package manager is invoked. |

No threat is rated high; nothing blocks at the configured ASVS L1 / block-on-high threshold.
</threat_model>

<verification>
- `README.md` exists, is at most 25 lines, links to `docs/performance.md`, and carries no Phase 5 distribution content.
- Every `100K rows, 5 margins` line in `tasks/prd-leafblower-core.md` carries a superseded marker naming `docs/performance.md`, and none was deleted.
- `.planning/REQUIREMENTS.md`'s US-003 and KPI-04 entries name `benchmarks/oris_soft_vs_competitors.R`, `tests/testthat/test-bench-gate.R` and the `LBW_BENCH_GATE=1` invocation, and both named artefacts exist and run.
- `grep -c 'Sys.getenv("CI")' tests/testthat/test-bench-gate.R` returns 0 and `grep -c 'LBW_BENCH_GATE' tests/testthat/test-bench-gate.R` returns 4.
- `Rscript -e "devtools::test(filter='bench-gate')"` completes in under 30 seconds with every heavy block reported as SKIP.
- `CI=1 LBW_BENCH_GATE=1 Rscript -e "testthat::test_dir('tests/testthat', filter='bench-gate', stop_on_failure=TRUE)"` exits 0.
- `CI=1 bash benchmarks/run_honest_gate.sh` exits 0 — the one command still regenerates every published figure and now also runs the re-gated kk1204 block.
- Project Definition of Done unbroken: `R CMD INSTALL --preclean .` then `Rscript -e "devtools::test()"` reports 0 FAIL, and the Python parity suite under single-thread BLAS reports 0 failures.
</verification>

<success_criteria>
- A reader of the package's front surface sees one performance figure, measured on this codebase with a live solver, linked to the page that states its input class and machine.
- Exactly one medium-scale number is readable across the repository; the `< 1 s` / `< 2 s` contradiction is retired in place at all three of its sites.
- Every performance row in the KPI bookkeeping names an artefact that exists and runs, and the void `test-lbfgsb.R` reference is annotated as void rather than left standing.
- Every heavy assertion in `tests/testthat/test-bench-gate.R` runs under the single `LBW_BENCH_GATE` convention, and the kk1204 block is labelled and cross-linked as the known-limit regression floor it is.
- No beads ticket was reopened or modified.
</success_criteria>

<output>
Create `.planning/phases/03-honest-performance-gate/03-04-SUMMARY.md` when done. Record: the
kk1204 block's observed pre-edit state (status, iterations, `best_error`, wall time,
PASS/FAIL); whether any of its thresholds moved and, if so, the before/after values and the
derivation; the US-003 entry quoted before and after; the KPI-04 entry quoted before and
after; the three PRD supersede markers as written; every numeric value in `README.md` next to
its `docs/performance.md` source; any other KPI row found still naming a non-existent
artefact (reported, not fixed); and confirmation that `leafblower-ylsy` was cited only and
`leafblower-kk1.20.4`'s resolution is recorded as D-01.
</output>
