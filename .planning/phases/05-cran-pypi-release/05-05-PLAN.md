---
phase: 05-cran-pypi-release
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements: [US-010, US-008, KPI-05, KPI-06]

estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full local DoD gate (R CMD INSTALL --preclean . + testthat, uv pip install -e . + pytest, single-thread BLAS) is green, including the new version-sync test on both sides of the language boundary it can be discovered from."
    - "The final state of the tree still passes 05-01's hygiene guard and R CMD check --as-cran, after all four prior plans' edits."
    - "A human has confirmed, with full knowledge of what remains CI-only (the true manylinux/macOS x Python 3.9-3.13 matrix, unrunnable without a git remote), that this phase's deliverables are acceptable to close."
  artifacts: []
  key_links:
    - "This plan verifies the combined state of 05-01 through 05-04's outputs together — a passing DoD gate alone does not prove the CI workflows are individually correct, and vice versa; both must hold simultaneously"
---

<objective>
Phase gate: run the full local Definition-of-Done, re-confirm 05-01's hygiene and CRAN-check
results still hold after 05-02/05-03/05-04's edits, and get explicit human sign-off on what
this phase proved locally versus what remains CI-only given this repository has no git
remote.

Purpose: Every prior plan in this phase (05-01 through 05-04) verified its own slice in
isolation. This plan is the integration check — the combined state, run once, all together
— matching this project's established phase-gate pattern (e.g. Phase 2's 02-08). It also
closes the loop honestly: SC3/SC4 (the true cross-platform, multi-Python-version wheel
matrix) cannot be executed without pushing to a GitHub remote, which is out of scope for an
agent session in a local-only repository — this plan states that limitation to the user
rather than silently claiming full coverage.
Output: A confirmed-green combined local state and an explicit human sign-off on phase
closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cran-pypi-release/05-CONTEXT.md
@.planning/phases/05-cran-pypi-release/05-01-PLAN.md
@.planning/phases/05-cran-pypi-release/05-02-PLAN.md
@.planning/phases/05-cran-pypi-release/05-03-PLAN.md
@.planning/phases/05-cran-pypi-release/05-04-PLAN.md
@CLAUDE.md
</context>

## Artifacts this phase produces

None — this plan produces no new files. It verifies the combined output of 05-01 through
05-04 and records an explicit human sign-off decision.

<tasks>

<task type="auto">
  <name>Task 1: Full local DoD gate</name>
  <files></files>
  <read_first>
    CLAUDE.md § Build & Test (the exact gate commands: `R CMD INSTALL --preclean .`,
    `Rscript -e "devtools::test()"`, `uv pip install -e . --reinstall-package leafblower`,
    the single-thread BLAS pytest invocation).
  </read_first>
  <action>
    Run the project's standard Definition-of-Done gate exactly as documented in CLAUDE.md:
    `R CMD INSTALL --preclean .` then `Rscript -e "devtools::test()"` on the R side;
    `cd python && uv pip install -e . --reinstall-package leafblower` then
    `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest`
    on the Python side. This must include 05-02's new
    `python/leafblower/test_version_sync.py` in the pytest collection (confirm it appears in
    the pytest summary, not skipped). Do not weaken either command with extra flags.
  </action>
  <acceptance_criteria>
    R test suite reports 0 FAIL. Python pytest reports 0 failed, and
    `test_version_sync.py::test_description_pyproject_version_match` appears in the passed
    list.
  </acceptance_criteria>
  <verify>
    <automated>R CMD INSTALL --preclean . && Rscript -e "devtools::test()" 2>&1 | tail -20 && cd python && uv pip install -e . --reinstall-package leafblower && OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -v 2>&1 | tail -30</automated>
  </verify>
  <done>R suite 0 FAIL and Python pytest 0 failed, with the version-sync test present and passing in the Python collection.</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm hygiene and CRAN-check-clean after all edits</name>
  <files></files>
  <read_first>05-01-PLAN.md Task 1 (the exact hygiene grep and R CMD check sequence).</read_first>
  <action>
    Re-run 05-01's combined hygiene-plus-check verification one more time against the final
    tree state (after 05-02, 05-03, 05-04's file additions), to confirm nothing in those
    later plans reintroduced a tracked stray or broke the CRAN check. Also confirm both
    `.github/workflows/r-check.yml` and `.github/workflows/python-wheels.yml` exist and are
    valid YAML (re-run each plan's own YAML-parse verify command), and that `cran-comments.md`
    still exists with real (non-placeholder) content.
  </action>
  <acceptance_criteria>
    The hygiene grep still returns 0. `R CMD check --as-cran`'s final Status line still
    reads `OK` or `1 NOTE` (new submission). Both workflow YAML files parse without error.
    `cran-comments.md` exists.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git ls-files | grep -Ec '^(cell_table_92c4f45\.(cpp|hpp)|ieppa_92c4f45\.cpp|patch_raking\.py|patch_wolfe\.py|test_output\.log|leafblower_0\.1\.0\.tar\.gz|REVIEW_FINDINGS\.md|code-review-findings\.md|baseline_bench\.R|security-review-2026-04-27-ieppa\.json|leafblower\.Rcheck/)')" = "0" && test -f cran-comments.md && Rscript -e 'yaml::read_yaml(".github/workflows/r-check.yml"); yaml::read_yaml(".github/workflows/python-wheels.yml"); cat("both workflows valid YAML\n")'</automated>
  </verify>
  <done>Hygiene, CRAN-check-clean state, cran-comments.md, and both workflow files are all confirmed intact on the final combined tree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on phase closure</name>
  <what-built>
    A hygiene-clean git tree (no tracked dev artifacts), a locally-passing
    `R CMD check --as-cran`, `cran-comments.md` documenting the real result, a version-sync
    regression test, and two GitHub Actions workflow files
    (`.github/workflows/r-check.yml`, `.github/workflows/python-wheels.yml`) authored and
    structurally verified, with the underlying Python wheel-build/repair/import mechanics
    proven locally without Docker across all 5 targeted CPython versions (3.9-3.13).
  </what-built>
  <how-to-verify>
    Review Task 1's DoD gate output and Task 2's hygiene/check re-confirmation above. Note
    explicitly what this phase did NOT prove: this repository has no git remote (per
    STATE.md § Repository Facts), so neither `.github/workflows/*.yml` has ever actually
    executed on GitHub Actions. 05-04 proved Python-version compatibility (3.9-3.13) for
    real, locally, via uv-managed interpreters — what remains CI-only is the OS/glibc
    portability layer (a true manylinux-tagged wheel, macOS Accelerate linking) and the
    CI-driven `R CMD check --as-cran` re-run (SC1 in CI). Both remain unexecuted until this
    repository is pushed to a GitHub remote, a decision and action outside this phase's
    scope.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase with this scope, or name what should happen next (e.g., set up the GitHub remote now to trigger real CI runs, or file a follow-up ticket for it).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Combined final tree state → what a stranger actually receives | This gate is the last check before a human decides the phase's deliverables are ready; nothing downstream re-verifies it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Repudiation | Silently claiming CI-matrix coverage (SC3/SC4) that was never actually executed, because no git remote exists to run it on | medium | mitigate | The checkpoint above states this limitation explicitly to the user rather than marking SC3/SC4 fully closed on local-only evidence |
</threat_model>

<verification>
Task 1 and Task 2's automated commands must both pass. Task 3 requires explicit human
sign-off — no agent message substitutes for it, per this project's established checkpoint
discipline (Phase 2's 02-08 precedent).
</verification>

<success_criteria>
Full local DoD gate green; hygiene and CRAN-check-clean state confirmed on the final
combined tree; both CI workflow files present and valid; human has explicitly signed off on
phase closure with full knowledge of what remains CI-only.
</success_criteria>

<output>
Create `.planning/phases/05-cran-pypi-release/05-05-SUMMARY.md` when done
</output>
