---
phase: 05-cran-pypi-release
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [".github/workflows/r-check.yml"]
autonomous: false
requirements: [US-010, KPI-05, KPI-06]

estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A GitHub Actions job exists that runs the exact R CMD build / R CMD check --as-cran sequence 05-01 proved locally, so a future push re-verifies SC1 automatically."
    - "The same job re-runs 05-01's tarball-hygiene grep as a regression guard, so a re-added stray fails CI instead of silently shipping again."
  artifacts:
    - ".github/workflows/r-check.yml (new)"
  key_links:
    - "r-check.yml's check step must invoke the identical command sequence 05-01's Task 1 verified locally (R CMD build . && R CMD check --as-cran) — a divergent CI command would prove nothing about the locally-verified state"
---

<objective>
Operationalize 05-01's proven local R packaging cycle as a GitHub Actions CI job, so it
re-runs automatically once this repository has a remote to push to.

Purpose: Wave 1 (05-01) proved the R CMD check --as-cran sequence passes clean on the
hygiene-fixed tree, once, locally. This plan is the "expansion" step per the tracer-first
directive: automate the exact same sequence in CI (D-02, locked) rather than reinventing a
divergent check. It also folds in the ongoing regression guard for tarball hygiene (SC2)
that Wave 0's gap analysis flagged as having no existing CI enforcement.
Output: `.github/workflows/r-check.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cran-pypi-release/05-CONTEXT.md
@.planning/phases/05-cran-pypi-release/05-RESEARCH.md
@.planning/phases/05-cran-pypi-release/05-01-PLAN.md
</context>

## Artifacts this phase produces

- `.github/workflows/r-check.yml` — new CI workflow: `R CMD build . && R CMD check
  --as-cran` plus the tarball-hygiene regression guard, on pinned r-lib/actions SHAs.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Confirm r-lib/actions legitimacy before pinning</name>
  <what-built>Nothing yet — this is the package-legitimacy confirmation for the exact
  GitHub Action reference this plan is about to pin into CI.</what-built>
  <what-to-verify>
    r-lib/actions (the tidyverse/r-lib-maintained standard R CI action set — `setup-r` and
    `setup-r-dependencies`) is confirmed at:
    https://github.com/r-lib/actions — org `r-lib` (RStudio/Posit-adjacent, tidyverse
    ecosystem maintainers), tag `v2` currently resolving to commit
    `d3c5be51b12e724e68f33216ca3c148b66d5f0b6` (confirmed via GitHub API at plan time,
    2026-08-15). This action was NOT flagged by the automated package-legitimacy gate (that
    gate scans PyPI/npm packages; r-lib/actions is a GitHub Action, out of its scan scope),
    but per RESEARCH.md's Known Threat Patterns table, any new CI Action reference should be
    confirmed before pinning, same discipline as a flagged package.
  </what-to-verify>
  <how-to-verify>
    Visit https://github.com/r-lib/actions and confirm it is the org/repo you expect
    (tidyverse-maintained, widely used for R package CI). Confirm the SHA above matches
    `git ls-remote https://github.com/r-lib/actions v2` if you want a second check.
  </how-to-verify>
  <resume-signal>Type "approved" to pin `r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6` (tag v2), or name a different reference to use instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author .github/workflows/r-check.yml</name>
  <files>.github/workflows/r-check.yml</files>
  <read_first>
    05-01-PLAN.md Task 1's exact verify command (the hygiene grep + `R CMD build . && R CMD
    check --as-cran` sequence this job must reproduce verbatim); CLAUDE.md § Build & Test
    (the project's DoD gate command conventions); RESEARCH.md § Architecture Patterns →
    System Architecture Diagram and Pattern 2 (two independent jobs, not one combined job —
    the R and Python toolchains share no setup steps).
  </read_first>
  <action>
    Create .github/workflows/r-check.yml, triggered on `push` and `pull_request`, with one
    job `r-check` running on `ubuntu-latest`: checkout (`actions/checkout@v4`), then
    `r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6` (pinned SHA, tag `v2` —
    r-lib/actions has no per-patch tags, so SHA-pinning is the exact-pin mechanism here, per
    RESEARCH.md's Known Threat Patterns table entry on Action supply-chain pinning), then
    `r-lib/actions/setup-r-dependencies@d3c5be51b12e724e68f33216ca3c148b66d5f0b6` with
    `extra-packages: any::rcmdcheck` and `needs: check`. Add a "Tarball hygiene guard" step
    BEFORE the check step that re-runs the exact grep from 05-01-PLAN.md Task 1's verify
    (matching the same 11 stray-file patterns plus `leafblower\.Rcheck/`) and fails the job
    (non-zero exit) if any match is found — this is the ongoing SC2 regression guard, not a
    one-off. Add the check step itself using
    `r-lib/actions/check-r-package@d3c5be51b12e724e68f33216ca3c148b66d5f0b6` with
    `args: 'c("--no-manual", "--as-cran")'` replaced by `args: 'c("--as-cran")'` (full
    manual/vignette build, matching 05-01's local run exactly — do not weaken the check with
    `--no-manual`) and `error-on: '"warning"'` so the job fails on any WARNING, not only
    ERROR. Do not add a Python setup step to this job — R and Python CI are deliberately
    separate jobs/files (05-04 owns the Python side), per RESEARCH.md Pattern 2.
  </action>
  <acceptance_criteria>
    .github/workflows/r-check.yml exists, is valid YAML, defines exactly one job named
    `r-check`, references `r-lib/actions/setup-r`, `r-lib/actions/setup-r-dependencies`, and
    `r-lib/actions/check-r-package` each pinned to the SHA
    `d3c5be51b12e724e68f33216ca3c148b66d5f0b6`, and contains a hygiene-guard step running
    before the check step that references at least the string `leafblower\.Rcheck`.
  </acceptance_criteria>
  <verify>
    <automated>Rscript -e 'wf <- yaml::read_yaml(".github/workflows/r-check.yml"); stopifnot(is.list(wf$jobs$"r-check")); cat("valid YAML, job present\n")'</automated>
  </verify>
  <reversibility rating="costly">D-02 (locked, CONTEXT.md-rated costly) — a CI pipeline is durable infrastructure; ripping it out later means re-deciding the whole matrix strategy. Not one-way (no checkpoint:decision required — the mechanism was already user-locked in CONTEXT.md), but flagged per REVERSIBILITY_GATES.</reversibility>
  <done>.github/workflows/r-check.yml exists, parses as valid YAML, and its check step reproduces 05-01's proven local R CMD check --as-cran sequence plus the ongoing hygiene guard.</done>
</task>

<task type="auto">
  <name>Task 2: Structural verification of the r-check.yml workflow</name>
  <files>.github/workflows/r-check.yml</files>
  <read_first>.github/workflows/r-check.yml (the file just written by Task 1).</read_first>
  <action>
    Confirm the workflow's structural correctness beyond bare YAML syntax: the job triggers
    on both `push` and `pull_request`; the `check-r-package` step's `args` includes
    `--as-cran` and does NOT include `--no-manual`; the `error-on` field is set to fail on
    `warning` (not the check-r-package default of `error`, which would let a WARNING through
    silently). No source-file changes — this task is a read-verify pass on Task 1's output.
  </action>
  <acceptance_criteria>
    grep confirms `--as-cran` present in r-check.yml; grep confirms `--no-manual` is absent;
    grep confirms `error-on:` is set to the literal `warning` (not `error`).
  </acceptance_criteria>
  <verify>
    <automated>grep -q -- '--as-cran' .github/workflows/r-check.yml && ! grep -q -- '--no-manual' .github/workflows/r-check.yml && grep -q 'error-on:.*warning' .github/workflows/r-check.yml && echo "structure OK"</automated>
  </verify>
  <reversibility rating="reversible">Pure structural verification, no new decision.</reversibility>
  <done>r-check.yml's `--as-cran` flag, absence of `--no-manual`, and `error-on: warning` setting are all confirmed present exactly as Task 1 specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repository secrets/environment | This job reads no secrets and writes no artifacts beyond CI logs — pure build-and-check, no elevated trust surface |
| Third-party Action supply chain → CI execution | `r-lib/actions/*` runs arbitrary code inside the CI runner on every push/PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | `r-lib/actions/setup-r`, `setup-r-dependencies`, `check-r-package` (third-party GitHub Actions) | high | mitigate | Pin to the exact commit SHA `d3c5be51b12e724e68f33216ca3c148b66d5f0b6` (not the floating `v2` tag) per RESEARCH.md's Known Threat Patterns table; confirmed via the checkpoint:human-verify task above before being written into the workflow |
| T-05-05 | Tampering | A future re-added stray file (any of the 11 named in 05-01, or a new one in the same category) silently re-entering the tarball because no CI check re-verifies hygiene | medium | mitigate | Task 1's "Tarball hygiene guard" step re-runs 05-01's exact grep on every push/PR, failing CI before merge |
</threat_model>

<verification>
Task 1's verify confirms the workflow is valid YAML with the required job and pinned SHAs.
Task 2's verify confirms the check-step flags (`--as-cran` present, `--no-manual` absent,
`error-on: warning`) are exactly as specified. Full CI execution (an actual GitHub Actions
run) requires a git remote, which this repository does not have — this plan proves the
workflow is correctly authored and locally structurally valid; the first real run happens
once a remote exists (out of this phase's control, tracked in 05-05's phase-gate summary).
</verification>

<success_criteria>
.github/workflows/r-check.yml exists, parses as valid YAML, reproduces 05-01's proven local
R CMD check --as-cran sequence with pinned Action SHAs, and re-runs the tarball-hygiene
guard on every push/PR.
</success_criteria>

<output>
Create `.planning/phases/05-cran-pypi-release/05-03-SUMMARY.md` when done
</output>