---
phase: 05-cran-pypi-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".Rbuildignore", "cran-comments.md"]
autonomous: true
requirements: [US-010, KPI-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R CMD check --as-cran on the built tarball reports 0 errors and 0 warnings, with at most the new-submission NOTE."
    - "git ls-files shows none of the 11 named dev-artifact strays or the leafblower.Rcheck/ tree tracked in the repository."
    - "cran-comments.md exists, explains the no--O and LAPACK build decisions, and states the actual observed R CMD check result (not placeholder text)."
  artifacts:
    - ".Rbuildignore (14 new anchored patterns)"
    - "cran-comments.md (new)"
  key_links:
    - ".Rbuildignore patterns match exactly the files git-rm'd from tracking (defense-in-depth only works if the patterns and the removed files agree)"
    - "cran-comments.md's build-flag/LAPACK explanation quotes src/Makevars.in and python/CMakeLists.txt verbatim, so the doc cannot drift from the source-of-truth comments"
---

<objective>
Tracer: prove the entire R release pipeline end-to-end, once, for real — remove every
git-tracked development artifact this repository currently ships, extend `.Rbuildignore`
as defense-in-depth, then run the actual `R CMD build . && R CMD check --as-cran` cycle
locally and confirm it comes back clean. Write `cran-comments.md` against the real,
observed result.

Purpose: This is the phase's single highest-risk unknown — nobody has run
`R CMD check --as-cran` against a hygiene-clean tree in this project's history. Proving it
locally, first, before any CI is authored (05-03/05-04 build on this proven state), is the
tracer-first requirement for this phase: one production-quality, fully verified vertical
slice through the R packaging pipeline before the Python track or CI automation is added.
Output: A git tree with no tracked dev artifacts, a passing local `R CMD check --as-cran`,
and `cran-comments.md` documenting the real result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cran-pypi-release/05-CONTEXT.md
@.planning/phases/05-cran-pypi-release/05-RESEARCH.md
@.Rbuildignore
@src/Makevars.in
@python/CMakeLists.txt
</context>

<!-- planner-discipline-allow: cell_table_92c4f45 -->
<!-- planner-discipline-allow: ieppa_92c4f45 -->
<!-- planner-discipline-allow: patch_raking -->
<!-- planner-discipline-allow: patch_wolfe -->
<!-- planner-discipline-allow: test_output.log -->
<!-- planner-discipline-allow: leafblower_0.1.0.tar.gz -->
<!-- planner-discipline-allow: REVIEW_FINDINGS -->
<!-- planner-discipline-allow: code-review-findings -->
<!-- planner-discipline-allow: baseline_bench -->
<!-- planner-discipline-allow: security-review-2026-04-27-ieppa -->
<!-- planner-discipline-allow: leafblower.Rcheck -->

## Artifacts this phase produces

- `.Rbuildignore` — 14 new anchored patterns appended (11 for git-rm'd strays, 3 for
  legitimately-kept tooling config).
- `cran-comments.md` — new file, documents the real observed R CMD check result.
- Removed from git tracking: `cell_table_92c4f45.cpp`, `cell_table_92c4f45.hpp`,
  `ieppa_92c4f45.cpp`, `patch_raking.py`, `patch_wolfe.py`, `test_output.log`,
  `leafblower_0.1.0.tar.gz`, `REVIEW_FINDINGS.md`, `code-review-findings.md`,
  `baseline_bench.R`, `security-review-2026-04-27-ieppa.json`, and the entire
  `leafblower.Rcheck/` tree (~120 files).

<tasks>

<task type="tracer">
  <name>Task 1: Remove tracked dev artifacts, extend .Rbuildignore, prove R CMD check --as-cran clean</name>
  <files>.Rbuildignore</files>
  <read_first>
    .Rbuildignore (all 23 current lines — match the existing `^literal\.ext$` anchoring style,
    one pattern per line, no wildcard globs anywhere in the file); src/Makevars.in (the
    no-`-O` comment block, lines 12-14); the 05-RESEARCH.md § Runtime State Inventory table
    (full disposition reasoning for every stray file, already resolved: 11 root files are
    dev artifacts to `git rm` + `.Rbuildignore`, `leafblower.Rcheck/` is a 120-file tracked
    build-output tree to `git rm -r`, and `GEMINI.md`/`.mcp.json`/`.coverage-thresholds.json`
    are legitimate tooling config to `.Rbuildignore` only — NOT `git rm`, same treatment as
    the already-`.Rbuildignore`'d `CLAUDE.md`).
  </read_first>
  <precondition>Working tree has unrelated pre-existing uncommitted deletions (.wolf/, .tldr/, .beads/plans/active-plan.md, .claude/settings.json — confirmed via `git status --porcelain` at plan time) that are NOT part of this phase's scope; commit only the explicit pathspec below, never `git add -A`.</precondition>
  <action>
    git rm (working-tree + index) exactly these 11 root-level tracked files, per D-01
    (leafblower-l6h0) extended to the 2 same-bucket strays RESEARCH.md's Runtime State
    Inventory found beyond D-01's original 9-file list: `cell_table_92c4f45.cpp`,
    `cell_table_92c4f45.hpp`, `ieppa_92c4f45.cpp`, `patch_raking.py`, `patch_wolfe.py`,
    `test_output.log`, `leafblower_0.1.0.tar.gz`, `REVIEW_FINDINGS.md`,
    `code-review-findings.md`, `baseline_bench.R`, `security-review-2026-04-27-ieppa.json`.
    Then `git rm -r leafblower.Rcheck` — the entire tracked ~120-file R-check output tree
    (confirmed 120 files via `git ls-files | grep -c '^leafblower.Rcheck/'` at plan time),
    including the nested `leafblower.Rcheck/00_pkg_src/leafblower/patch_wolfe.py` duplicate
    of the root stray. This directory is already `.gitignore`'d (`*.Rcheck/`, line 16) so a
    future `R CMD check` run regenerating it will not be re-staged automatically — no
    `.gitignore` change needed, per D-01's "both ends" being `git rm` + `.Rbuildignore` only.
    Append 14 new anchored patterns to `.Rbuildignore`, matching the existing style exactly
    (append after the last existing line, one per line, no trailing comments): the 11
    literal patterns for the files just removed (`^cell_table_92c4f45\.cpp$`,
    `^cell_table_92c4f45\.hpp$`, `^ieppa_92c4f45\.cpp$`, `^patch_raking\.py$`,
    `^patch_wolfe\.py$`, `^test_output\.log$`, `^leafblower_0\.1\.0\.tar\.gz$`,
    `^REVIEW_FINDINGS\.md$`, `^code-review-findings\.md$`, `^baseline_bench\.R$`,
    `^security-review-2026-04-27-ieppa\.json$`) plus 3 more for the legitimately-kept-tracked
    tooling config (`^GEMINI\.md$`, `^\.mcp\.json$`, `^\.coverage-thresholds\.json$`) — these
    3 stay git-tracked, only excluded from the tarball, same treatment as the existing
    `^CLAUDE\.md$` line. Do NOT touch `leafblower.Rcheck`'s existing `.Rbuildignore` pattern
    (already present, line 11) or the `cleanup` file (confirmed standard R packaging special
    file per RESEARCH.md, not a stray). Then run the real cycle: `R CMD build .` followed by
    `R CMD check --as-cran` on the produced tarball. This compiles the C++17 core and runs
    the full test suite non-interactively; allow up to 10 minutes. After the check completes,
    remove the build-generated `leafblower_*.tar.gz` and `leafblower.Rcheck/` from the working
    tree (`rm -f leafblower_*.tar.gz && rm -rf leafblower.Rcheck`) so nothing new gets
    accidentally staged — this exact regeneration pattern is what produced the original
    tracked-artifact problem this task just fixed.
  </action>
  <acceptance_criteria>
    `git ls-files | grep -Ec '^(cell_table_92c4f45\.(cpp|hpp)|ieppa_92c4f45\.cpp|patch_raking\.py|patch_wolfe\.py|test_output\.log|leafblower_0\.1\.0\.tar\.gz|REVIEW_FINDINGS\.md|code-review-findings\.md|baseline_bench\.R|security-review-2026-04-27-ieppa\.json|leafblower\.Rcheck/)'`
    returns `0`. `.Rbuildignore` has 37 lines (23 existing + 14 new), each new line matching
    the `^literal\.ext$` anchoring style. `R CMD check --as-cran`'s final `Status:` line
    reads `OK` or `1 NOTE` where the NOTE text contains `New submission`; zero lines matching
    `^(WARNING|ERROR):` appear anywhere in the check output.
  </acceptance_criteria>
  <verify>
    <automated>git ls-files | grep -Ec '^(cell_table_92c4f45\.(cpp|hpp)|ieppa_92c4f45\.cpp|patch_raking\.py|patch_wolfe\.py|test_output\.log|leafblower_0\.1\.0\.tar\.gz|REVIEW_FINDINGS\.md|code-review-findings\.md|baseline_bench\.R|security-review-2026-04-27-ieppa\.json|leafblower\.Rcheck/)' ; test "$(git ls-files | grep -Ec '^(cell_table_92c4f45\.(cpp|hpp)|ieppa_92c4f45\.cpp|patch_raking\.py|patch_wolfe\.py|test_output\.log|leafblower_0\.1\.0\.tar\.gz|REVIEW_FINDINGS\.md|code-review-findings\.md|baseline_bench\.R|security-review-2026-04-27-ieppa\.json|leafblower\.Rcheck/)')" = "0" && R CMD build . && R CMD check --as-cran "$(ls -t leafblower_*.tar.gz | head -1)" 2>&1 | tail -40</automated>
  </verify>
  <reversibility rating="reversible">D-01 (locked, CONTEXT.md-rated reversible) — files remain in git history, git rm doesn't destroy anything, .Rbuildignore patterns are trivially editable.</reversibility>
  <done>The named strays and the leafblower.Rcheck/ tree are gone from `git ls-files`; .Rbuildignore carries the 14 new patterns; a local `R CMD check --as-cran` run against the cleaned tree reports 0 errors/0 warnings with at most the new-submission NOTE.</done>
</task>

<task type="auto">
  <name>Task 2: Write cran-comments.md against the real Task 1 result</name>
  <files>cran-comments.md</files>
  <read_first>
    src/Makevars.in lines 12-14 (the exact no-`-O` rationale comment: "R supplies the
    user/site -O level via $(CXXFLAGS) in $(ALL_CXXFLAGS); a user wanting -O3 sets it in
    ~/.R/Makevars"); python/CMakeLists.txt lines 93-99 (`find_package(LAPACK REQUIRED)` and
    the `-O3`/asymmetry comment referencing "phase-02 SC2, leafblower-qzto"); src/Makevars.in
    line 16 (`PKG_LIBS = ... $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)` — R's own build-supplied
    LAPACK linking, distinct from the Python side's hard CMake dependency); the exact
    `R CMD check --as-cran` output captured by Task 1's verify command (use the REAL
    Status/NOTE lines, not placeholder text); 05-CONTEXT.md § cran-comments.md content
    (D-04) and § Publication channel (D-05, D-06).
  </read_first>
  <action>
    Create cran-comments.md (already `.Rbuildignore`d at line 8, `^cran-comments\.md$` —
    correct, CRAN wants this file outside the tarball) with four sections: "## Submission
    type" stating this is an intermediate GitHub + r-universe release per D-05, not a CRAN
    web-form submission, and that `R CMD check --as-cran` is still run and must pass per D-06
    as the honest quality bar regardless of publication channel; "## Test environments"
    naming the actual local environment (`R 4.6.1, Linux x86_64` — from Task 1's run) plus a
    forward-reference to the CI matrix authored in 05-03/05-04; "## R CMD check results"
    populated with Task 1's actual observed `Status:` line and NOTE text (word-for-word, not
    the RESEARCH.md skeleton's placeholder "0 errors | 0 warnings | 1 note"); "## Notes on
    build configuration" quoting (not paraphrasing) the no-`-O` rationale from src/Makevars.in
    and the LAPACK-hard-dependency distinction between the R side (`$(LAPACK_LIBS)` via R's
    own build macros, no extra `SystemRequirements` needed) and the Python side
    (`find_package(LAPACK REQUIRED)`, no fallback) per D-04.
  </action>
  <acceptance_criteria>
    cran-comments.md exists with all four named sections; the "## R CMD check results"
    section's Status line matches Task 1's actual verify output byte-for-byte (not the
    RESEARCH.md skeleton's placeholder text); the "## Notes on build configuration" section
    contains the literal phrase "R supplies the user/site" (quoting Makevars.in) and the
    literal phrase "find_package(LAPACK REQUIRED)" (quoting CMakeLists.txt).
  </acceptance_criteria>
  <verify>
    <automated>test -f cran-comments.md && grep -c "^## Submission type$" cran-comments.md && grep -c "^## Test environments$" cran-comments.md && grep -c "^## R CMD check results$" cran-comments.md && grep -c "^## Notes on build configuration$" cran-comments.md && grep -c "find_package(LAPACK REQUIRED)" cran-comments.md</automated>
  </verify>
  <reversibility rating="reversible">D-04 (locked, CONTEXT.md-rated reversible) — a markdown file, freely editable.</reversibility>
  <done>cran-comments.md exists, documents the real observed R CMD check result, and explains the no-`-O` and LAPACK build decisions by quoting the actual source-of-truth comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree → source tarball | `R CMD build` decides what ships to a stranger's machine; anything tracked-and-not-`.Rbuildignore`'d crosses this boundary |
| Repository history → public distribution | Once `leafblower_0.1.0.tar.gz`/`leafblower.Rcheck/` are removed from tracking, they still exist in git history — acceptable per D-01's own reversibility rating, but a future `git gc`/history-rewrite is explicitly out of this phase's scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `REVIEW_FINDINGS.md`, `code-review-findings.md`, `security-review-2026-04-27-ieppa.json` (internal review/security findings docs, currently tarball-shippable) | medium | mitigate | Task 1: `git rm` + `.Rbuildignore` — these documents never reach a stranger's `install.packages()` tarball |
| T-05-02 | Tampering | `leafblower_0.1.0.tar.gz` (a stale nested tarball, currently git-tracked) shipping inside the real, current tarball, confusing which version a user actually got | low | mitigate | Task 1: `git rm` — the nested tarball is a known artifact of an earlier, already-superseded build, not a live threat vector, but its presence is confusing/incorrect either way |
</threat_model>

<verification>
Run Task 1's combined verify command. It fails (nonzero grep count, or a `WARNING`/`ERROR`
line in the check output) if either the hygiene fix or the CRAN-check-clean state is
incomplete. Task 2's verify confirms cran-comments.md exists with real, non-placeholder
content.
</verification>

<success_criteria>
`git ls-files` shows none of the 11 named strays or `leafblower.Rcheck/` tracked.
`R CMD check --as-cran` on the rebuilt tarball reports 0 errors/0 warnings, at most the
new-submission NOTE. `cran-comments.md` exists and documents the real result.
</success_criteria>

<output>
Create `.planning/phases/05-cran-pypi-release/05-01-SUMMARY.md` when done
</output>
