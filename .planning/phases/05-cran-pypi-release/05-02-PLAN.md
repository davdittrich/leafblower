---
phase: 05-cran-pypi-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["python/leafblower/test_version_sync.py"]
autonomous: true
requirements: [US-010]

estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The DESCRIPTION Version and python/pyproject.toml version cannot silently drift — a mismatch fails an automated test, in both the local DoD gate and CI."
  artifacts:
    - "python/leafblower/test_version_sync.py (new)"
  key_links:
    - "The regex extracting DESCRIPTION's Version: line and pyproject.toml's version = \"...\" line must correctly parse both files' actual current single-line formats (verified: DESCRIPTION:3 = 'Version: 0.1.0', pyproject.toml:10 = 'version = \"0.1.0\"')"
---

<objective>
Add a regression guard: an automated test that reads `DESCRIPTION`'s `Version:` field and
`python/pyproject.toml`'s `version = "..."` field and fails if they disagree.

Purpose: D-03 (locked) — this is a test-suite assertion, not a CI-only step or pre-commit
hook, so it runs on every local DoD gate invocation as well as in CI. This is the phase's
SC5 (version drift caught by a check, not by a reader).
Output: One new test file, passing today (both files read `0.1.0`), that will fail loudly
the moment either file is edited without the other.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cran-pypi-release/05-CONTEXT.md
@.planning/phases/05-cran-pypi-release/05-RESEARCH.md
@python/leafblower/test_core_sources_sync.py
@DESCRIPTION
@python/pyproject.toml
</context>

## Artifacts this phase produces

- `python/leafblower/test_version_sync.py` — new regression test guarding DESCRIPTION vs.
  pyproject.toml version drift.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DESCRIPTION/pyproject.toml version-sync test</name>
  <files>python/leafblower/test_version_sync.py</files>
  <read_first>
    python/leafblower/test_core_sources_sync.py (full file, 35 lines) — the exact analog:
    same class of test (build/config-drift guard, read two files, regex-extract, assert
    equality), same docstring shape (`SC<N> (leafblower-<id> / phase <N>)` header, WHY the
    drift is dangerous, WHERE it surfaces without the test, a "Verified baseline (date): X
    == Y" closing line), same `REPO_ROOT = Path(__file__).resolve().parents[2]` repo-root
    resolution (this file lives at the identical directory depth:
    `python/leafblower/test_version_sync.py` → `parents[2]` → repo root); DESCRIPTION line 3
    (`Version: 0.1.0`); python/pyproject.toml line 10 (`version = "0.1.0"`).
  </read_first>
  <behavior>
    - Test 1: DESCRIPTION `Version:` and pyproject.toml `version = "..."` both parse and are
      equal today (`0.1.0` == `0.1.0`) — test passes.
    - Test 2 (reasoned, not separately exercised): if either file's version string changes
      without the other, `assert desc_version == py_version` fails with a message naming
      both observed values — this is the drift this test exists to catch.
  </behavior>
  <action>
    Create python/leafblower/test_version_sync.py following test_core_sources_sync.py's
    structure exactly: `REPO_ROOT = Path(__file__).resolve().parents[2]`; one function
    `test_description_pyproject_version_match()`; extract DESCRIPTION's version via
    `re.search(r"^Version:\s*(\S+)", desc_text, re.M).group(1)`; extract pyproject.toml's
    version via `re.search(r'^version\s*=\s*"([^"]+)"', pyproject_text, re.M).group(1)` — a
    plain regex read of both files' simple single-line format, no TOML-parsing dependency
    (avoids the `tomllib` 3.11+ vs `tomli` <3.11 split entirely, per RESEARCH.md's Don't
    Hand-Roll table); `assert desc_version == py_version, f"DESCRIPTION Version={{desc_version!r}} != pyproject.toml version={{py_version!r}}"`.
    Docstring: cite this ticket (`leafblower-l6h0` / phase 05, SC5), explain that without
    this test a `DESCRIPTION`-only or `pyproject.toml`-only version bump silently ships a
    mismatched pair to CRAN and PyPI, and close with the verified baseline line
    ("Verified baseline (2026-08-15): DESCRIPTION Version=0.1.0 == pyproject.toml
    version=0.1.0 — zero drift today"). No mocking or fixtures — plain `Path.read_text()`,
    matching the analog's minimalism.
  </action>
  <acceptance_criteria>
    `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest python/leafblower/test_version_sync.py -v`
    passes with exactly 1 test collected and 1 passed. The extracted `desc_version` and
    `py_version` both equal `0.1.0`.
  </acceptance_criteria>
  <verify>
    <automated>cd python && OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest leafblower/test_version_sync.py -v</automated>
  </verify>
  <reversibility rating="reversible">D-03 (locked, CONTEXT.md-rated reversible) — a single test file.</reversibility>
  <done>python/leafblower/test_version_sync.py exists, passes today, and will fail with a clear message the moment DESCRIPTION and pyproject.toml versions diverge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release metadata (two independently-editable files) | No trust boundary crosses untrusted input — this is a pure build-metadata consistency guard, not a security control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of release metadata, unintentional) | `DESCRIPTION` / `python/pyproject.toml` version strings | low | accept | No exploitable trust boundary here — a version mismatch is a release-hygiene bug, not an attacker-controlled input; the test itself is the mitigation for the low-severity honest-mistake case, not a security control |
</threat_model>

<verification>
Run the pytest command above; it must report 1 passed.
</verification>

<success_criteria>
The version-sync test exists, passes against the current in-sync `0.1.0`/`0.1.0` state, and
is included in the standard pytest collection (no special flag needed to discover it).
</success_criteria>

<output>
Create `.planning/phases/05-cran-pypi-release/05-02-SUMMARY.md` when done
</output>
