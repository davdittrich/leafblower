---
phase: 05-cran-pypi-release
plan: 09
type: execute
wave: 6
depends_on: ["05-08"]
files_modified:
  - DESCRIPTION
  - .github/workflows/r-check.yml
  - cran-comments.md
autonomous: true
gap_closure: true
requirements: [US-010, KPI-05]
beads: [leafblower-xfz4]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A CRAN-representative `R CMD check --as-cran` — every declared Suggests package installed, a missing Suggests package treated as an error rather than a skip — reports 0 errors and 0 warnings on the built tarball, locally AND on a real GitHub Actions run (D-06)."
    - "The GitHub Actions r-check log shows the PracTools, survey, arrow and DiceKriging test blocks EXECUTING; no test in the suite is skipped for want of an uninstalled Suggests package."
    - "Every package named in DESCRIPTION's Suggests field is resolvable from a mainstream repository, so the masking workaround has nothing left to work around and cannot be reintroduced without an immediately red check."
  artifacts:
    - "DESCRIPTION — Suggests field carries only repository-resolvable packages"
    - ".github/workflows/r-check.yml — dependency resolution installs Suggests, and an unavailable Suggests package fails the check instead of silently widening the skip set"
    - "cran-comments.md — check evidence restated against the full-Suggests result (D-04)"
  key_links:
    - "DESCRIPTION Suggests <-> r-check.yml dependency resolution: once the unresolvable entry is gone the workflow needs no per-package exclusion mechanism at all, which is why the fix is two deletions rather than a hand-maintained install list that would drift out of sync with DESCRIPTION the next time a Suggests entry is added"
    - "R CMD check's own missing-Suggests error is the durability mechanism: it is a built-in guard, so nothing bespoke has to be written or maintained to keep this gap closed"
    - "cran-comments.md is the artifact Phase 7 submits; if it still quotes the weakened check result, Phase 7 makes a claim to CRAN that CRAN's own machinery will contradict"
---

<objective>
Make this repository's `R CMD check` evidence CRAN-representative. The green GitHub Actions
result Phase 5 relied on was produced by a dependency-resolution setting that skipped ALL
Suggests packages — not just the one it was added to route around — so at least 25 tests
never ran, including the one that fails on r-universe's Suggests-complete build farm.

Purpose: SC1/KPI-05's claim of "0 errors, 0 warnings" must hold under the check CRAN actually
runs (D-06), not under a weakened one. 05-08 fixed the defect the masking hid; this plan
removes the masking itself and makes its return structurally impossible.
Output: a Suggests field with no unresolvable entry, a workflow that installs and exercises
Suggests, a real green CI run with the previously-skipped tests executing, and a
cran-comments.md that describes that run rather than the weakened one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cran-pypi-release/05-VERIFICATION.md
@.planning/phases/05-cran-pypi-release/05-CONTEXT.md
@DESCRIPTION
@.github/workflows/r-check.yml
</context>

## Artifacts this plan produces

| Artifact | Change |
|----------|--------|
| `DESCRIPTION` | `autumn (>= 0.2.0)` removed from Suggests — a GitHub-only package at version 0.1, so the declared constraint is unsatisfiable from any repository, and no shipped file calls it |
| `.github/workflows/r-check.yml` | Job-level environment override at line 12 deleted; dependency-resolution key at line 24 of the `setup-r-dependencies` `with:` block deleted; Suggests are installed and exercised |
| `cran-comments.md` | Check evidence restated against the Suggests-complete run (D-04, D-06) |

<tasks>

<task type="tracer">
  <name>Task 1: Make every declared Suggests resolvable, then prove a Suggests-complete check locally</name>
  <files>DESCRIPTION</files>
  <read_first>
    - `DESCRIPTION` lines 19-20 — the full Suggests field:
      `autumn (>= 0.2.0), testthat (>= 3.0.0), bench, lhs, DiceKriging, ggplot2, rprojroot, survey, PracTools (>= 1.4.0), arrow, callr, jsonlite, withr`.
    - `.planning/phases/05-cran-pypi-release/05-VERIFICATION.md` gap 1 — the log lines
      `{PracTools} is not installed (1)`, `{DiceKriging} is not installed (1)`,
      `{arrow} is not installed (21)`, `{survey} is not installed (2)` from run 32076158299.
    - `R/harvest.R` lines 1, 34, 253 and `man/harvest.Rd` lines 5, 79 — every remaining
      mention of the package about to be dropped is roxygen prose inside `\code{...}`, not a
      call and not an `\link[...]{}` cross-reference.
    - `.Rbuildignore` entries `^benchmarks$` and `^tests/testthat/fixtures$` — the only two
      directories that reference it are excluded from the tarball, so nothing R CMD check
      runs can need it.
  </read_first>
  <action>
    Delete `autumn (>= 0.2.0), ` from the Suggests field in `DESCRIPTION`, leaving the other
    twelve entries untouched and the field's line wrapping valid.

    This is the root fix, not a workaround: autumn is distributed only from
    `github.com/aaronrudkin/autumn` at version 0.1, so the declared `>= 0.2.0` constraint is
    unsatisfiable from CRAN, from r-universe, and from any `Additional_repositories` feed. It
    is the single entry that forced the whole-Suggests masking in the first place, and it is
    also a CRAN incoming-check NOTE ("Suggests or Enhances not in mainstream repositories")
    waiting to fire in Phase 7. Before deleting, confirm with a repo-wide search that no
    shipped file calls it: matches must be limited to roxygen prose in `R/harvest.R` and
    `man/harvest.Rd`, two comments in `src/leafblower.h`, and files under `benchmarks/` and
    `tests/testthat/fixtures/`, both `.Rbuildignore`d. Leave that prose alone — the package
    is still described as a drop-in replacement for it, and naming a package in prose creates
    no dependency.

    Then install the remaining twelve Suggests locally and run the CRAN-representative check
    on the BUILT TARBALL (not on the source directory), leaving the missing-Suggests guard
    at its default — that is, do not set any environment variable that downgrades an
    unavailable Suggests package to a skip. survey, arrow and DiceKriging are already present
    on this machine and PracTools was installed by 05-08; install whatever else is missing
    from CRAN first, so the check does not error on the very guard being restored.

    Expect 0 errors and 0 warnings. NOTEs are acceptable only if `cran-comments.md` already
    explains them or Task 3 will; if a NEW note appears that is caused by the now-installed
    Suggests, record it verbatim in the SUMMARY for Task 3 to address. If a test that was
    previously skipped now FAILS, halt and report: that is a second instance of
    leafblower-xfz4's failure mode and needs its own root-cause fix, never a re-mask.
  </action>
  <acceptance_criteria>
    - Every declared Suggests entry exists on CRAN:
      `Rscript -e 'pk <- trimws(gsub("\\s*\\(.*\\)","",strsplit(read.dcf("DESCRIPTION","Suggests")[1,1],",\\s*")[[1]])); miss <- setdiff(pk, rownames(available.packages(repos="https://cloud.r-project.org"))); print(miss); stopifnot(length(miss)==0)'` exits 0.
    - Every declared Suggests entry is installed locally:
      `Rscript -e 'pk <- trimws(gsub("\\s*\\(.*\\)","",strsplit(read.dcf("DESCRIPTION","Suggests")[1,1],",\\s*")[[1]])); stopifnot(all(vapply(pk, requireNamespace, logical(1), quietly=TRUE)))'` exits 0.
    - `R CMD check --as-cran` on the built tarball ends with a `Status:` line reporting 0 ERRORs and 0 WARNINGs.
    - `leafblower.Rcheck/00check.log` contains no line reporting a package as not installed.
  </acceptance_criteria>
  <verify>
    <automated>R CMD build . && TARBALL=$(ls -t leafblower_*.tar.gz | head -1) && R CMD check --as-cran "$TARBALL" 2>&1 | tail -40; grep -E "^Status:" leafblower.Rcheck/00check.log; grep -c "is not installed" leafblower.Rcheck/00check.log</automated>
  </verify>
  <reversibility rating="reversible">One field edit in DESCRIPTION; `git checkout DESCRIPTION` restores it, and the check that validates it is re-runnable at will.</reversibility>
  <done>Every package the package declares as Suggests can actually be installed from a mainstream repository, and a Suggests-complete `R CMD check --as-cran` on the tarball is clean on this machine — the local half of SC1/KPI-05, now genuinely representative.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two lines that masked the skip, and prove on real CI that the Suggests tests execute</name>
  <files>.github/workflows/r-check.yml</files>
  <precondition>`gh auth status` succeeds and this clone can push to `origin` (https://github.com/davdittrich/leafblower.git) — the only oracle for this task is a real GitHub Actions run, not a local re-check.</precondition>
  <read_first>
    - `.github/workflows/r-check.yml` in full (40 lines). The two lines to remove are line 12
      inside the job-level `env:` block, and line 24 inside the
      `r-lib/actions/setup-r-dependencies` `with:` block. Read both verbatim before editing so
      the deletion is exact and the surrounding YAML indentation survives.
    - `.planning/phases/05-cran-pypi-release/05-VERIFICATION.md` Key Link table row
      "`setup-r-dependencies` -> Suggests packages actually installed | NOT WIRED AS INTENDED"
      — the mechanism statement this task inverts.
    - `.planning/phases/05-cran-pypi-release/05-CONTEXT.md` D-02 (CI is the verification
      mechanism for this phase) and D-06 (`--as-cran`, 0 errors / 0 warnings, still applies
      unchanged).
  </read_first>
  <action>
    Delete line 12 — the job-level environment variable that downgrades an unavailable
    Suggests package from an error to a skip. With Task 1's DESCRIPTION fix in place there is
    nothing left that cannot be installed, and restoring the default turns any future
    unavailable Suggests package into an immediately red check instead of a silently widened
    skip set. This is the durability requirement of the gap, and it costs zero lines of
    bespoke guard code because R CMD check already implements it.

    Delete line 24 — the `dependencies` key inside the `setup-r-dependencies` `with:` block,
    whose current value restricts installation to non-Suggests dependency classes. Removing
    the key entirely restores the action's default, which resolves and installs the package's
    own Suggests. Do NOT replace it with a hand-written package list in `extra-packages`: such
    a list is a second source of truth beside DESCRIPTION and drifts the moment a Suggests
    entry is added, which is the same class of defect this plan is closing. Leave
    `extra-packages`, `needs` and `install-pandoc` exactly as they are.

    Commit both this change and Task 1's DESCRIPTION change, push to `origin`, and wait for
    the r-check workflow run to complete. Then read the run's logs and confirm two things, in
    this order: (a) the dependency-installation step lists PracTools, survey, arrow and
    DiceKriging among the packages it installed; (b) the check step's test output shows the
    blocks guarded by those packages running rather than skipping. A green conclusion alone is
    not sufficient evidence — a green conclusion is exactly what the masked configuration also
    produced, and mistaking it for proof is the error this whole gap-closure exists to
    correct. Quote the relevant log lines in the SUMMARY.

    If the run fails because a Suggests package has no binary for the runner, do not restore
    either deleted line. Either fix the availability, or, if the package turns out to serve
    only `benchmarks/` (which is `.Rbuildignore`d), remove it from Suggests the same way Task
    1 removed autumn — a declared dependency that nothing shipped uses is a defect in the
    declaration, not a reason to weaken the check.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*#' .github/workflows/r-check.yml | grep -c 'FORCE_SUGGESTS'` returns 0.
    - `grep -v '^\s*#' .github/workflows/r-check.yml | grep -c '"hard"'` returns 0.
    - `grep -c 'install-pandoc: false' .github/workflows/r-check.yml` returns 1 and `grep -c 'needs: check' .github/workflows/r-check.yml` returns 1 — the rest of the `with:` block was not collaterally edited.
    - `Rscript -e 'yaml::yaml.load_file(".github/workflows/r-check.yml")'` parses without error (YAML still valid after the deletions).
    - The GitHub Actions r-check run for the pushed commit concludes `success`, AND its log contains an install line for PracTools, AND its test output contains no line reporting PracTools, survey, arrow or DiceKriging as not installed.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/r-check.yml | grep -c 'FORCE_SUGGESTS\|"hard"' ; Rscript -e 'invisible(yaml::yaml.load_file(".github/workflows/r-check.yml")); cat("yaml ok\n")' && git push origin HEAD && gh run watch "$(gh run list --workflow r-check.yml --limit 1 --json databaseId --jq '.[0].databaseId')" --exit-status && gh run view "$(gh run list --workflow r-check.yml --limit 1 --json databaseId --jq '.[0].databaseId')" --log | grep -c "is not installed"</automated>
  </verify>
  <reversibility rating="reversible">Two line deletions in a workflow file; revertible by `git revert`, and re-validated by re-running the same workflow.</reversibility>
  <done>The CI configuration installs the Suggests it declares, the previously-skipped tests are observed executing in a real run's logs, and an unavailable Suggests package can no longer degrade into a silent skip (leafblower-xfz4's CI half closed).</done>
</task>

<task type="auto">
  <name>Task 3: Restate cran-comments.md against the Suggests-complete result</name>
  <files>cran-comments.md</files>
  <read_first>
    - `cran-comments.md` in full — the current test-environment section and the NOTE
      explanations written in 05-05, including which CI run it cites.
    - `.planning/phases/05-cran-pypi-release/05-VERIFICATION.md` Required Artifacts table row
      for `cran-comments.md` ("Does not (and could not, at the time) disclose the r-universe
      full-Suggests failure").
    - `.planning/phases/05-cran-pypi-release/05-CONTEXT.md` D-04 (what cran-comments.md must
      explain: the deliberate absence of an optimisation flag in the package's own compiler
      flags, and the LAPACK dependency) and D-05 (submission itself is out of scope this
      phase — this file is written for Phase 7 to use, not to submit now).
  </read_first>
  <action>
    Update `cran-comments.md` so its check evidence describes the run that actually happened
    under Task 2's configuration: cite the new GitHub Actions run id and state that the check
    ran with the full Suggests set installed. Keep the existing D-04 explanations (the
    deliberate absence of a package-set optimisation flag, and the LAPACK requirement) and
    re-verify each remaining NOTE is still present and still explained — if Task 1's
    Suggests-complete check produced a new NOTE, explain it here or, if it is not genuinely
    explainable, treat it as a defect and fix the cause instead.

    Add one short paragraph recording that a previous CI configuration produced a green result
    without installing Suggests, that this was corrected, and that the current result is the
    Suggests-complete one — a reviewer comparing this repository's history against the
    r-universe build log will otherwise find an unexplained contradiction, and disclosing it
    costs nothing while concealing it is exactly the kind of claim CRAN reviewers penalise.

    Do not describe Windows here yet: 05-10 is what makes the Windows claim true, and this
    file must not assert a platform that has not yet been proven.
  </action>
  <acceptance_criteria>
    - `cran-comments.md` cites the GitHub Actions run id produced by Task 2 (the id appears verbatim in the file).
    - `grep -ci 'suggests' cran-comments.md` returns at least 1 and the surrounding sentence states the Suggests set was installed for that run.
    - Every NOTE reported by Task 1's local `R CMD check --as-cran` has a matching explanation paragraph in the file (checked by reading the check log's NOTE headings against the file).
    - The file makes no claim about a Windows check result.
  </acceptance_criteria>
  <verify>
    <automated>test -s cran-comments.md && grep -c "$(gh run list --workflow r-check.yml --limit 1 --json databaseId --jq '.[0].databaseId')" cran-comments.md && grep -n "NOTE" leafblower.Rcheck/00check.log</automated>
    <human-check>Read `cran-comments.md` end to end and confirm each NOTE listed in `leafblower.Rcheck/00check.log` has an explanation that a CRAN reviewer would accept, and that the disclosure paragraph about the earlier weakened check reads as a correction rather than as an excuse. This is a judgement about how a human reviewer will read prose; no command can assert it.</human-check>
  </verify>
  <reversibility rating="reversible">A markdown file; freely editable and not shipped in the tarball (`.Rbuildignore` excludes it).</reversibility>
  <done>The document Phase 7 will submit describes the check that CRAN's own machinery will reproduce, including an honest note about the earlier weakened result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> package registry | `setup-r-dependencies` now resolves and installs twelve Suggests packages plus their closure into the runner |
| repository -> CRAN reviewer | `cran-comments.md` is a claim made to a human gatekeeper about check results |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09-01 | Repudiation | green CI status as evidence | high | mitigate | The defect itself: a green run that proved less than it appeared to. Task 2's acceptance criteria require log evidence that the packages installed and the tests executed, not merely a `success` conclusion. |
| T-05-09-02 | Tampering | npm/pip/cargo/CRAN installs | high | mitigate | No NEW package is added to any manifest; the change is a deletion. The twelve remaining Suggests are pre-existing CRAN entries covered by 05-RESEARCH.md's Package Legitimacy Audit, and the `r-lib/actions` steps stay pinned to the existing commit SHA `d3c5be51b12e724e68f33216ca3c148b66d5f0b6`. No legitimacy checkpoint is triggered. |
| T-05-09-03 | Elevation of privilege | workflow `env:` / `with:` edits | medium | accept | Both edits are deletions inside an existing job; no new secret, permission, `run:` step or third-party action is introduced. |
| T-05-09-04 | Denial of service | CI runtime | low | accept | Installing arrow and its closure lengthens each run; binary repositories keep it bounded, and a slower honest check is strictly preferable to a fast masked one. |
</threat_model>

<verification>
- Local: `R CMD check --as-cran` on the built tarball, Suggests complete, 0 errors and 0 warnings, no "not installed" line in `00check.log`.
- CI: the r-check run for the pushed commit concludes `success`, its logs show PracTools installed, and its test output shows no Suggests-attributable skips.
- `cran-comments.md` cites that run and explains every remaining NOTE.
</verification>

<success_criteria>
SC1/KPI-05 becomes durable rather than nominal: the "0 errors, 0 warnings" claim now comes
from a check run under CRAN-representative conditions (D-06), and the configuration that
produced the misleading green result is gone with a built-in guard in its place. The
remaining Phase 5 gap after this plan is SC6's Windows compile failure, which is 05-10.
</success_criteria>

<output>
Create `.planning/phases/05-cran-pypi-release/05-09-SUMMARY.md` when done, quoting the CI log
lines that show the Suggests packages installing and the previously-skipped tests running.
</output>

