---
phase: 05-cran-pypi-release
plan: 08
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - src/design_effect.cpp
  - tests/testthat/test-design-pratools-parity.R
  - tests/testthat/test-design.R
  - python/leafblower/test_design_effect.py
  - R/design_effect.R
  - man/design_effect.Rd
  - NEWS.md
autonomous: true
gap_closure: true
requirements: [US-010, US-008, KPI-05, KPI-06]
beads: [leafblower-xfz4]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tests/testthat/test-design-pratools-parity.R actually EXECUTES (PracTools installed, no skip) and PASSES: design_effect() agrees with an independent PracTools/base-R Eq-3.5 oracle on the seed-2024 uniform-weight fixture at a measured, documented tolerance."
    - "The Henry & Valliant Eq 3.5 invariant deff_H <= deff_K holds on every design_effect() fixture in the suite — the property that is violated today (deff_H = 8.293 with deff_K = 1) and that would have caught this defect without PracTools."
    - "design_effect(w, outcome, data, target) on the empty-target degenerate path still returns exactly deff_K, and R and Python still return byte-identical deff_H for the same input (shared C++ core, US-008/KPI-06 stay green)."
  artifacts:
    - "src/design_effect.cpp — calibration design matrix X carries a leading constant column, so p = 1 + sum(cat_counts[k] - 1)"
    - "tests/testthat/test-design-pratools-parity.R — third-party oracle rebuilt on primitives that are defined at var(w) = 0"
    - "tests/testthat/test-design.R + python/leafblower/test_design_effect.py — closed-form fixtures re-derived for the corrected column space"
    - "NEWS.md — entry recording the corrected deff_H numeric behaviour of an already-published exported function"
  key_links:
    - "R/design_effect.R roxygen (and the regenerated man/design_effect.Rd) describe the X that src/design_effect.cpp actually builds — the doc currently says 'GREG residuals', which is only true once the column space contains the constant vector"
    - "python/leafblower/_design_effect.py and R/design_effect.R both dispatch into the SAME rk_design_effect entry point, so the C++ change propagates to the wheel with no Python-side edit; the Python fixture assertion is the only Python artifact that must move"
    - "src/design_effect.cpp's rank-deficiency fallback (cholesky_factor_inplace failure -> deff_H = deff_K) must keep working once p grows by one, or the collinear-margins edge-case test starts erroring instead of degrading"
---

<objective>
Fix the real defect behind Phase 5's SC1/KPI-05 failure: `design_effect()`'s 4-argument
Henry & Valliant Eq-3.5 path builds its calibration covariate matrix from reference-coded
margin dummies with NO constant column, so its column space omits the constant vector, its
"GREG residuals" are not GREG residuals, and the returned deff_H is wrong by a factor of ~42
on the parity fixture (8.2930209003 returned; 0.1976715311 correct).

Purpose: the r-universe check ERROR that failed SC1 is not a PracTools version quirk and not
a tolerance problem — it is this. Phase 7 (CRAN submission) hits the identical ERROR on
CRAN's own machinery unless the statistic is corrected. Per CLAUDE.md this gets a root-cause
fix, not a loosened tolerance and not a skip.
Output: a corrected C++ core, a parity test that actually runs and passes against an oracle
that is defined at var(w) = 0, a permanent mathematical-invariant regression guard, and the
downstream fixture/doc/NEWS propagation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cran-pypi-release/05-VERIFICATION.md
@.planning/phases/05-cran-pypi-release/05-CONTEXT.md
@CLAUDE.md
@src/design_effect.cpp
@tests/testthat/test-design-pratools-parity.R
</context>

## Artifacts this plan produces

| Artifact | Change |
|----------|--------|
| `src/design_effect.cpp` | Leading constant column in `X`; `p = 1 + sum(cat_counts[k] - 1)`; margin dummies shifted right by one column |
| `tests/testthat/test-design-pratools-parity.R` | Both `test_that` blocks rewritten: oracle 1 rebuilt from `PracTools::deffK` + a WLS fit that includes the constant; oracle 2 (hand-rolled Eq 3.5) gains the constant column; new `deff_H <= deff_K` invariant assertion |
| `tests/testthat/test-design.R` | Perfect-fit closed-form fixture re-derived for the corrected column space |
| `python/leafblower/test_design_effect.py` | Mirror of the same re-derived fixture |
| `R/design_effect.R` + `man/design_effect.Rd` | Roxygen describes the design matrix that is actually built (constant + reference-coded dummies); `man/` regenerated |
| `NEWS.md` | One entry recording the corrected numeric behaviour of an exported function already published as 0.1.0 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED — reproduce the divergence end-to-end (R test -> R wrapper -> C++ core) with PracTools actually installed</name>
  <files>tests/testthat/test-design-pratools-parity.R</files>
  <precondition>PracTools installs from CRAN into the user library on this machine (`install.packages("PracTools")`; it pulls dplyr, geosphere, ggplot2, MASS, usmap). Until it does, the parity test is a silent `skip_if_not_installed` no-op and nothing in this plan can be proven — halt and report if the install fails.</precondition>
  <read_first>
    - `tests/testthat/test-design-pratools-parity.R` in full — both blocks. Note line 9's own comment asserting that under uniform weights "PracTools Eq3.4 second-component vanishes", and lines 41-46 of the second block, which hand-build `X2` with three columns (S, E, W) and no constant.
    - `src/design_effect.cpp` lines 90-202 — specifically line 115 (`p_acc += cat_counts[k] - 1`), lines 142-153 (the "drop first level per margin" X build), lines 185-192 (residual formation), line 200 (the Eq-3.5 quotient).
    - `R/design_effect.R` lines 35-87 — how `data_codes`/`cat_counts` are packed, and the roxygen claim at lines 6-14 that the residuals are GREG residuals.
    - `.planning/phases/05-cran-pypi-release/05-VERIFICATION.md` gap 1 and the Root-Cause Note — the evidence chain showing this assertion has never executed in any environment.
  </read_first>
  <behavior>
    - Fixture (`set.seed(2024L)`, n = 200, one 4-level margin N/S/E/W, `w = rep(1, n)`, `y = 10 + 3*(region=="N") - 2*(region=="S") + rnorm(n)`), current build: `design_effect(w, outcome=y, data=data, target=target)` returns 8.2930209003.
    - `PracTools::deffH(w = w, y = y, x = X)` on that fixture returns NaN, because its Eq-3.4 correction term is `n*sqrt(vw)*(rho.u2w*sqrt(vu) - 2*A*rho.uw*sqrt(vu))/(Nhat*vy)` with both rho terms formed as a covariance divided by `sqrt(vu2*vw)`; at `vw = wtdvar(w,w) = 0` those are 0/0 and the term evaluates as 0*NaN rather than vanishing. This is an upstream removable singularity, present in the current CRAN source, and NOT version-dependent — pinning a PracTools version cannot fix it.
    - The Eq-3.5 limit of that same formula, built from primitives that ARE defined at `vw = 0` — `PracTools::deffK(w) * var_w(u) / var_w(y)` with `u = coef[1] + residuals` from `glm(y ~ X, weights = w)` — returns 0.1976715311 on the same fixture.
    - Therefore the assertion fails on the numbers, not on the NaN: 8.293 vs 0.198 is a 42x divergence, and the divergence direction is diagnostic — deff_H > deff_K is impossible for Eq 3.5 whenever the model space contains the constant vector, since the constant-only model is then nested and weighted SSE <= weighted SST.
  </behavior>
  <action>
    Install PracTools, then rewrite the FIRST `test_that` block of
    `tests/testthat/test-design-pratools-parity.R` so it asserts against an oracle that is
    mathematically defined at `var(w) = 0`, and run it to observe RED before touching any
    C++ (leafblower-xfz4, CLAUDE.md strict-TDD: observe the failing test first).

    Keep `skip_if_not_installed("PracTools")` and keep the existing fixture bit-for-bit
    (`set.seed(2024L)`, n = 200, the same `target`, the same `w`, the same `y`) — the fixture
    is the thing r-universe actually executed, so changing it would discard the live evidence.

    Replace the oracle only. Build `X <- model.matrix(~ region, data = data)[, -1L, drop = FALSE]`
    as today, fit `reg <- glm(y ~ X, weights = w)`, form `u <- unname(reg$coefficients[1L]) + reg$residuals`
    exactly as PracTools does internally, and compute the oracle as
    `PracTools::deffK(w) * wv(u, w) / wv(y, w)` where `wv` is a local weighted variance
    `function(z, wt) { m <- sum(wt*z)/sum(wt); sum(wt*(z-m)^2)/sum(wt) }`. Note in a comment
    that PracTools' own `wtdvar` carries an `n/(n-1)` factor that cancels in this quotient, so
    the local helper and PracTools' helper give the same oracle; `deffK` is taken from
    PracTools so the Kish factor stays third-party, and `glm` supplies a beta-hat computed by
    a QR path fully independent of the package's Cholesky path.

    Add, in the same block, the upstream-singularity tripwire: call
    `d_full <- PracTools::deffH(w = w, y = y, x = X)` and compare it to the leafblower value
    ONLY when `is.finite(d_full)`, with a comment recording that today it is NaN by the 0*NaN
    mechanism above. This keeps the direct third-party call in the suite and makes the test
    self-healing if upstream ever removes the singularity, without asserting on a NaN.

    Do NOT yet touch the second `test_that` block, `src/design_effect.cpp`, or any fixture —
    this task's whole output is a reproducible, quantified failure.

    Record in the SUMMARY the three measured numbers: the value the package returns, the
    value the oracle returns, and the ratio.
  </action>
  <acceptance_criteria>
    - `Rscript -e 'requireNamespace("PracTools")'` succeeds (the test can no longer silently skip).
    - The rewritten first block FAILS when run against the current build, and the failure
      message contains both numbers, not a NaN comparison error.
    - `grep -c 'deffK' tests/testthat/test-design-pratools-parity.R` returns at least 1
      (the oracle still routes through PracTools, it was not replaced by pure base R).
    - `grep -c 'is.finite' tests/testthat/test-design-pratools-parity.R` returns at least 1
      (the deffH tripwire is present and conditional).
  </acceptance_criteria>
  <verify>
    <automated>Rscript -e 'if (!requireNamespace("PracTools", quietly=TRUE)) install.packages("PracTools", repos="https://cloud.r-project.org")' && R CMD INSTALL --preclean . && Rscript -e 'library(leafblower); library(testthat); res <- as.data.frame(test_file("tests/testthat/test-design-pratools-parity.R", reporter="silent")); cat("failed:", sum(res$failed), "errors:", sum(res$error), "skipped:", sum(res$skipped), "\n"); stopifnot(sum(res$skipped) == 0, sum(res$failed) + sum(res$error) > 0)'</automated>
  </verify>
  <reversibility rating="reversible">A single test file edit plus a CRAN package install; `git checkout` reverts it entirely.</reversibility>
  <done>The parity test executes instead of skipping, and fails with the two concrete numbers on the record — the defect is reproduced locally, in this repository, without needing r-universe's build farm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — give the calibration design matrix a constant column so the residuals are GREG residuals</name>
  <files>src/design_effect.cpp</files>
  <read_first>
    - `src/design_effect.cpp` lines 1-100 FIRST — in particular however the `K <= 0` /
      empty-target entry is routed, because `design_effect(w, outcome, data, target = list())`
      must keep returning exactly `deff_K` (pinned by `tests/testthat/test-design.R` and by
      `python/leafblower/test_design_effect.py::test_design_effect_4arg_empty_target_returns_deff_K`).
    - `src/design_effect.cpp` lines 100-202 — the `p_acc` accumulation, the INT_MAX and
      `size_t` overflow guards that consume `p`, the column-major X fill, the lower-triangle
      `XtWX` accumulation, `cholesky_factor_inplace` + its rank-deficient fallback, and
      `weighted_var`.
    - `src/calib_linalg.hpp` — `cholesky_factor_inplace` / `cholesky_solve` signatures and the
      Gill-Murray-Wright pre-perturbation, to confirm a wider `p` needs no call-site change.
  </read_first>
  <behavior>
    - Seed-2024 parity fixture: `design_effect()` returns 0.1976715311 (was 8.2930209003), matching the Task-1 oracle.
    - Empty target (`K = 0`): returns exactly `deff_K`, unchanged.
    - Perfect-fit fixture (3 levels A/B/C, 20 obs each, `y` equal to the group mean, `w = 1`): residuals are identically zero once the constant is in the column space, so deff_H is 0 (was 1/3).
    - Two perfectly collinear margins: still finite and still positive, still routed through the Gill-Murray-Wright perturbation rather than the rank-deficient early return.
    - Invariant, all fixtures: deff_H <= deff_K.
  </behavior>
  <action>
    In the `X`-construction region of `src/design_effect.cpp` (currently lines 102-153), make
    the design matrix span the constant vector, which is what turns `u` into an actual GREG
    residual and is required by Henry & Valliant Eq 3.5:

    1. Seed the column accumulator with the constant column: `std::size_t p_acc = 1;` before
       the `cat_counts` loop, leaving the loop body's `p_acc += cat_counts[k] - 1;` untouched.
       The existing `cat_counts[k] < 2`, `cat_counts[k] > n`, INT_MAX and `size_t`-overflow
       guards all keep operating on the new, larger `p` with no edit.
    2. Fill column 0 of `X` with 1.0 for all `n` rows.
    3. Start the per-margin column offset at 1 instead of 0, so margin `k`'s dummy for
       category `c >= 1` lands at column `col_off + c - 1` with `col_off` initialised to 1 and
       advanced by `cat_counts[k] - 1` per margin exactly as today.
    4. Rewrite the block comment above the fill to state that X is a constant column followed
       by reference-coded dummies (first level dropped per margin), and state WHY: the
       calibration constraints always include the population size, so the constant belongs to
       the calibration column space; dropping one level per margin without a constant removes
       that dimension, leaves the reference cell's residual equal to its raw outcome, and
       inflates sigma^2_u above sigma^2_y. Cite Henry & Valliant (2015) Eq 3.5 and ticket
       leafblower-xfz4 in the comment.

    Change nothing else — not `weighted_var`, not the `sigma2_y < eps` guard, not the
    rank-deficient fallback, not the Cholesky calls. This is a one-dimension widening of the
    column space, and every downstream loop is already written in terms of `p`.

    Add the permanent invariant guard to `tests/testthat/test-design.R`: a `test_that` block
    that, for each of the parity fixture and the RVAL.4 n=20/K=2 fixture, asserts the 4-arg
    result does not exceed the 1-arg result by more than 1e-12 relative. Derive it in the
    comment from nesting: with the constant in the column space the intercept-only model is
    nested in the calibration model, so weighted SSE <= weighted SST, hence
    sigma^2_u <= sigma^2_y and deff_H <= deff_K. This is the guard that would have caught
    leafblower-xfz4 with no third-party package at all.

    Then rebuild via the project gate and run the full R suite; the perfect-fit fixture at
    `tests/testthat/test-design.R` and its Python mirror are EXPECTED to fail here — Task 3
    re-derives them. Do not edit them in this task, and do not touch the C++ again to make
    them pass.
  </action>
  <acceptance_criteria>
    - `R CMD INSTALL --preclean .` exits 0.
    - `tests/testthat/test-design-pratools-parity.R` runs with 0 skips, 0 failures, 0 errors.
    - `Rscript -e 'library(leafblower); set.seed(2024L); n <- 200L; d <- data.frame(region=sample(c("N","S","E","W"), n, TRUE)); w <- rep(1,n); y <- 10+3*(d$region=="N")-2*(d$region=="S")+rnorm(n); v <- design_effect(w, outcome=y, data=d, target=list(region=c(N=.25,S=.25,E=.25,W=.25))); stopifnot(abs(v - 0.1976715311) < 1e-8)'` exits 0.
    - `grep -v '^\s*//' src/design_effect.cpp | grep -c 'p_acc = 1'` returns 1.
    - The empty-target equivalence and the collinear-margins edge case still pass:
      `tests/testthat/test-design-edge-cases.R` and `tests/testthat/test-cr-d14-design-effect.R` report 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>R CMD INSTALL --preclean . && OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 Rscript -e 'library(leafblower); library(testthat); for (f in c("tests/testthat/test-design-pratools-parity.R","tests/testthat/test-design-edge-cases.R","tests/testthat/test-cr-d14-design-effect.R")) { r <- as.data.frame(test_file(f, reporter="silent")); cat(f, "failed:", sum(r$failed), "errors:", sum(r$error), "skipped:", sum(r$skipped), "\n"); stopifnot(sum(r$failed) + sum(r$error) == 0) }'</automated>
  </verify>
  <reversibility rating="costly">Changes the numeric output of an exported function already published as 0.1.0 on PyPI and built on r-universe. The diff itself is trivially revertible, but any consumer who recorded a 4-arg deff_H sees a different number after this ships — which is the point (the old number was wrong), and is why Task 3 records it in NEWS.md rather than shipping it silently.</reversibility>
  <done>`design_effect()`'s 4-argument path returns the Henry & Valliant Eq-3.5 value computed from genuine GREG residuals, it agrees with the independent PracTools/glm oracle, and the deff_H <= deff_K invariant is pinned in the suite forever.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate — re-derive the closed-form fixtures, the hand-rolled oracle, the roxygen and NEWS</name>
  <files>tests/testthat/test-design.R, tests/testthat/test-design-pratools-parity.R, python/leafblower/test_design_effect.py, R/design_effect.R, man/design_effect.Rd, NEWS.md</files>
  <read_first>
    - `tests/testthat/test-design.R` lines 59-80 — the empty-target equivalence and the
      perfect-fit block whose comment derives beta_hat = [20, 30] and deff_H = 1/3 from an X
      with no constant column.
    - `tests/testthat/test-design-pratools-parity.R` second block, lines 25-66 — the
      hand-rolled Eq-3.5 oracle, its `X2` construction and its documented 1e-6 tolerance
      rationale (R vs C++ accumulation order, ~4e-8 relative at n = 200).
    - `python/leafblower/test_design_effect.py::test_design_effect_4arg_r_parity` — the Python
      mirror of the same closed-form fixture, including its comment about the dropped level's
      residual.
    - `R/design_effect.R` lines 1-34 — the roxygen block describing the Eq-3.5 path.
    - `NEWS.md` — existing entry format and the current version heading.
  </read_first>
  <action>
    Bring every downstream artifact into agreement with the corrected column space.

    1. `tests/testthat/test-design-pratools-parity.R`, second block: add a leading column of
       1.0 to the hand-rolled `X2` so the oracle reproduces what the core now computes, and
       update the derivation comment accordingly. Then MEASURE the relative difference between
       the package value and the hand-rolled value on that fixture, put the measured number in
       the comment, and set `tolerance` to the next power of ten above it (do not exceed 1e-6,
       and do not raise it above what the measurement justifies — an evidence-free tolerance
       is exactly the failure mode this plan exists to remove).
    2. `tests/testthat/test-design.R`, perfect-fit block: with the constant in the column
       space the calibration model reproduces every group mean exactly, so all residuals are
       zero and the Eq-3.5 quotient is 0. Replace the 1/3 expectation with
       `expect_lt(abs(d), 1e-12)` — an equality-to-zero assertion needs an absolute bound, not
       a relative `tolerance` — and rewrite the derivation comment to show beta_hat, the zero
       residual vector, and sigma^2_u = 0 for the corrected X.
    3. `python/leafblower/test_design_effect.py::test_design_effect_4arg_r_parity`: mirror the
       same re-derivation and the same absolute bound, and rewrite the comment that currently
       explains the dropped level's residual as the raw outcome.
    4. `R/design_effect.R` roxygen: state that X is a constant column followed by
       reference-coded margin dummies (first level per margin dropped), which is what makes
       u_i a GREG residual and what bounds deff_H by deff_K. Regenerate `man/design_effect.Rd`
       with roxygen2 — do not hand-edit the `.Rd`.
    5. `NEWS.md`: one entry under the current version recording that the 4-argument
       `design_effect()` returned an inflated deff_H because its design matrix omitted the
       constant column, that the corrected value on the documented fixture moves from
       8.2930209003 to 0.1976715311, and that any recorded 4-argument value from 0.1.0 is
       superseded. Reference leafblower-xfz4.
    6. Re-run the full Definition of Done: R build gate, R suite, and the Python parity suite
       under single-thread BLAS. The Python side needs a reinstall so the wheel picks up the
       new core before `test_design_effect_parity.py` compares R against Python.
  </action>
  <acceptance_criteria>
    - Full R suite: 0 FAIL, and `tests/testthat/test-design-pratools-parity.R` reports 0 skipped.
    - Full Python suite under `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1`: 0 failures, including `test_design_effect_parity.py` (R and Python still agree on the same input).
    - `grep -c 'intercept\|constant column' R/design_effect.R` returns at least 1, and `man/design_effect.Rd` contains the same statement (regenerated, not hand-edited: `git diff --stat man/design_effect.Rd` shows only roxygen-shaped changes).
    - `grep -c 'xfz4' NEWS.md` returns 1.
    - `grep -c '8.2930209003' NEWS.md` returns 1 (the superseded value is on the record, not quietly dropped).
  </acceptance_criteria>
  <verify>
    <automated>R CMD INSTALL --preclean . && Rscript -e 'devtools::test()' 2>&1 | tail -20 && cd python && uv pip install -e . --reinstall-package leafblower && OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest -q 2>&1 | tail -15</automated>
    <human-check>Read the rewritten derivation comments in `tests/testthat/test-design.R` and the second block of `tests/testthat/test-design-pratools-parity.R` and confirm the algebra is spelled out (beta_hat, residual vector, sigma^2_u, sigma^2_y) rather than asserted — these comments are the only place a future reader can check that the new expected values were derived and not fitted to whatever the code emitted.</human-check>
  </verify>
  <reversibility rating="reversible">Test expectations, roxygen and a NEWS line; all revert with `git checkout` and are re-validated by the same suite run.</reversibility>
  <done>Every fixture, doc string and release note in the repository states the same thing the C++ core computes; R and Python still agree byte-for-byte; the full Definition of Done is green with PracTools installed and executing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published artifact -> downstream analyst | `design_effect()` is exported and already published (PyPI 0.1.0, r-universe); its numeric output crosses into third-party survey estimates |
| CRAN Suggests -> local dev machine | `install.packages("PracTools")` pulls a CRAN package and its dependency closure onto the build host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-01 | Tampering | `design_effect()` 4-arg numeric output | high | mitigate | The defect itself: a silently wrong published statistic. Fixed at the root (Task 2), pinned by an independent third-party oracle and by the deff_H <= deff_K invariant, and disclosed in NEWS.md with the superseded value (Task 3) rather than changed silently. |
| T-05-08-02 | Repudiation | test suite evidence | medium | mitigate | The suite previously reported "0 FAIL" while the only PracTools assertion was skipped. Task 1's verify asserts `sum(skipped) == 0` for that file, so a future skip cannot masquerade as a pass. |
| T-05-08-03 | Tampering | npm/pip/cargo/CRAN installs | high | mitigate | Only `PracTools`, an already-declared DESCRIPTION Suggests entry covered by 05-RESEARCH.md's Package Legitimacy Audit, is installed; no NEW package is added to any manifest by this plan, so no legitimacy checkpoint is triggered. |
| T-05-08-04 | Denial of service | `p` widening in `src/design_effect.cpp` | low | accept | `p` grows by exactly 1; the existing `cat_counts[k] > n`, INT_MAX and `size_t`-overflow guards already bound it, and the allocation is `n*p` doubles. |
</threat_model>

<verification>
- `R CMD INSTALL --preclean .` exits 0.
- `Rscript -e 'devtools::test()'`: 0 FAIL, and the PracTools parity file reports 0 skipped.
- `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 .venv/bin/python -m pytest`: 0 failures.
- The seed-2024 fixture returns 0.1976715311 (+/- 1e-8), agreeing with the PracTools/glm oracle.
- deff_H <= deff_K on every design_effect fixture in the suite.
</verification>

<success_criteria>
The `R CMD check` ERROR that r-universe reported on all 8 Linux/macOS configs
(build 32069801748, commit 8920cf22) cannot recur, because the assertion that produced it now
passes against a correct statistic — verified locally with PracTools installed rather than
skipped. SC1/KPI-05's remaining blocker moves from "a real numeric defect" to "CI must
actually install the Suggests", which is 05-09.
</success_criteria>

<output>
Create `.planning/phases/05-cran-pypi-release/05-08-SUMMARY.md` when done, recording the
three measured numbers from Task 1 and the measured tolerance from Task 3.
</output>
