
# Added by code-review-graph
.beads-credential-key
# Beads / Dolt files (added by bd init)
*.db
*.dll
.dolt/
# Git worktrees
# Local dev-tooling / agent artifacts (not part of the package)
.wolf/
.beads/
.claude/
.metaswarm/
.tldr/
.tldrignore
.gemini/
.gemini-bridge/
GEMINI.md
.mcp.json
AGENTS.md
.coverage-thresholds.json
tasks/
# Graphify cache (large, auto-regenerated)
graphify-out/
*.o
# Python build artifacts
python/leafblower/*.pyd
python/leafblower/*.so
# R build artifacts
*.Rcheck/
..Rcheck/
# R CMD check output
*.so
src/leafblower.so
src/*.o
# Python bytecode
**/__pycache__/
*.pyc
# Stray local build outputs
/Cholesky
benchmarks/results/sor_sweep.csv
# Test failure tracking
tests/testthat/_problems/
tests/testthat/testthat-problems.rds
.wolf/backups/
.worktrees/

# Parity bench weight feathers (large, regen via parity_bench.{R,py})
benchmarks/results/parity/weights_*/

# Single-thread parity bench weight feathers (large, regen via parity_bench_st_*.{R,py})
benchmarks/2apm/weights_R_st/
benchmarks/2apm/weights_Py_st/


# 2apm T3 weight feathers (regen via winner_bench.{R,py})
benchmarks/2apm/weights_R/
benchmarks/2apm/weights_Py/
benchmarks/2apm/no_winner.toon

# Benchmark study (leafblower-2ouc) RUN OUTPUTS -- regenerated by run_arm.{R,py};
# NOT part of the frozen runnable tree. (spec/instance_family/ + env/ ARE committed.)
benchmarks/study/rehearsal/
benchmarks/study/results/

