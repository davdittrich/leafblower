# ALM ieppa_soft Epic E — R API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wire the `capacity_penalty` parameter into `R/harvest.R`, fix the `test-safety.R` B7 arity, and write the T3-strengthened + T4–T10 ALM correctness test suite in `test-calibration-solvers.R`.

**Architecture:** Epic E is purely the R-layer and test layer — no C++ changes. It consumes the 32-arg `.Call` ABI stabilized by Epics B (ABI), C (bridges), and D (solver), then adds R-side validation, Roxygen documentation, a `.Call` arity fix for the B7 safety test, and the TDD suite described in the spec.

**Tech Stack:** R, roxygen2, devtools, testthat 3, `bench` (T8 only, skipped on CI without fixture)

---

## File Map

| File | Action |
|------|--------|
| `R/harvest.R` | Add `capacity_penalty = NULL` param; validation block; warn for non-ieppa_soft; insert at `.Call` slot 9 |
| `man/harvest.Rd` | Auto-regenerated by `devtools::document()` — do not edit by hand |
| `tests/testthat/test-safety.R` | Insert `NULL` at slot 9 in the B7 `.Call` test to restore 32-arg arity |
| `tests/testthat/test-calibration-solvers.R` | Replace T3 (strict `<`); append T4–T10 tests |

---

## Task 1: T9 — `R/harvest.R` — add `capacity_penalty` param + Roxygen

**Files:**
- Modify: `R/harvest.R`
- Modify: `man/harvest.Rd` (auto-generated — verify only)

### Step 1: Read current state

Read `R/harvest.R` to confirm the exact signature line (`min_weight = 0,`) and the `.Call` block (lines 245–280). **Evidence required before editing.**

- [ ] Confirm `min_weight = 0,` is the last named param before `...` starts (line 162 in the current file, confirmed by reading above).
- [ ] Confirm `.Call("C_rk_calibrate", ...)` spans lines 245–280 with `sw_vec` at position 8 and `as.double(if (conv$absolute_tol > 0) ...` at position 9.

Current slot-8 argument (position 8 in the .Call):
```r
               sw_vec,
```
Current slot-9 argument (legacy tol_abs — shifts to slot 10 after insertion):
```r
               as.double(if (conv$absolute_tol > 0) conv$absolute_tol else 1e-6),  # slot 9: legacy tol_abs
```

### Step 2: Add `capacity_penalty = NULL` to function signature

Insert the new param into the function signature. The insertion point is **after** `min_weight = 0,` and before `method = "ieppa",`. Look at the actual file: `min_weight` is at line 129, `method` is at line 131.

- [ ] Open `R/harvest.R` and apply the following Edit:

```
old:
  min_weight       = 0,
  max_weight       = 5,
  method           = "ieppa",

new:
  min_weight       = 0,
  max_weight       = 5,
  capacity_penalty = NULL,
  method           = "ieppa",
```

### Step 3: Add validation block after existing param checks

The existing "Not-in-v1 hard stops" block ends at line 170 (`if (!is.null(collapse_vars))`). Insert validation immediately after the block closes (before `target <- parse_target(...)`).

- [ ] Apply the following Edit (insert after the collapse_vars stop):

```r
  if (!is.null(capacity_penalty)) {
    if (!is.numeric(capacity_penalty) || length(capacity_penalty) != 1L ||
        !is.finite(capacity_penalty) || capacity_penalty <= 0) {
      stop("capacity_penalty must be NULL (auto) or a positive finite scalar; got: ",
           deparse(capacity_penalty))
    }
    if (capacity_penalty > 1e15) {
      stop("capacity_penalty must be NULL (auto) or a positive finite scalar; got: ",
           deparse(capacity_penalty))
    }
    if (capacity_penalty < 1e-15) {
      warning("capacity_penalty=", capacity_penalty,
              " is below recommended range; constraint enforcement may be ineffective",
              call.=FALSE)
    }
  }
```

Exact insertion point (after line 170 `stop("collapse_vars is not supported in leafblower v1.")`):

```
old:
  if (!is.null(collapse_vars))
    stop("collapse_vars is not supported in leafblower v1.")

  target  <- parse_target(target, target_map)

new:
  if (!is.null(collapse_vars))
    stop("collapse_vars is not supported in leafblower v1.")

  if (!is.null(capacity_penalty)) {
    if (!is.numeric(capacity_penalty) || length(capacity_penalty) != 1L ||
        !is.finite(capacity_penalty) || capacity_penalty <= 0) {
      stop("capacity_penalty must be NULL (auto) or a positive finite scalar; got: ",
           deparse(capacity_penalty))
    }
    if (capacity_penalty > 1e15) {
      stop("capacity_penalty must be NULL (auto) or a positive finite scalar; got: ",
           deparse(capacity_penalty))
    }
    if (capacity_penalty < 1e-15) {
      warning("capacity_penalty=", capacity_penalty,
              " is below recommended range; constraint enforcement may be ineffective",
              call.=FALSE)
    }
  }

  target  <- parse_target(target, target_map)
```

### Step 4: Add warning for non-ieppa_soft usage

Insert after the `accelerate_bool` assignment (line 208 in current file):

```
old:
  accelerate_bool <- isTRUE(accelerate) && method == "raking"

  # design_weights: used as start_weights when supplied

new:
  accelerate_bool <- isTRUE(accelerate) && method == "raking"

  if (!is.null(capacity_penalty) && !grepl("ieppa_soft", method)) {
    warning("capacity_penalty is only used by method='ieppa_soft'; ignored for method='",
            method, "'", call.=FALSE)
  }

  # design_weights: used as start_weights when supplied
```

### Step 5: Insert `capacity_penalty` at slot 9 in `.Call`

The current `.Call` has `sw_vec` (slot 8) immediately followed by `as.double(if (conv$absolute_tol > 0) ...)` (slot 9 — legacy tol_abs). Insert the new slot between them.

- [ ] Apply Edit to `R/harvest.R`:

```
old:
               sw_vec,
               as.double(if (conv$absolute_tol > 0) conv$absolute_tol else 1e-6),  # slot 9: legacy tol_abs

new:
               sw_vec,
               if (is.null(capacity_penalty)) NULL else as.double(capacity_penalty),  # 9: capacity_penalty
               as.double(if (conv$absolute_tol > 0) conv$absolute_tol else 1e-6),  # 10: legacy tol_abs
```

### Step 6: Update `@param method` Roxygen to include `ieppa_soft`

The current `@param method` line (lines 7–12 in harvest.R) ends with `\code{"grake"}`. Insert `ieppa_soft` entry into the enumeration. The spec gives the exact phrasing:

- [ ] Edit the `@param method` block:

```
old:
#' @param method Calibration method. One of \code{"auto"} (default: iEPPA or
#'   raking based on M_cell/n ratio), \code{"ieppa"} (paper-faithful iEPPA),
#'   \code{"raking"} (IPF + water-filling box projection (KL projection, Csiszar-Tusnady 1984)), \code{"lbfgsb"}
#'   (L-BFGS-B on concave dual), \code{"sinkhorn"} (KL Bregman Dykstra),
#'   \code{"greg"} (Newton QP, Deville-Sarndal 1992), \code{"chebyshev"}
#'   (L-infinity LP via IPM), \code{"grake"} (normalized Chebyshev via IPM).

new:
#' @param method Calibration method. One of \code{"auto"} (default: iEPPA or
#'   raking based on M_cell/n ratio), \code{"ieppa"} (paper-faithful iEPPA),
#'   \code{"ieppa_soft"} (iEPPA with augmented Lagrangian soft capacity enforcement;
#'   better than \code{"ieppa"} on tight-bounds problems where cells hit
#'   \code{max_weight}),
#'   \code{"raking"} (IPF + water-filling box projection (KL projection, Csiszar-Tusnady 1984)), \code{"lbfgsb"}
#'   (L-BFGS-B on concave dual), \code{"sinkhorn"} (KL Bregman Dykstra),
#'   \code{"greg"} (Newton QP, Deville-Sarndal 1992), \code{"chebyshev"}
#'   (L-infinity LP via IPM), \code{"grake"} (normalized Chebyshev via IPM).
```

### Step 7: Add `@param capacity_penalty` Roxygen block

Insert after the existing `@param min_weight` line (which is at line 5, `#' @param min_weight Lower bound...`):

- [ ] Edit `R/harvest.R`, insert after `#' @param min_weight Lower bound on weights. Default 0 (no lower bound).`:

```r
#' @param capacity_penalty Numeric, controls how strongly capacity bounds are
#'   enforced during ALM optimization in \code{method="ieppa_soft"}. Use
#'   \code{NULL} (default) for auto-computed value (M_cell / n). Larger values
#'   force tighter constraint adherence; smaller values allow more temporary
#'   violation before the final projection enforces exact bounds. Tuning: if
#'   \code{attr(result, "result")$alm_capacity_mu_final / capacity_penalty >= 1000},
#'   the adaptive schedule hit its ceiling — increase \code{capacity_penalty} by 10x.
#'   Ignored for methods other than \code{"ieppa_soft"}.
```

### Step 8: Add `@details` block for ieppa vs ieppa_soft guidance

Insert a `@details` block before the existing `@return` (which starts at line 89). If a `@details` block already exists, merge. Add:

- [ ] Edit `R/harvest.R`, insert before `#' @return data frame with weights column...`:

```r
#' @details
#' \strong{When to use \code{ieppa_soft} vs \code{ieppa}}: Use
#' \code{method="ieppa_soft"} when \code{ieppa} gives \code{max_error > 1e-3}
#' and many observations are near \code{max_weight}. \code{ieppa_soft} finds
#' the constrained optimum by temporarily relaxing bounds during optimization
#' then projecting to exact feasibility at exit. Roughly 10-30\% slower than
#' \code{ieppa} due to ALM bookkeeping.
```

### Step 9: Add 4 ALM diagnostic fields to `@return` itemize

The `@return` block currently closes the `\itemize{...}` after `\code{convergence_reason}`. Append 4 new items inside the `\itemize`:

- [ ] Edit `R/harvest.R`, inside the `\itemize{` block in `@return`, after the last existing `\item`:

```r
#'         \item \code{alm_capacity_mu_final}: final ALM penalty after adaptive scaling (0 if not ieppa_soft).
#'         \item \code{alm_n_growth_events}: adaptive growth fire count (high values suggest increasing capacity_penalty).
#'         \item \code{alm_max_dual_norm}: max absolute Lagrange dual at exit.
#'         \item \code{alm_sum_drift}: |sum(weights) - n| after final projection (bounded by 1e-6*n).
```

### Step 10: Regenerate `man/harvest.Rd`

- [ ] Run:
```bash
Rscript -e "devtools::document()"
```
Expected output: `Writing man/harvest.Rd` (no errors).

### Step 11: Verify harvest.Rd content counts

- [ ] Run:
```bash
grep -c "capacity_penalty" man/harvest.Rd
```
Expected: `>= 3`

- [ ] Run:
```bash
grep -c "ieppa_soft" man/harvest.Rd
```
Expected: `>= 4`

### Step 12: Verify FAIL count (RED state for new tests)

The new tests in Task 3 are not yet written. The existing test baseline should still hold. Run:

- [ ] Run:
```bash
Rscript -e "devtools::test()" 2>&1 | tail -3
```
Expected: FAIL count exactly 3 (pre-existing failures, not new ones from this task).

If FAIL count increases, the `capacity_penalty` param or its validation is broken — debug before committing.

### Step 13: Commit

- [ ] Run:
```bash
git add R/harvest.R man/harvest.Rd
git commit -m "feat(harvest): add capacity_penalty param; Roxygen ieppa_soft docs; warn on non-ieppa_soft use"
```

---

## Task 2: T12 — `test-safety.R` B7 arity fix

**Files:**
- Modify: `tests/testthat/test-safety.R`

### Step 1: Locate the B7 test

The B7 test is `test_that("B7: C_rk_calibrate start_weights length mismatch produces error", ...)` at line 20 of `tests/testthat/test-safety.R`. The `.Call` currently passes 31 positional arguments ending with `as.integer(0L)  # 31: accelerate_bool`.

The current slot-8 is `bad_sw` (labeled `# 8: start_weights`). After Epic E's slot-9 insertion, slot 9 must be `NULL` (capacity_penalty = auto), which shifts `as.double(1e-6)` (legacy tol_abs) to slot 10 — matching the new ABI. Without this fix, the `.Call` passes 31 args to a 32-arg registration and R errors with "Incorrect number of arguments to .Call".

### Step 2: Insert `NULL` at slot 9

- [ ] Edit `tests/testthat/test-safety.R`:

```
old:
          bad_sw,                      # 8:  start_weights (wrong length — the probe)
          as.double(1e-6),             # 9:  tol_abs (legacy slot)

new:
          bad_sw,                      # 8:  start_weights (wrong length — the probe)
          NULL,                        # 9:  capacity_penalty (NULL = auto, not tested here)
          as.double(1e-6),             # 10: tol_abs (legacy slot)
```

Also update the trailing comment on line 62 (currently `# 31: accelerate_bool`) to `# 32: accelerate_bool`:

```
old:
          as.integer(0L)               # 31: accelerate_bool

new:
          as.integer(0L)               # 32: accelerate_bool
```

### Step 3: Run safety tests — must all PASS

- [ ] Run:
```bash
Rscript -e "devtools::test(filter='safety')" 2>&1 | tail -5
```
Expected: all tests PASS (no new FAILs; the B7 test in particular must PASS — before fix it would error on arity mismatch).

### Step 4: Commit

- [ ] Run:
```bash
git add tests/testthat/test-safety.R
git commit -m "fix(test-safety): update B7 .Call to 32 args (capacity_penalty=NULL at slot 9)"
```

---

## Task 3: T11 — `test-calibration-solvers.R` — T3 strict + T4–T10

**Files:**
- Modify: `tests/testthat/test-calibration-solvers.R`

### Step 1: Locate the current T3 test

The current T3 test is at lines 388–402 of `tests/testthat/test-calibration-solvers.R`:

```r
test_that("T3: ieppa_soft max_err strictly < ieppa max_err on tight bounds", {
  ...
  expect_true(me_soft <= me_hard + 1e-9,
              label="ieppa_soft max_err not worse than ieppa (no regression)")
})
```

This is the weakened version (merely `<=`). The spec requires strict `<` with a `1e-6` gap.

### Step 2: Replace T3 with the strict version

- [ ] Edit `tests/testthat/test-calibration-solvers.R`:

```
old:
test_that("T3: ieppa_soft max_err strictly < ieppa max_err on tight bounds", {
  set.seed(3L); n <- 5000L
  df  <- data.frame(v1 = factor(sample(5L, n, TRUE)))
  tgt <- list(v1 = setNames(c(0.4, 0.3, 0.15, 0.1, 0.05), as.character(1:5)))
  r_hard <- leafblower::harvest(df, tgt, method="ieppa",
    max_weight=1.8, min_weight=0, max_iterations=500L, attach_weights=FALSE)
  r_soft <- leafblower::harvest(df, tgt, method="ieppa_soft",
    max_weight=1.8, min_weight=0, max_iterations=500L, attach_weights=FALSE)
  me_hard <- attr(r_hard, "result")$max_error
  me_soft <- attr(r_soft, "result")$max_error
  expect_true(me_soft <= me_hard + 1e-9,
              label="ieppa_soft max_err not worse than ieppa (no regression)")
})

new:
test_that("T3: ieppa_soft max_err strictly < ieppa max_err on tight bounds", {
  set.seed(3L); n <- 5000L
  df  <- data.frame(v1 = factor(sample(5L, n, TRUE)))
  tgt <- list(v1 = setNames(c(0.4, 0.3, 0.15, 0.1, 0.05), as.character(1:5)))
  r_hard <- leafblower::harvest(df, tgt, method="ieppa",
    max_weight=1.8, min_weight=0, max_iterations=500L, attach_weights=FALSE)
  r_soft <- leafblower::harvest(df, tgt, method="ieppa_soft",
    max_weight=1.8, min_weight=0, max_iterations=500L, attach_weights=FALSE)
  me_hard <- attr(r_hard, "result")$max_error
  me_soft <- attr(r_soft, "result")$max_error
  expect_lt(me_soft, me_hard - 1e-6,
    label = sprintf("ieppa_soft must beat ieppa by >=1e-6: hard=%.6e, soft=%.6e",
                    me_hard, me_soft))
})
```

### Step 3: Append T4–T10 tests verbatim from spec

- [ ] Append the following block to the end of `tests/testthat/test-calibration-solvers.R`. The tests below are copied verbatim from spec §TDD with only `harvest(` prefixed as `leafblower::harvest(` for consistency with the test file's existing style (the file uses `leafblower::harvest` throughout):

```r
# ── T4: capacity_penalty parameter routing and validation ─────────────────────

test_that("ieppa_soft: capacity_penalty=NULL routes to auto-computed value", {
  set.seed(4L); n <- 1000L
  df <- data.frame(v = factor(sample(letters[1:3], n, TRUE)))
  tgt <- list(v = c(a = 0.4, b = 0.4, c = 0.2))
  r1 <- leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = NULL,
                            attach_weights = FALSE)
  r2 <- leafblower::harvest(df, tgt, method = "ieppa_soft", attach_weights = FALSE)
  expect_equal(as.numeric(r1), as.numeric(r2), tolerance = 1e-12)
  cm <- attr(r1, "result")$alm_capacity_mu_final
  expect_true(is.finite(cm) && cm > 0)
})

test_that("ieppa_soft: capacity_penalty rejects invalid input", {
  df  <- data.frame(v = factor(c("a", "a", "b")))
  tgt <- list(v = c(a = 0.5, b = 0.5))
  expect_error(leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = -1),
               "positive finite scalar")
  expect_error(leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = 0),
               "positive finite scalar")
  expect_error(leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = Inf),
               "positive finite scalar")
  expect_error(leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = NaN),
               "positive finite scalar")
  expect_error(leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = c(1, 2)),
               "positive finite scalar")
  expect_error(leafblower::harvest(df, tgt, method = "ieppa_soft", capacity_penalty = "0.5"),
               "positive finite scalar")
})

# ── T5: final projection — exact bounds + sum drift ───────────────────────────

test_that("ieppa_soft: final weights respect bounds exactly via hard projection", {
  set.seed(5L); n <- 5000L
  df  <- data.frame(v = factor(sample(5L, n, TRUE)))
  tgt <- list(v = setNames(c(0.4, 0.3, 0.15, 0.1, 0.05), as.character(1:5)))
  r <- leafblower::harvest(df, tgt, method = "ieppa_soft",
                           max_weight = 1.8, min_weight = 0.1,
                           max_iterations = 300L, attach_weights = FALSE)
  w <- as.numeric(r)
  expect_true(max(w) <= 1.8)
  expect_true(min(w) >= 0.1)
  # Weights must not be identical to ieppa hard-clamp (would mean ALM didn't run):
  r_hard <- leafblower::harvest(df, tgt, method = "ieppa",
                                max_weight = 1.8, min_weight = 0.1,
                                max_iterations = 300L, attach_weights = FALSE)
  expect_false(isTRUE(all.equal(as.numeric(r), as.numeric(r_hard), tolerance = 1e-10)),
               label = "ieppa_soft weights must differ from ieppa hard-clamp (T3 escaped local min)")
})

test_that("ieppa_soft: degenerate asymmetric bounds — final projection bounded sum drift", {
  set.seed(15L); n <- 2000L
  df  <- data.frame(v = factor(sample(c("a", "b"), n, TRUE, prob = c(.95, .05))))
  tgt <- list(v = c(a = 0.3, b = 0.7))
  r <- leafblower::harvest(df, tgt, method = "ieppa_soft",
                           max_weight = 8.0, min_weight = 0.01,
                           max_iterations = 200L, attach_weights = FALSE)
  w <- as.numeric(r)
  expect_true(max(w) <= 8.0)
  expect_true(min(w) >= 0.01)
  expect_lt(abs(sum(w) - n), 1e-6 * n)
})

# ── T6: auto capacity_penalty on large fixture ────────────────────────────────

test_that("ieppa_soft: auto capacity_penalty achieves max_err <= ieppa on stepstone", {
  skip_on_cran()
  skip_if_not_installed("arrow")
  skip_if_not_installed("jsonlite")
  skip_if(!file.exists("benchmarks/stepstone_fulldata_bench_data.parquet"),
          "stepstone fixture not available")
  df  <- arrow::read_parquet("benchmarks/stepstone_fulldata_bench_data.parquet")
  df$uuid <- NULL
  tgt <- jsonlite::fromJSON("benchmarks/stepstone_fulldata_bench_targets.json")
  tgt <- lapply(tgt, function(t) { t <- unlist(t); t / sum(t) })
  for (nm in names(tgt)) df[[nm]] <- factor(df[[nm]])

  r_hard <- leafblower::harvest(df, tgt, method = "ieppa",
                                max_weight = 5, max_iterations = 500L,
                                attach_weights = FALSE)
  r_soft <- leafblower::harvest(df, tgt, method = "ieppa_soft",
                                max_weight = 5, max_iterations = 500L,
                                attach_weights = FALSE)
  me_hard <- attr(r_hard, "result")$max_error
  me_soft <- attr(r_soft, "result")$max_error
  expect_lte(me_soft, me_hard + 1e-9)
})

# ── T7: adaptive μ growth observable on adversarial input ────────────────────

test_that("ieppa_soft: adaptive growth scales capacity_mu when primal violation persists", {
  set.seed(7L); n <- 2000L
  df  <- data.frame(v = factor(sample(c("X", "Y"), n, TRUE, prob = c(.3, .7))))
  tgt <- list(v = c(X = 0.99, Y = 0.01))
  r <- leafblower::harvest(df, tgt, method = "ieppa_soft",
                           capacity_penalty = 1e-6,
                           max_weight = 10, max_iterations = 300L,
                           attach_weights = FALSE)
  res <- attr(r, "result")
  expect_true(res$status %in% c(0L, 4L, 5L))
  expect_gt(res$alm_capacity_mu_final, 1e-6 * 1.5,
            label = "capacity_mu must have grown beyond initial 1e-6")
  expect_gt(res$alm_n_growth_events, 0L,
            label = "adaptive growth must have fired >= 1 time")
  w <- as.numeric(r)
  expect_true(max(w) <= 10)
})

# ── T8: wall-time budget — ieppa_soft within 1.5x of ieppa (interleaved) ─────

test_that("ieppa_soft wall-time within 1.5x of ieppa on stepstone (interleaved)", {
  skip_on_cran()
  skip_if_not_installed("bench")
  skip_if_not_installed("arrow")
  skip_if_not_installed("jsonlite")
  skip_if(!file.exists("benchmarks/stepstone_fulldata_bench_data.parquet"),
          "stepstone fixture not available")
  df  <- arrow::read_parquet("benchmarks/stepstone_fulldata_bench_data.parquet")
  df$uuid <- NULL
  tgt <- jsonlite::fromJSON("benchmarks/stepstone_fulldata_bench_targets.json")
  tgt <- lapply(tgt, function(t) { t <- unlist(t); t / sum(t) })
  for (nm in names(tgt)) df[[nm]] <- factor(df[[nm]])

  res_bench <- bench::mark(
    ieppa      = leafblower::harvest(df, tgt, method = "ieppa",
                                     max_iterations = 300L, attach_weights = FALSE),
    ieppa_soft = leafblower::harvest(df, tgt, method = "ieppa_soft",
                                     max_iterations = 300L, attach_weights = FALSE),
    iterations    = 3L,
    check         = FALSE,
    min_iterations = 3L,
    filter_gc      = FALSE
  )
  t_hard <- as.numeric(res_bench$median[res_bench$expression == "ieppa"])
  t_soft <- as.numeric(res_bench$median[res_bench$expression == "ieppa_soft"])
  expect_lt(t_soft, 1.5 * t_hard,
            label = sprintf("ieppa_soft median (%.2fs) must be within 1.5x of ieppa median (%.2fs)",
                            t_soft, t_hard))
})

# ── T9: backward compat — method='ieppa' bit-identical to pre-ALM fixture ─────

test_that("method='ieppa' produces bit-identical weights vs pre-ALM reference fixture", {
  fixture_path <- testthat::test_path("fixtures/ieppa_pre_alm_ref.rds")
  skip_if(!file.exists(fixture_path),
          "Step 0 fixture not captured — run data-raw/gen_ieppa_pre_alm_ref.R first")
  ref <- readRDS(fixture_path)

  r <- leafblower::harvest(ref$df, ref$tgt, method = "ieppa",
                           max_weight = ref$max_weight, min_weight = ref$min_weight,
                           max_iterations = ref$max_iterations,
                           convergence = ref$convergence, attach_weights = FALSE)
  w_post <- as.numeric(r)
  expect_equal(w_post, ref$weights, tolerance = 1e-12,
               label = "method='ieppa' must produce bit-identical weights pre/post ALM merge")
  res_post <- attr(r, "result")
  expect_equal(res_post$status,     ref$result$status)
  expect_equal(res_post$iterations, ref$result$iterations)
  expect_equal(res_post$max_error,  ref$result$max_error, tolerance = 1e-12)
})

# ── T10: capacity_penalty warns when used with non-ieppa_soft method ──────────

test_that("capacity_penalty emits warning when passed to non-ieppa_soft method", {
  set.seed(10L); n <- 100L
  df  <- data.frame(v = factor(sample(c("a", "b"), n, TRUE)))
  tgt <- list(v = c(a = 0.5, b = 0.5))
  expect_warning(
    leafblower::harvest(df, tgt, method = "ieppa", capacity_penalty = 0.5,
                        attach_weights = FALSE),
    regexp = "capacity_penalty.*ieppa_soft.*ignored"
  )
  expect_warning(
    leafblower::harvest(df, tgt, method = "raking", capacity_penalty = 0.5,
                        attach_weights = FALSE),
    regexp = "capacity_penalty.*ieppa_soft.*ignored"
  )
})
```

### Step 4: Run calibration-solvers tests — confirm RED state

At this point the ALM C++ solver (Epic D) may or may not be landed. If not landed, T3, T4 (auto value test), T5 (bounds difference from ieppa), T7 (adaptive growth) will FAIL. T10 (warning) and T4 (validation errors) will PASS immediately since they only test R-layer behavior.

- [ ] Run:
```bash
Rscript -e "devtools::test(filter='calibration-solvers')" 2>&1 | tail -20
```
Expected before Epic D lands: T3/T5/T6/T7/T8 FAIL; T4 (validation sub-test) and T10 PASS. Record which tests fail — this is the explicit RED state.

### Step 5: Commit

- [ ] Run:
```bash
git add tests/testthat/test-calibration-solvers.R
git commit -m "test(calibration-solvers): T3 strict; T4-T10 ALM correctness suite"
```

---

## Spec Coverage Self-Review

| Spec section | Task that covers it |
|---|---|
| `capacity_penalty = NULL` signature + validation | Task 1 Steps 2–3 |
| Warning on non-ieppa_soft use | Task 1 Step 4 |
| `.Call` slot 9 insertion | Task 1 Step 5 |
| `@param method` update for ieppa_soft | Task 1 Step 6 |
| `@param capacity_penalty` Roxygen | Task 1 Step 7 |
| `@details` ieppa vs ieppa_soft guidance | Task 1 Step 8 |
| `@return` 4 ALM diagnostic fields | Task 1 Step 9 |
| man/harvest.Rd verification counts (≥3, ≥4) | Task 1 Steps 11 |
| FAIL baseline lock at 3 | Task 1 Step 12 |
| B7 arity fix (32 args) | Task 2 |
| T3 strict `<` with 1e-6 gap | Task 3 Step 2 |
| T4 auto routing + validation suite | Task 3 Step 3 |
| T5 exact bounds + sum drift | Task 3 Step 3 |
| T6 stepstone auto μ | Task 3 Step 3 |
| T7 adaptive growth observable | Task 3 Step 3 |
| T8 interleaved wall-time 1.5× budget | Task 3 Step 3 |
| T9 backward compat fixture | Task 3 Step 3 |
| T10 warning regexp | Task 3 Step 3 |

**Placeholder scan**: No TBD/TODO/placeholder patterns. Every code block is complete and self-contained.

**Type consistency**: `capacity_penalty` is spelled identically across signature, validation block, `.Call` argument, Roxygen, and test calls. `alm_capacity_mu_final`, `alm_n_growth_events`, `alm_max_dual_norm`, `alm_sum_drift` are referenced identically in `@return` Roxygen and in T4/T7 test assertions.

---

**Plan complete and saved to `docs/superpowers/plans/2026-04-29-alm-epic-e-r-api.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** — execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
